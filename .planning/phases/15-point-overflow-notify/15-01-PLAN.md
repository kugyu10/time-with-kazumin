---
phase: 15-point-overflow-notify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260329000001_point_overflow_notify.sql
  - src/emails/PointOverflowEmail.tsx
  - src/lib/actions/admin/tasks.ts
  - src/app/admin/tasks/tasks-client.tsx
autonomous: true
requirements: [POINT-02, POINT-04, POINT-05]
must_haves:
  truths:
    - "task_execution_logs の CHECK 制約に point_overflow_notify が含まれている"
    - "TaskName 型に point_overflow_notify が含まれている"
    - "管理画面タスクフィルタに ポイント溢れ通知 が選択肢として表示される"
    - "PointOverflowEmail.tsx テンプレートファイルが存在し編集可能"
  artifacts:
    - path: "supabase/migrations/20260329000001_point_overflow_notify.sql"
      provides: "CHECK制約拡張 + pg_cronジョブ定義"
      contains: "point_overflow_notify"
    - path: "src/emails/PointOverflowEmail.tsx"
      provides: "React Email テンプレート（ファイルベース管理用）"
      exports: ["PointOverflowEmail", "PointOverflowEmailProps"]
    - path: "src/lib/actions/admin/tasks.ts"
      provides: "TaskName 型拡張"
      contains: "point_overflow_notify"
    - path: "src/app/admin/tasks/tasks-client.tsx"
      provides: "フィルタUI拡張"
      contains: "ポイント溢れ通知"
  key_links:
    - from: "supabase/migrations/20260329000001_point_overflow_notify.sql"
      to: "task_execution_logs"
      via: "CHECK constraint ALTER"
      pattern: "point_overflow_notify"
    - from: "src/lib/actions/admin/tasks.ts"
      to: "src/app/admin/tasks/tasks-client.tsx"
      via: "TaskName type import"
      pattern: "point_overflow_notify"
---

<objective>
Phase 15 Plan 01: ポイント溢れ通知の基盤整備 --- DB マイグレーション + TypeScript 型拡張 + React Email テンプレート作成

Purpose: Edge Function（Plan 02）が task_execution_logs に書き込める状態にし、管理画面でフィルタ可能にし、メールテンプレートをファイルベースで編集可能にする。
Output: マイグレーションSQL、React Email テンプレート、TypeScript 型更新
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-point-overflow-notify/15-CONTEXT.md
@.planning/phases/15-point-overflow-notify/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/actions/admin/tasks.ts:
```typescript
export type TaskStatus = "success" | "partial_success" | "failed"
export type TaskName = "monthly_point_grant" | "reminder_email" | "thank_you_email"
```

From src/app/admin/tasks/tasks-client.tsx:
```typescript
const taskNameOptions: { value: TaskName | "all"; label: string }[] = [
  { value: "all", label: "すべて" },
  { value: "monthly_point_grant", label: "月次ポイント付与" },
  { value: "reminder_email", label: "リマインダーメール" },
  { value: "thank_you_email", label: "サンキューメール" },
]
```

From supabase/migrations/20260223000001_automation_tasks.sql:
```sql
CREATE TABLE IF NOT EXISTS task_execution_logs (
    id SERIAL PRIMARY KEY,
    task_name TEXT NOT NULL CHECK (task_name IN ('monthly_point_grant', 'reminder_email', 'thank_you_email')),
    ...
);
```

From src/emails/BookingConfirmation.tsx (template pattern):
```typescript
import { Section, Text, Button, Hr, Link } from "@react-email/components"
import * as React from "react"
import { Layout } from "./components/Layout"

export interface BookingConfirmationProps { ... }
export function BookingConfirmation({ ... }: BookingConfirmationProps) {
  return <Layout>...</Layout>
}
export default BookingConfirmation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB マイグレーション --- CHECK 制約拡張 + pg_cron ジョブ定義</name>
  <files>supabase/migrations/20260329000001_point_overflow_notify.sql</files>
  <read_first>
    supabase/migrations/20260223000001_automation_tasks.sql
  </read_first>
  <action>
新規マイグレーションファイル `supabase/migrations/20260329000001_point_overflow_notify.sql` を作成する。

1. task_execution_logs の CHECK 制約を拡張する（per D-07, D-09）:
```sql
ALTER TABLE task_execution_logs
  DROP CONSTRAINT IF EXISTS task_execution_logs_task_name_check;

ALTER TABLE task_execution_logs
  ADD CONSTRAINT task_execution_logs_task_name_check
  CHECK (task_name IN (
    'monthly_point_grant',
    'reminder_email',
    'thank_you_email',
    'point_overflow_notify'
  ));
```

2. pg_cron ジョブ定義をコメントアウトで追加する（per D-08: UTC `0 0 20 * *` = JST 20日09:00）。
   既存の automation_tasks.sql のコメントアウトパターンに従い、Vault + pg_net を使用:
```sql
-- 毎月20日 00:00 UTC (JST 09:00) にポイント溢れ通知
-- SELECT cron.schedule(
--     'point-overflow-notify',
--     '0 0 20 * *',
--     $$
--     SELECT
--         net.http_post(
--             url:=vault.get_secret('edge_function_url') || '/point-overflow-notify',
--             headers:=jsonb_build_object('Authorization', 'Bearer ' || vault.get_secret('edge_function_anon_key')),
--             body:='{}'::jsonb
--         ) as request_id;
--     $$
-- );
```

ファイル冒頭にコメントヘッダーを追加（既存マイグレーションのスタイルに合わせる）。
  </action>
  <verify>
    <automated>grep -q "point_overflow_notify" supabase/migrations/20260329000001_point_overflow_notify.sql && grep -q "0 0 20" supabase/migrations/20260329000001_point_overflow_notify.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "point_overflow_notify" で CHECK 制約内に値が存在する
    - grep "0 0 20" で pg_cron スケジュールが定義されている（コメントアウト）
    - DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT パターンが使われている
  </acceptance_criteria>
  <done>マイグレーションファイルが存在し、CHECK制約拡張とpg_cronジョブ定義（コメントアウト）が含まれている</done>
</task>

<task type="auto">
  <name>Task 2: React Email テンプレート + TypeScript 型拡張 + 管理画面フィルタ追加</name>
  <files>
    src/emails/PointOverflowEmail.tsx
    src/lib/actions/admin/tasks.ts
    src/app/admin/tasks/tasks-client.tsx
  </files>
  <read_first>
    src/emails/BookingConfirmation.tsx
    src/emails/components/Layout.tsx
    src/lib/actions/admin/tasks.ts
    src/app/admin/tasks/tasks-client.tsx
  </read_first>
  <action>
**2a. React Email テンプレート作成** (per D-01, D-02, D-03, D-04, POINT-02)

`src/emails/PointOverflowEmail.tsx` を作成。BookingConfirmation.tsx のパターンに従う。

Props インターフェース:
```typescript
export interface PointOverflowEmailProps {
  userName: string
  currentPoints: number
  monthlyPoints: number
  maxPoints: number
  overflow: number       // = currentPoints + monthlyPoints - maxPoints (D-06)
  bookingUrl: string     // 予約ページへの CTA リンク (D-02)
}
```

テンプレート内容 (D-01 やさしいリマインダートーン):
- 見出し: `{userName}さん、ポイントがもったいないです!`
- 本文: やさしく友だち感覚で「来月のポイント付与で {overflow}ポイントが使えなくなってしまいます」と伝える
- 詳細セクション (D-03):
  - 現在のポイント: {currentPoints}ポイント
  - 月次付与ポイント: {monthlyPoints}ポイント
  - ポイント上限: {maxPoints}ポイント
  - 溢れるポイント: {overflow}ポイント
- CTA ボタン (D-02): 「今すぐ予約する」→ bookingUrl へのリンク
- フッター: Layout コンポーネント経由で「かずみん、時間空いてる？」

既存テンプレート同様に `@react-email/components` の Section, Text, Button, Hr を使用し、Layout でラップ。
export default PointOverflowEmail を忘れずに追加。

**2b. TaskName 型拡張** (per D-10, POINT-05)

`src/lib/actions/admin/tasks.ts` の TaskName 型に `"point_overflow_notify"` を追加:
```typescript
export type TaskName = "monthly_point_grant" | "reminder_email" | "thank_you_email" | "point_overflow_notify"
```

**2c. 管理画面フィルタ追加** (per D-10, POINT-05)

`src/app/admin/tasks/tasks-client.tsx` の `taskNameOptions` 配列に追加:
```typescript
{ value: "point_overflow_notify", label: "ポイント溢れ通知" },
```
既存の `thank_you_email` エントリの後に追加する。
  </action>
  <verify>
    <automated>grep -q "PointOverflowEmailProps" src/emails/PointOverflowEmail.tsx && grep -q "point_overflow_notify" src/lib/actions/admin/tasks.ts && grep -q "ポイント溢れ通知" src/app/admin/tasks/tasks-client.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/emails/PointOverflowEmail.tsx が存在し、PointOverflowEmailProps に currentPoints, monthlyPoints, maxPoints, overflow, bookingUrl が含まれる
    - TaskName 型に "point_overflow_notify" が含まれる
    - taskNameOptions に { value: "point_overflow_notify", label: "ポイント溢れ通知" } が含まれる
    - PointOverflowEmail が Layout コンポーネントを使用している
  </acceptance_criteria>
  <done>React Email テンプレートが存在し props に必要なフィールドがある。TaskName 型とフィルタ UI が拡張されている</done>
</task>

</tasks>

<verification>
- supabase/migrations/20260329000001_point_overflow_notify.sql が存在し CHECK 制約に point_overflow_notify が含まれる
- src/emails/PointOverflowEmail.tsx が存在し export されている
- src/lib/actions/admin/tasks.ts の TaskName に point_overflow_notify が含まれる
- src/app/admin/tasks/tasks-client.tsx のフィルタに ポイント溢れ通知 が含まれる
</verification>

<success_criteria>
- DB マイグレーションが Edge Function の task_execution_logs 書き込みを許容する CHECK 制約を含む
- React Email テンプレートがファイルベースで編集可能（POINT-02）
- 管理画面でポイント溢れ通知の実行状況をフィルタ可能（POINT-05）
</success_criteria>

<output>
After completion, create `.planning/phases/15-point-overflow-notify/15-01-SUMMARY.md`
</output>
