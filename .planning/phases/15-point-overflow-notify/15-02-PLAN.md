---
phase: 15-point-overflow-notify
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - supabase/functions/point-overflow-notify/index.ts
autonomous: true
requirements: [POINT-01, POINT-03, POINT-04]
must_haves:
  truths:
    - "Edge Function が溢れ予定会員を正しく検出する（current_points + monthly_points > max_points）"
    - "メール本文に現在ポイント・月次付与ポイント・上限・溢れ量が記載されている"
    - "同月に複数回実行しても2回目はスキップされる（冪等性）"
    - "task_execution_logs に送信件数（success_count, failed_count）が記録される"
  artifacts:
    - path: "supabase/functions/point-overflow-notify/index.ts"
      provides: "ポイント溢れ通知 Edge Function"
      min_lines: 150
      contains: "point_overflow_notify"
  key_links:
    - from: "supabase/functions/point-overflow-notify/index.ts"
      to: "task_execution_logs"
      via: "supabase.from('task_execution_logs').insert"
      pattern: "point_overflow_notify"
    - from: "supabase/functions/point-overflow-notify/index.ts"
      to: "member_plans JOIN plans"
      via: "supabase.from('member_plans').select"
      pattern: "current_points.*monthly_points.*max_points"
    - from: "supabase/functions/point-overflow-notify/index.ts"
      to: "Resend API"
      via: "fetch('https://api.resend.com/emails')"
      pattern: "api.resend.com"
---

<objective>
Phase 15 Plan 02: ポイント溢れ通知 Edge Function 実装

Purpose: 毎月20日に pg_cron から呼ばれる Edge Function を実装し、溢れ予定会員へリマインダーメールを送信する。冪等性チェック + task_execution_logs 記録 + リトライロジックを monthly-point-grant / check-reminder-emails パターンで踏襲。
Output: supabase/functions/point-overflow-notify/index.ts
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-point-overflow-notify/15-CONTEXT.md
@.planning/phases/15-point-overflow-notify/15-RESEARCH.md
@.planning/phases/15-point-overflow-notify/15-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor MUST follow -->

From supabase/functions/monthly-point-grant/index.ts (idempotency + structure pattern):
```typescript
import { createClient } from 'jsr:@supabase/supabase-js@2'

Deno.serve(async (req) => {
  const authHeader = req.headers.get('Authorization')
  if (!authHeader) {
    return new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, ... })
  }
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  )
  const startedAt = new Date().toISOString()
  // ... idempotency check, then main logic, then task_execution_logs insert
})
```

From supabase/functions/check-reminder-emails/index.ts (email sending pattern):
```typescript
const resendApiKey = Deno.env.get('RESEND_API_KEY')
const fromEmail = Deno.env.get('FROM_EMAIL')

// sendEmailWithRetry function:
async function sendEmailWithRetry(
  apiKey: string, from: string, to: string, subject: string, html: string, maxRetries = 3
): Promise<void> {
  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    try {
      const response = await fetch('https://api.resend.com/emails', {
        method: 'POST',
        headers: { 'Authorization': `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
        body: JSON.stringify({ from, to, subject, html }),
      })
      if (!response.ok) {
        const error = await response.text()
        throw new Error(`Resend API error: ${response.status} ${error}`)
      }
      return
    } catch (error) {
      console.error(`[Email] Attempt ${attempt}/${maxRetries} failed:`, error)
      if (attempt === maxRetries) throw error
    }
  }
}

// Inline HTML template pattern (NOT React Email import in Deno):
function renderBookingReminder(props: { ... }): string {
  return `<!DOCTYPE html><html>...</html>`
}
```

From src/emails/PointOverflowEmail.tsx (props reference, created in Plan 01):
```typescript
export interface PointOverflowEmailProps {
  userName: string
  currentPoints: number
  monthlyPoints: number
  maxPoints: number
  overflow: number
  bookingUrl: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ポイント溢れ通知 Edge Function 実装</name>
  <files>supabase/functions/point-overflow-notify/index.ts</files>
  <read_first>
    supabase/functions/monthly-point-grant/index.ts
    supabase/functions/check-reminder-emails/index.ts
    .planning/phases/15-point-overflow-notify/15-CONTEXT.md
    .planning/phases/15-point-overflow-notify/15-RESEARCH.md
  </read_first>
  <action>
`supabase/functions/point-overflow-notify/index.ts` を新規作成する。

monthly-point-grant の構造 + check-reminder-emails のメール送信パターンを組み合わせる。

**全体構造:**
```
1. 認証ヘッダー検証
2. Supabase クライアント作成（SERVICE_ROLE_KEY）
3. Resend 環境変数取得
4. 冪等性チェック（task_execution_logs ベース、per D-09）
5. 溢れ対象会員クエリ
6. メール送信ループ
7. task_execution_logs 記録
8. エラーハンドリング
```

**冪等性チェック (per D-09):**
task_execution_logs に当月分の `point_overflow_notify` レコード（status が success または partial_success）が存在する場合はスキップ。monthly-point-grant と同じ currentMonth 計算パターンを使用:
```typescript
const now = new Date()
const currentMonth = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`
```
ただしクエリは task_execution_logs から検索する（monthly-point-grant は point_transactions から検索するが、D-09 で task_execution_logs ベースと確定）:
```typescript
const { data: existingLog, error: checkError } = await supabase
  .from('task_execution_logs')
  .select('id')
  .eq('task_name', 'point_overflow_notify')
  .in('status', ['success', 'partial_success'])
  .gte('started_at', `${currentMonth}-01T00:00:00Z`)
  .lt('started_at', `${currentMonth}-31T23:59:59Z`)
  .limit(1)
  .single()
```
既に存在する場合、status: 'success' でログを記録し「Already processed this month」を返す。

**溢れ対象会員クエリ (per D-05):**
member_plans を profiles と plans で JOIN し、アクティブ会員を取得。max_points が NULL のプランは除外する（Pitfall 2 対策）:
```typescript
const { data: members } = await supabase
  .from('member_plans')
  .select(`
    id,
    current_points,
    monthly_points,
    user:profiles!inner (
      email,
      full_name
    ),
    plan:plans!inner (
      max_points
    )
  `)
  .eq('status', 'active')
  .not('plan.max_points', 'is', null)
```
アプリ層で `current_points + monthly_points > plan.max_points` をフィルタ (D-05)。
overflow の計算: `current_points + monthly_points - plan.max_points` (D-06)。

**メール送信ループ (check-reminder-emails パターン踏襲):**
- successCount, failedCount, errors 配列で結果を追跡
- 各会員に対して sendEmailWithRetry でメール送信（maxRetries = 3）
- 件名: `【かずみん時間】ポイントがもったいないです！`
- HTML: renderPointOverflowHtml 関数でインライン HTML を生成（Deno 環境では React Email を使わない）
- CTA リンク先 (D-02): `${Deno.env.get('SITE_URL') || 'https://time-with-kazumin.vercel.app'}/bookings/new`

**インライン HTML テンプレート関数 renderPointOverflowHtml:**
check-reminder-emails の renderBookingReminder と同じパターンで、以下の情報を含める (D-01, D-03):
- 見出し: `{userName}さん、ポイントがもったいないです!`
- 本文（やさしいトーン per D-01）: 来月のポイント付与で溢れる旨を友だち感覚で伝える
- ポイント詳細セクション（背景色 #f9fafb、角丸 8px）:
  - 現在のポイント: {currentPoints}ポイント
  - 月次付与: +{monthlyPoints}ポイント
  - ポイント上限: {maxPoints}ポイント
  - 溢れるポイント: {overflow}ポイント（赤色 #dc2626 で強調）
- CTA ボタン（青色 #2563eb、角丸 8px）: 「今すぐ予約する」
- フッター: 「かずみん、時間空いてる？」

**task_execution_logs 記録 (per D-07, D-11):**
送信完了後に status（success / partial_success / failed）、total_count、success_count、failed_count を記録。
details に `{ month: currentMonth, targets_found: targets.length }` を含める。
error_details にエラー配列（あれば）を含める。

**sendEmailWithRetry 関数:**
check-reminder-emails/index.ts L341-380 のパターンをそのまま複製する（Resend fetch API 直接呼び出し、3回リトライ）。

**エラーハンドリング:**
monthly-point-grant と同じパターン: try/catch で全体をラップし、失敗時は task_execution_logs に status: 'failed' を記録。
  </action>
  <verify>
    <automated>grep -q "point_overflow_notify" supabase/functions/point-overflow-notify/index.ts && grep -q "sendEmailWithRetry" supabase/functions/point-overflow-notify/index.ts && grep -q "current_points" supabase/functions/point-overflow-notify/index.ts && grep -q "api.resend.com" supabase/functions/point-overflow-notify/index.ts && grep -q "task_execution_logs" supabase/functions/point-overflow-notify/index.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Edge Function ファイルが存在する
    - 冪等性チェックが task_execution_logs ベースで実装されている（grep "point_overflow_notify" + grep "task_execution_logs"）
    - 溢れ対象クエリが member_plans + plans JOIN で実装されている（grep "member_plans" + grep "max_points"）
    - アプリ層フィルタで current_points + monthly_points > max_points を判定している
    - sendEmailWithRetry が Resend fetch API で実装されている（grep "api.resend.com"）
    - インライン HTML テンプレートに currentPoints, monthlyPoints, maxPoints, overflow が含まれている
    - task_execution_logs に success_count, failed_count が記録されている
    - 認証ヘッダー検証がある
  </acceptance_criteria>
  <done>Edge Function が冪等性チェック + 溢れ対象クエリ + メール送信ループ + task_execution_logs 記録を含み、monthly-point-grant / check-reminder-emails パターンに従っている</done>
</task>

</tasks>

<verification>
- supabase/functions/point-overflow-notify/index.ts が存在する
- 冪等性: task_execution_logs で当月チェック → 存在時スキップ
- 溢れ判定: current_points + monthly_points > max_points (D-05)
- メール送信: Resend fetch API + リトライ (sendEmailWithRetry)
- ログ記録: task_execution_logs に success_count / failed_count (D-11)
- HTML: インライン HTML にポイント詳細 + CTA ボタン (D-01, D-02, D-03)
</verification>

<success_criteria>
- Edge Function が monthly-point-grant / check-reminder-emails の確立済みパターンに従っている
- 溢れ予定会員の検出ロジックが D-05 に準拠している（POINT-01）
- メール本文にポイント詳細4項目が含まれている（POINT-03）
- 冪等性が task_execution_logs ベースで担保されている（POINT-04）
</success_criteria>

<output>
After completion, create `.planning/phases/15-point-overflow-notify/15-02-SUMMARY.md`
</output>
