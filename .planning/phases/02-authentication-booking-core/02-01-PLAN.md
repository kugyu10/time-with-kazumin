---
phase: 02-authentication-booking-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/auth/callback/route.ts
  - src/app/(member)/layout.tsx
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/middleware.ts
  - src/components/auth/GoogleSignInButton.tsx
  - src/components/auth/LoginForm.tsx
autonomous: true
requirements:
  - MEMBER-01

must_haves:
  truths:
    - "会員はGoogleアカウントでログインできる"
    - "会員はメール/パスワードでログインできる"
    - "ログイン成功後、予約ページへリダイレクトされる"
    - "未認証ユーザーは会員ページにアクセスできない"
    - "招待されていないユーザーはログインできない"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "ログインページUI"
      min_lines: 50
    - path: "src/app/auth/callback/route.ts"
      provides: "OAuth認証コールバック処理"
      exports: ["GET"]
    - path: "src/lib/supabase/server.ts"
      provides: "SSR用Supabaseクライアント"
      exports: ["createServerClient"]
    - path: "src/middleware.ts"
      provides: "認証ミドルウェア"
      exports: ["middleware", "config"]
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "supabase.auth.signInWithOAuth"
      via: "Google OAuth呼び出し"
      pattern: "signInWithOAuth.*google"
    - from: "src/app/auth/callback/route.ts"
      to: "supabase.auth.exchangeCodeForSession"
      via: "コード交換"
      pattern: "exchangeCodeForSession"
    - from: "src/middleware.ts"
      to: "/login"
      via: "未認証リダイレクト"
      pattern: "redirect.*login"
---

<objective>
Supabase Authを使った会員認証基盤の構築

Purpose: 会員がGoogle OAuth/メール認証でログインし、保護されたページにアクセスできるようにする
Output: ログインページ、OAuthコールバック、認証ミドルウェア、Supabaseクライアント
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-booking-core/02-CONTEXT.md
@.planning/phases/02-authentication-booking-core/02-RESEARCH.md
@.planning/phases/01-database-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabaseクライアント3パターン実装</name>
  <files>
    src/lib/supabase/client.ts
    src/lib/supabase/server.ts
    src/lib/supabase/middleware.ts
  </files>
  <action>
    @supabase/ssrパッケージを使用して3種類のSupabaseクライアントを実装。

    **client.ts（ブラウザ用）:**
    - `createBrowserClient`を使用
    - 環境変数: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`

    **server.ts（Server Components/Route Handlers用）:**
    - `createServerClient`を使用
    - `cookies()`からの読み取り専用パターン
    - cookiesオブジェクトをget/set/removeメソッドで渡す

    **middleware.ts（ミドルウェア用）:**
    - `createServerClient`を使用
    - NextResponseのcookies操作を使用
    - セッションリフレッシュ用

    既存の`@supabase/supabase-js`は削除せず、SSR用に`@supabase/ssr`を追加。
    `npm install @supabase/ssr`を実行。
  </action>
  <verify>
    - `npm install @supabase/ssr`が成功
    - 各ファイルが正しくexportされている
    - TypeScriptエラーがない（`npx tsc --noEmit`）
  </verify>
  <done>
    3パターンのSupabaseクライアントが実装され、TypeScriptコンパイルが通る
  </done>
</task>

<task type="auto">
  <name>Task 2: ログインページとOAuthコールバック実装</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/auth/callback/route.ts
    src/components/auth/GoogleSignInButton.tsx
    src/components/auth/LoginForm.tsx
  </files>
  <action>
    CONTEXT.mdの決定事項に従い実装:
    - Google OAuth優先（「Googleでログイン」を大きく表示）
    - メール/パスワードは代替手段として小さく表示

    **login/page.tsx:**
    - Server Component（認証済みなら/bookings/newへリダイレクト）
    - GoogleSignInButtonとLoginFormを配置
    - 招待制の注意書き表示

    **GoogleSignInButton.tsx（Client Component）:**
    - `use client`ディレクティブ
    - `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: '/auth/callback' } })`
    - shadcn/ui Buttonを使用（variant: default、サイズ大）

    **LoginForm.tsx（Client Component）:**
    - `use client`ディレクティブ
    - メール/パスワード入力フォーム
    - `supabase.auth.signInWithPassword`使用
    - エラー表示: インラインで赤文字
    - ローディング状態: ボタン無効化 + スピナー

    **auth/callback/route.ts:**
    - GET Route Handler
    - `searchParams.get('code')`からコード取得
    - `exchangeCodeForSession`でセッション確立
    - 成功時: `/bookings/new`へリダイレクト
    - エラー時: `/login?error=auth_error`へリダイレクト

    スタイリング: Tailwind CSSでオレンジ系ブランドカラーを使用。
  </action>
  <verify>
    - `npm run build`が成功
    - `/login`ページがレンダリングされる
    - GoogleSignInButtonがクリック可能
    - LoginFormがバリデーション付きで動作
  </verify>
  <done>
    ログインページとOAuthコールバックが実装され、ビルドが通る
  </done>
</task>

<task type="auto">
  <name>Task 3: 認証ミドルウェアと会員レイアウト実装</name>
  <files>
    src/middleware.ts
    src/app/(member)/layout.tsx
  </files>
  <action>
    **middleware.ts:**
    - `matcher`で保護対象パスを指定: `/dashboard/:path*`, `/bookings/:path*`
    - セッションリフレッシュ（`supabase.auth.getUser()`呼び出し）
    - 未認証の場合: `/login`へリダイレクト
    - `/login`アクセス時に認証済みの場合: `/bookings/new`へリダイレクト

    **(member)/layout.tsx:**
    - Server Component
    - `supabase.auth.getUser()`で認証チェック
    - 未認証: `redirect('/login')`
    - 認証済み: childrenをレンダリング

    招待制チェック（v1要件）:
    - profilesテーブルにuser_idが存在するか確認
    - 存在しない場合: ログアウトして`/login?error=not_invited`へリダイレクト
    - これはSupabase Dashboardで管理者がprofilesにレコードを追加することで招待とする

    Note: 完全な招待フローはPhase 5で実装。Phase 2ではprofiles存在チェックのみ。
  </action>
  <verify>
    - `npm run build`が成功
    - 未認証で`/dashboard`アクセス時に`/login`へリダイレクト
    - 認証済みで`/login`アクセス時に`/bookings/new`へリダイレクト
  </verify>
  <done>
    認証ミドルウェアが機能し、保護ページへの未認証アクセスがブロックされる
  </done>
</task>

</tasks>

<verification>
1. `npm run build` が成功する
2. `npx tsc --noEmit` でTypeScriptエラーがない
3. `/login` ページが正しく表示される
4. Google OAuthボタンがクリックでSupabase認証フローを開始する
5. メール/パスワードフォームが入力バリデーション付きで動作する
6. 未認証状態で`/dashboard`にアクセスすると`/login`にリダイレクトされる
</verification>

<success_criteria>
- MEMBER-01要件を満たす: Google認証とメール/パスワード認証の両方が利用可能
- ログイン成功後、予約ページ（/bookings/new）へリダイレクトされる
- 招待制チェック（profilesテーブル存在確認）が機能する
- 認証ガードが全ての保護ページで機能する
</success_criteria>

<output>
完了後、`.planning/phases/02-authentication-booking-core/02-01-SUMMARY.md`を作成
</output>
