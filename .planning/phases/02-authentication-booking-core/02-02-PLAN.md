---
phase: 02-authentication-booking-core
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/api/bookings/route.ts
  - src/app/(member)/bookings/new/page.tsx
  - src/app/(member)/bookings/confirm/page.tsx
  - src/app/(member)/dashboard/page.tsx
  - src/lib/bookings/saga.ts
  - src/lib/bookings/types.ts
  - src/lib/integrations/zoom.ts
  - src/lib/integrations/google-calendar.ts
  - src/lib/integrations/email.ts
  - src/lib/utils/idempotency.ts
  - src/components/bookings/SlotPicker.tsx
  - src/components/bookings/MenuSelect.tsx
  - src/components/bookings/BookingConfirm.tsx
  - src/components/dashboard/PointBalance.tsx
  - src/components/layout/Header.tsx
  - supabase/migrations/20260222100001_idempotency_keys.sql
autonomous: true
requirements:
  - MEMBER-02
  - MEMBER-03

must_haves:
  truths:
    - "会員はメニューを選択して予約できる"
    - "予約時にポイントが消費される"
    - "確認画面でメニュー、日時、消費ポイントを確認できる"
    - "会員は現在のポイント残高を確認できる（ヘッダー＋ダッシュボード）"
    - "予約失敗時にポイントが返還される（補償処理）"
    - "同じリクエストで二重予約されない（冪等性）"
  artifacts:
    - path: "src/app/api/bookings/route.ts"
      provides: "予約作成API（Sagaオーケストレーター）"
      exports: ["POST"]
    - path: "src/lib/bookings/saga.ts"
      provides: "Sagaパターン実装（補償トランザクション）"
      exports: ["createBookingSaga"]
    - path: "src/lib/utils/idempotency.ts"
      provides: "冪等性キー管理"
      exports: ["checkIdempotencyKey", "saveIdempotencyKey"]
    - path: "src/app/(member)/bookings/new/page.tsx"
      provides: "予約作成ページ（スロット＋メニュー選択）"
      min_lines: 100
    - path: "src/components/dashboard/PointBalance.tsx"
      provides: "ポイント残高表示コンポーネント"
      min_lines: 30
  key_links:
    - from: "src/app/api/bookings/route.ts"
      to: "src/lib/bookings/saga.ts"
      via: "createBookingSaga呼び出し"
      pattern: "createBookingSaga"
    - from: "src/lib/bookings/saga.ts"
      to: "supabase.rpc('consume_points')"
      via: "ポイント消費"
      pattern: "consume_points"
    - from: "src/lib/bookings/saga.ts"
      to: "supabase.rpc('refund_points')"
      via: "補償トランザクション"
      pattern: "refund_points"
    - from: "src/app/(member)/bookings/new/page.tsx"
      to: "/api/bookings"
      via: "予約作成リクエスト"
      pattern: "fetch.*api/bookings"
---

<objective>
Sagaパターンによる予約作成フローとポイント残高表示の実装

Purpose: 会員がメニューを選択してポイント消費で予約でき、失敗時は補償処理でポイント返還される
Output: 予約作成API、Sagaオーケストレーター、予約フローUI、ポイント残高表示
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-booking-core/02-CONTEXT.md
@.planning/phases/02-authentication-booking-core/02-RESEARCH.md
@.planning/phases/01-database-foundation/01-01-SUMMARY.md
@.planning/phases/01-database-foundation/01-02-SUMMARY.md
@.planning/phases/02-authentication-booking-core/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 冪等性キーテーブルとユーティリティ実装</name>
  <files>
    supabase/migrations/20260222100001_idempotency_keys.sql
    src/lib/utils/idempotency.ts
  </files>
  <action>
    **マイグレーション（idempotency_keys.sql）:**
    ```sql
    CREATE TABLE idempotency_keys (
      id SERIAL PRIMARY KEY,
      key TEXT NOT NULL UNIQUE,
      request_hash TEXT NOT NULL,
      response JSONB NOT NULL,
      created_at TIMESTAMPTZ DEFAULT NOW(),
      expires_at TIMESTAMPTZ NOT NULL DEFAULT (NOW() + INTERVAL '24 hours')
    );

    CREATE INDEX idx_idempotency_keys_expires_at ON idempotency_keys(expires_at);
    CREATE INDEX idx_idempotency_keys_key ON idempotency_keys(key);
    ```

    RLSは不要（service_role経由でのみアクセス）。

    **idempotency.ts:**
    - `checkIdempotencyKey(key, requestHash)`: 既存キーチェック
      - 存在＆ハッシュ一致: 前回レスポンス返却
      - 存在＆ハッシュ不一致: ConflictError
      - 存在しない: null
    - `saveIdempotencyKey(key, requestHash, response)`: キー保存
    - `hashRequest(body)`: SHA-256でリクエストボディをハッシュ化
    - `npm install nanoid`でキー生成ライブラリ追加
  </action>
  <verify>
    - `supabase migration up`が成功
    - `idempotency_keys`テーブルが作成されている
    - TypeScriptファイルがコンパイルエラーなし
  </verify>
  <done>
    冪等性キー管理の基盤が実装され、マイグレーションが適用されている
  </done>
</task>

<task type="auto">
  <name>Task 2: モック統合サービスとSagaオーケストレーター実装</name>
  <files>
    src/lib/integrations/zoom.ts
    src/lib/integrations/google-calendar.ts
    src/lib/integrations/email.ts
    src/lib/bookings/types.ts
    src/lib/bookings/saga.ts
  </files>
  <action>
    **Phase 2ではモック実装（Phase 4で本実装に置換）**

    **zoom.ts（モック）:**
    ```typescript
    export async function createZoomMeeting(params: { topic: string; start_time: string; duration: number }) {
      console.log('[MOCK] Creating Zoom meeting:', params);
      return {
        zoom_meeting_id: `mock-${Date.now()}`,
        zoom_join_url: `https://zoom.us/j/mock-${Date.now()}`,
      };
    }

    export async function deleteZoomMeeting(meetingId: string) {
      console.log('[MOCK] Deleting Zoom meeting:', meetingId);
      return { success: true };
    }
    ```

    **google-calendar.ts（モック）:**
    ```typescript
    export async function addCalendarEvent(params: { summary: string; start: string; end: string }) {
      console.log('[MOCK] Adding calendar event:', params);
      return { google_event_id: `mock-event-${Date.now()}` };
    }

    export async function deleteCalendarEvent(eventId: string) {
      console.log('[MOCK] Deleting calendar event:', eventId);
      return { success: true };
    }
    ```

    **email.ts（モック）:**
    ```typescript
    export async function sendBookingConfirmationEmail(params: { to: string; bookingDetails: any }) {
      console.log('[MOCK] Sending confirmation email:', params);
      return { success: true };
    }
    ```

    **types.ts:**
    - `BookingRequest`: { member_plan_id, menu_id, start_time, end_time, idempotency_key? }
    - `BookingResponse`: { id, status, zoom_join_url?, error? }
    - `SagaStep`: { name, execute, compensate }

    **saga.ts（Sagaオーケストレーター）:**
    - `createBookingSaga(request, supabase)`
    - ステップ順序:
      1. 冪等性チェック
      2. スロット空き確認（bookingsテーブルで重複チェック）
      3. ポイント消費（`supabase.rpc('consume_points')`）
      4. 予約レコード作成（status: 'pending'）
      5. Zoom会議作成（モック）
      6. Calendar追加（モック）
      7. 予約確定（status: 'confirmed'）
      8. メール送信（モック、失敗しても継続）
    - 失敗時は逆順で補償実行
    - リトライロジック: ロック競合（55P03）時は指数バックオフ（最大3回）
  </action>
  <verify>
    - TypeScriptコンパイルエラーなし
    - saga.tsがcreateBookingSagaをexport
    - 各モック関数がconsole.logで出力
  </verify>
  <done>
    Sagaオーケストレーターが実装され、補償トランザクションが各ステップに定義されている
  </done>
</task>

<task type="auto">
  <name>Task 3: 予約作成APIと予約フローUI実装</name>
  <files>
    src/app/api/bookings/route.ts
    src/app/(member)/bookings/new/page.tsx
    src/app/(member)/bookings/confirm/page.tsx
    src/components/bookings/SlotPicker.tsx
    src/components/bookings/MenuSelect.tsx
    src/components/bookings/BookingConfirm.tsx
    src/components/dashboard/PointBalance.tsx
    src/components/layout/Header.tsx
    src/app/(member)/dashboard/page.tsx
  </files>
  <action>
    **CONTEXT.mdの決定事項に従い実装:**
    - メニュー選択を先に行い、その長さに合う空きスロットを表示
    - 確認画面必須

    **api/bookings/route.ts:**
    - POST: 認証チェック → Sagaオーケストレーター呼び出し
    - Idempotency-Keyヘッダーから冪等性キー取得（なければnanoid生成）
    - エラーレスポンス: { error: string, code?: string }
    - 成功レスポンス: { booking: BookingResponse }

    **bookings/new/page.tsx:**
    - Client Component（状態管理が必要）
    - ステップ1: MenuSelect（メニュー選択）
    - ステップ2: SlotPicker（カレンダーで空きスロット選択）
    - 選択完了後、確認画面へ遷移（URLパラメータで渡す）

    **MenuSelect.tsx:**
    - meeting_menusテーブルからアクティブなメニュー取得
    - カード形式で表示（名前、時間、ポイント数）
    - 選択状態をハイライト

    **SlotPicker.tsx:**
    - 週表示カレンダー（CONTEXT.mdの決定）
    - 選択したメニューの時間に合うスロットのみ表示
    - weekly_schedulesから営業時間取得
    - 既存bookingsと重複チェック
    - クリックでスロット選択

    **bookings/confirm/page.tsx:**
    - URLパラメータから選択内容取得（menu_id, start_time）
    - メニュー詳細、日時、消費ポイントを表示
    - 「予約する」ボタンで`POST /api/bookings`呼び出し
    - 成功: `/bookings/complete`へ（Plan 02-03で実装、一旦ダッシュボードへ）
    - 失敗: インラインエラー表示（CONTEXT.md決定）

    **BookingConfirm.tsx:**
    - 確認画面のカード表示
    - ローディング状態でボタン無効化

    **PointBalance.tsx:**
    - member_plansからcurrent_points取得
    - 数字のみ簡易表示（ヘッダー用）
    - 詳細表示（ダッシュボード用、プラン名＋最大ポイント含む）

    **Header.tsx:**
    - PointBalance（簡易版）を右上に配置
    - ログアウトボタン

    **dashboard/page.tsx:**
    - PointBalance（詳細版）を表示
    - 今後の予約概要（件数のみ、詳細はPlan 02-03）
  </action>
  <verify>
    - `npm run build`が成功
    - `/bookings/new`ページが表示される
    - メニュー選択 → スロット選択 → 確認画面のフローが動作
    - `POST /api/bookings`がSagaオーケストレーターを呼び出す
    - ポイント残高がヘッダーとダッシュボードに表示される
  </verify>
  <done>
    予約作成フローが完成し、ポイント消費・補償処理が動作する
  </done>
</task>

</tasks>

<verification>
1. `npm run build` が成功する
2. `supabase migration up` が成功する
3. `/bookings/new` で予約フロー（メニュー選択→スロット選択→確認）が動作する
4. `POST /api/bookings` でポイント消費が発生する（consume_points呼び出し確認）
5. Saga失敗時に補償処理が実行される（refund_points呼び出し確認）
6. 同じIdempotency-Keyで再リクエストすると前回レスポンスが返る
7. ヘッダーとダッシュボードにポイント残高が表示される
</verification>

<success_criteria>
- MEMBER-02要件を満たす: メニュー選択してポイント消費で予約できる
- MEMBER-03要件を満たす: ポイント残高をヘッダー＋ダッシュボードで確認できる
- 確認画面でメニュー、日時、消費ポイントを確認してから予約確定できる
- Sagaパターンによる補償トランザクションが実装されている
- 冪等性キーによる二重予約防止が機能する
</success_criteria>

<output>
完了後、`.planning/phases/02-authentication-booking-core/02-02-SUMMARY.md`を作成
</output>
