---
phase: 05-admin-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/components/admin/sidebar.tsx
  - src/components/ui/data-table.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/form.tsx
  - src/components/ui/select.tsx
  - src/components/ui/dialog.tsx
  - src/lib/actions/admin/menus.ts
  - src/lib/actions/admin/plans.ts
  - src/app/(admin)/menus/page.tsx
  - src/app/(admin)/menus/columns.tsx
  - src/components/admin/forms/menu-form.tsx
  - src/app/(admin)/plans/page.tsx
  - src/app/(admin)/plans/columns.tsx
  - src/components/admin/forms/plan-form.tsx
autonomous: true
requirements:
  - ADMIN-06
  - ADMIN-07

must_haves:
  truths:
    - "管理者は管理画面にアクセスできる"
    - "管理者はメニュー一覧を確認できる"
    - "管理者はメニューを作成・編集・削除できる"
    - "管理者はプラン一覧を確認できる"
    - "管理者はプランを作成・編集・削除できる"
  artifacts:
    - path: "src/app/(admin)/layout.tsx"
      provides: "管理画面共通レイアウト（認証チェック付き）"
      contains: "role !== 'admin'"
    - path: "src/components/admin/sidebar.tsx"
      provides: "管理画面ナビゲーション"
      min_lines: 30
    - path: "src/components/ui/data-table.tsx"
      provides: "TanStack Table統合データテーブル"
      contains: "@tanstack/react-table"
    - path: "src/lib/actions/admin/menus.ts"
      provides: "メニューCRUD Server Actions"
      exports: ["createMenu", "updateMenu", "deleteMenu"]
    - path: "src/lib/actions/admin/plans.ts"
      provides: "プランCRUD Server Actions"
      exports: ["createPlan", "updatePlan", "deletePlan"]
  key_links:
    - from: "src/app/(admin)/menus/page.tsx"
      to: "src/lib/actions/admin/menus.ts"
      via: "Server Action呼び出し"
      pattern: "createMenu|updateMenu|deleteMenu"
    - from: "src/app/(admin)/plans/page.tsx"
      to: "src/lib/actions/admin/plans.ts"
      via: "Server Action呼び出し"
      pattern: "createPlan|updatePlan|deletePlan"
---

<objective>
管理画面の共通基盤とメニュー・プランのCRUD機能を実装する

Purpose: 管理者がセッション種別（メニュー）とサブスクリプションプラン（プラン）を管理できるようにする
Output: 管理画面レイアウト、サイドバー、データテーブル、メニューCRUD、プランCRUD
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-features/05-RESEARCH.md

@src/lib/supabase/server.ts
@src/lib/supabase/service-role.ts
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 管理画面基盤セットアップ</name>
  <files>
    src/app/(admin)/layout.tsx
    src/app/(admin)/dashboard/page.tsx
    src/components/admin/sidebar.tsx
    src/components/ui/data-table.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/form.tsx
    src/components/ui/select.tsx
    src/components/ui/dialog.tsx
    src/components/ui/checkbox.tsx
  </files>
  <action>
    1. shadcn/uiコンポーネント追加:
       ```bash
       pnpm dlx shadcn@latest add table dropdown-menu dialog select checkbox
       pnpm dlx shadcn@latest add form
       pnpm add @tanstack/react-table react-hook-form @hookform/resolvers zod
       ```

    2. Data Table共通コンポーネント作成 (src/components/ui/data-table.tsx):
       - TanStack Tableをラップしたshadcn/ui Data Table
       - ソート、ページネーション機能
       - 行アクション用ドロップダウンメニュー対応

    3. Admin Layout作成 (src/app/(admin)/layout.tsx):
       - Server Componentで認証・認可チェック
       - createServerClient().auth.getUser()でユーザー取得
       - profilesテーブルでrole === 'admin'チェック
       - 非管理者は/にリダイレクト
       - サイドバー + メインコンテンツのフレックスレイアウト

    4. Admin Sidebar作成 (src/components/admin/sidebar.tsx):
       - ナビゲーションリンク: ダッシュボード、営業時間、予約管理、会員管理、メニュー管理、プラン管理
       - 現在のパス（usePathname）でアクティブ状態表示
       - lucide-reactアイコン使用

    5. Admin Dashboard作成 (src/app/(admin)/dashboard/page.tsx):
       - 簡易な概要ページ（各リソースへのリンク）

    注意: Middlewareだけでなく、layout.tsx内で必ず認証・認可チェックを実施（CVE-2025-29927対策）
  </action>
  <verify>
    - npm run build が成功する
    - /admin/dashboard にアクセスすると管理者のみ表示される（非管理者はリダイレクト）
  </verify>
  <done>
    - 管理画面レイアウトとサイドバーが表示される
    - Data Tableコンポーネントが使用可能
    - 非管理者は管理画面にアクセスできない
  </done>
</task>

<task type="auto">
  <name>Task 2: メニューCRUD実装</name>
  <files>
    src/lib/actions/admin/menus.ts
    src/app/(admin)/menus/page.tsx
    src/app/(admin)/menus/columns.tsx
    src/components/admin/forms/menu-form.tsx
  </files>
  <action>
    1. Server Actions作成 (src/lib/actions/admin/menus.ts):
       - 'use server'ディレクティブ
       - 各関数内で認証・認可チェック（Defense-in-Depth）
       - Zodスキーマでバリデーション
       - service_roleクライアントでRLSバイパス

       createMenu(data):
         - name: string (1-100文字)
         - duration_minutes: number (15-480)
         - points_required: number (0以上)
         - zoom_account: 'A' | 'B'
         - description: string (optional)
         - is_active: boolean
         - revalidatePath('/admin/menus')

       updateMenu(id, data):
         - 同上のバリデーション
         - meeting_menus.updateで更新

       deleteMenu(id):
         - 論理削除（is_active = false）または物理削除

    2. カラム定義 (src/app/(admin)/menus/columns.tsx):
       - 'use client'
       - ColumnDef<Menu>[]定義
       - カラム: メニュー名、時間(分)、ポイント、Zoomアカウント、ステータス、アクション
       - アクションドロップダウン: 編集、削除

    3. メニュー一覧ページ (src/app/(admin)/menus/page.tsx):
       - Server Componentでservice_roleクライアント使用
       - meeting_menusテーブルから全件取得
       - DataTableコンポーネントに渡す
       - 新規作成ボタン（Dialogで表示）

    4. メニューフォーム (src/components/admin/forms/menu-form.tsx):
       - 'use client'
       - React Hook Form + zodResolver
       - shadcn/ui FormコンポーネントでUI
       - Server Action呼び出し
       - 成功時にDialogを閉じる
  </action>
  <verify>
    - /admin/menus でメニュー一覧が表示される
    - 新規作成ダイアログでメニューを追加できる
    - 編集・削除がDBに反映される
  </verify>
  <done>
    - 管理者はメニュー一覧を確認できる
    - 管理者はメニューを作成・編集・削除できる
  </done>
</task>

<task type="auto">
  <name>Task 3: プランCRUD実装</name>
  <files>
    src/lib/actions/admin/plans.ts
    src/app/(admin)/plans/page.tsx
    src/app/(admin)/plans/columns.tsx
    src/components/admin/forms/plan-form.tsx
  </files>
  <action>
    1. Server Actions作成 (src/lib/actions/admin/plans.ts):
       - Task 2と同様のパターン
       - 各関数内で認証・認可チェック

       createPlan(data):
         - name: string (1-100文字)
         - monthly_points: number (0以上)
         - price_yen: number (0以上)
         - description: string (optional)
         - is_active: boolean
         - revalidatePath('/admin/plans')

       updatePlan(id, data):
         - 同上

       deletePlan(id):
         - 論理削除（is_active = false）

    2. カラム定義 (src/app/(admin)/plans/columns.tsx):
       - カラム: プラン名、月間ポイント、価格(円)、ステータス、アクション
       - アクションドロップダウン: 編集、削除

    3. プラン一覧ページ (src/app/(admin)/plans/page.tsx):
       - plansテーブルから全件取得
       - DataTableコンポーネントに渡す
       - 新規作成ボタン

    4. プランフォーム (src/components/admin/forms/plan-form.tsx):
       - React Hook Form + Zod
       - Server Action呼び出し
  </action>
  <verify>
    - /admin/plans でプラン一覧が表示される
    - 新規作成ダイアログでプランを追加できる
    - 編集・削除がDBに反映される
  </verify>
  <done>
    - 管理者はプラン一覧を確認できる
    - 管理者はプランを作成・編集・削除できる
  </done>
</task>

</tasks>

<verification>
- npm run build 成功
- npm run lint 成功
- /admin/* ルートが管理者のみアクセス可能
- メニューCRUD操作がDBに反映
- プランCRUD操作がDBに反映
</verification>

<success_criteria>
- 管理画面の共通基盤（レイアウト、サイドバー、Data Table）が構築されている
- メニュー（meeting_menus）のCRUD機能が動作する
- プラン（plans）のCRUD機能が動作する
- 全Server Actionで認証・認可チェックが実施されている
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-features/05-01-SUMMARY.md`
</output>
