---
phase: 05-admin-features
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/lib/actions/admin/bookings.ts
  - src/app/(admin)/bookings/page.tsx
  - src/app/(admin)/bookings/columns.tsx
  - src/components/admin/booking-status-select.tsx
  - src/components/admin/booking-cancel-dialog.tsx
autonomous: true
requirements:
  - ADMIN-03

must_haves:
  truths:
    - "管理者は全予約一覧を確認できる"
    - "管理者は予約のステータスを変更できる"
    - "管理者は予約をキャンセルできる"
  artifacts:
    - path: "src/lib/actions/admin/bookings.ts"
      provides: "予約管理Server Actions"
      exports: ["getBookings", "updateBookingStatus", "cancelBooking"]
    - path: "src/app/(admin)/bookings/page.tsx"
      provides: "予約一覧ページ"
      contains: "DataTable"
    - path: "src/app/(admin)/bookings/columns.tsx"
      provides: "予約テーブルカラム定義"
      contains: "ColumnDef"
  key_links:
    - from: "src/lib/actions/admin/bookings.ts"
      to: "src/lib/bookings/cancel.ts"
      via: "cancelBookingオーケストレーター再利用"
      pattern: "import.*cancelBooking.*from.*cancel"
    - from: "src/app/(admin)/bookings/columns.tsx"
      to: "src/lib/actions/admin/bookings.ts"
      via: "Server Action呼び出し"
      pattern: "updateBookingStatus|cancelBooking"
---

<objective>
管理者向け予約管理機能を実装する

Purpose: 管理者が全予約を確認し、ステータス変更やキャンセルを行えるようにする
Output: 予約一覧ページ、ステータス変更機能、キャンセル機能
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-features/05-RESEARCH.md
@.planning/phases/05-admin-features/05-01-SUMMARY.md

@src/lib/bookings/cancel.ts
@src/lib/supabase/service-role.ts
@src/app/(admin)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 予約一覧・ステータス変更</name>
  <files>
    src/lib/actions/admin/bookings.ts
    src/app/(admin)/bookings/page.tsx
    src/app/(admin)/bookings/columns.tsx
    src/components/admin/booking-status-select.tsx
  </files>
  <action>
    1. Server Actions作成 (src/lib/actions/admin/bookings.ts):
       - 'use server'ディレクティブ
       - 各関数内で認証・認可チェック

       getBookings(filters?):
         - service_roleでRLSバイパス
         - bookingsテーブルから全件取得
         - JOINs: member_plans→profiles (会員情報), meeting_menus (メニュー情報)
         - ソート: start_time降順
         - オプション: status, date_from, date_toでフィルタ

       updateBookingStatus(bookingId, newStatus):
         - Zodでバリデーション（statusは有効な値のみ）
         - bookings.statusを更新
         - revalidatePath('/admin/bookings')

    2. カラム定義 (src/app/(admin)/bookings/columns.tsx):
       - 'use client'
       - カラム: 日時、ユーザー（名前/メール）、メニュー、ステータス、タイプ（会員/ゲスト）、アクション
       - ステータス: BadgeでPending/Confirmed/Cancelled/Completed表示
       - アクションドロップダウン: ステータス変更、キャンセル

    3. 予約一覧ページ (src/app/(admin)/bookings/page.tsx):
       - Server ComponentでgetBookings()で初期データ取得
       - DataTableコンポーネントで表示
       - ステータスフィルタ（Selectで選択）

    4. ステータス変更Select (src/components/admin/booking-status-select.tsx):
       - 'use client'
       - shadcn/ui Selectコンポーネント
       - 選択変更時にServer Action呼び出し
       - 楽観的更新（UI先行更新）

    予約ステータス:
    - pending: 確認待ち
    - confirmed: 確定
    - cancelled: キャンセル
    - completed: 完了
  </action>
  <verify>
    - /admin/bookings で全予約一覧が表示される
    - ステータス変更がDBに反映される
    - フィルタ機能が動作する
  </verify>
  <done>
    - 管理者は全予約一覧を確認できる
    - 管理者は予約のステータスを変更できる
  </done>
</task>

<task type="auto">
  <name>Task 2: 管理者キャンセル機能</name>
  <files>
    src/lib/actions/admin/bookings.ts (追記)
    src/components/admin/booking-cancel-dialog.tsx
  </files>
  <action>
    1. キャンセルServer Action追加 (src/lib/actions/admin/bookings.ts):
       - cancelBookingByAdmin(bookingId):
         - 認証・認可チェック
         - 既存のcancelBookingオーケストレーター（src/lib/bookings/cancel.ts）を再利用
         - 会員予約: ポイント返還あり（refund_points RPC）
         - ゲスト予約: ポイント返還なし
         - Zoom会議削除、Googleカレンダー削除、キャンセルメール送信
         - revalidatePath('/admin/bookings')

    2. キャンセル確認ダイアログ (src/components/admin/booking-cancel-dialog.tsx):
       - 'use client'
       - shadcn/ui AlertDialog使用
       - 予約詳細を表示（日時、ユーザー、メニュー）
       - キャンセル理由入力（オプション）
       - 確認ボタンで Server Action呼び出し
       - 処理中はローディング表示

    既存インフラ活用:
    - cancelBookingオーケストレーター（04-03で実装済み）は会員/ゲスト両対応
    - 外部API削除は非ブロッキング（失敗してもキャンセル成功）
    - メール送信（Resend）はcancelBooking内で実行
  </action>
  <verify>
    - キャンセル確認ダイアログが表示される
    - キャンセル実行後、予約ステータスがcancelledに変更される
    - 会員予約の場合、ポイントが返還される
    - Zoom会議が削除される（ログで確認）
  </verify>
  <done>
    - 管理者は予約をキャンセルできる
    - キャンセル時の補償処理（ポイント返還、外部API削除、メール送信）が実行される
  </done>
</task>

</tasks>

<verification>
- npm run build 成功
- npm run lint 成功
- /admin/bookings で全予約一覧が表示
- ステータス変更が動作
- キャンセルが動作（ポイント返還、外部API削除含む）
</verification>

<success_criteria>
- 管理者は全予約一覧を確認できる
- 管理者は予約のステータスを変更できる
- 管理者は予約をキャンセルでき、関連する補償処理が実行される
- 全Server Actionで認証・認可チェックが実施されている
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-features/05-03-SUMMARY.md`
</output>
