---
phase: 06-automation-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260223000001_automation_tasks.sql
  - src/emails/BookingReminder.tsx
  - src/emails/ThankYouEmail.tsx
  - src/components/admin/forms/menu-form.tsx
  - src/lib/actions/admin/menus.ts
autonomous: true
requirements:
  - SYS-01
  - SYS-05
  - MEMBER-06

must_haves:
  truths:
    - "task_execution_logsテーブルでタスク実行履歴を記録できる"
    - "bookingsにreminder_sent_at/thank_you_sent_atフラグが存在し重複送信を防げる"
    - "meeting_menusにsend_thank_you_emailフラグが存在しメニューごとにサンキューメールON/OFF設定できる"
    - "リマインダーメールテンプレートが存在しセッション情報を含む"
    - "サンキューメールテンプレートが存在しフィードバック依頼を含む"
  artifacts:
    - path: "supabase/migrations/20260223000001_automation_tasks.sql"
      provides: "task_execution_logs, カラム追加, pg_cronジョブ定義"
      contains: "CREATE TABLE task_execution_logs"
    - path: "src/emails/BookingReminder.tsx"
      provides: "リマインダーメールテンプレート"
      exports: ["BookingReminder"]
    - path: "src/emails/ThankYouEmail.tsx"
      provides: "サンキューメールテンプレート"
      exports: ["ThankYouEmail"]
  key_links:
    - from: "src/components/admin/forms/menu-form.tsx"
      to: "meeting_menus.send_thank_you_email"
      via: "フォームフィールド追加"
      pattern: "send_thank_you_email"
---

<objective>
自動化タスクのためのDB基盤とメールテンプレートを作成する。

Purpose: Edge Functionsが使用するテーブル構造、メール重複送信防止フラグ、メールテンプレートを準備する。
Output: マイグレーションファイル、React Emailテンプレート2つ、メニュー管理UI拡張
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-tasks/06-CONTEXT.md
@.planning/phases/06-automation-tasks/06-RESEARCH.md

# 既存のメールテンプレート構造
@src/emails/BookingConfirmation.tsx
@src/emails/components/Layout.tsx

# 既存のメニュー管理フォーム
@src/components/admin/forms/menu-form.tsx
@src/lib/actions/admin/menus.ts

# 既存のマイグレーション構造
@supabase/migrations/20260222000001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: 自動化タスク用マイグレーション作成</name>
  <files>supabase/migrations/20260223000001_automation_tasks.sql</files>
  <action>
以下を含むマイグレーションファイルを作成:

1. **task_execution_logsテーブル**（CONTEXT.mdの設計に従う）:
   - id SERIAL PRIMARY KEY
   - task_name TEXT NOT NULL（'monthly_point_grant', 'reminder_email', 'thank_you_email'）
   - status TEXT NOT NULL CHECK（'success', 'failed', 'partial_success', 'timeout'）
   - started_at, completed_at TIMESTAMPTZ
   - total_count, success_count, failed_count INTEGER（バッチ処理用）
   - reference_type TEXT, reference_id INTEGER（個別実行用: 'booking', 'member_plan'）
   - details JSONB, error_details TEXT[]
   - created_at TIMESTAMPTZ DEFAULT NOW()
   - インデックス: task_name, status, created_at, (reference_type, reference_id)

2. **bookingsテーブルへのカラム追加**:
   - reminder_sent_at TIMESTAMPTZ（リマインダー送信済みフラグ）
   - thank_you_sent_at TIMESTAMPTZ（サンキュー送信済みフラグ）

3. **meeting_menusテーブルへのカラム追加**:
   - send_thank_you_email BOOLEAN DEFAULT false

4. **pg_cron拡張とジョブ定義**:
   - CREATE EXTENSION IF NOT EXISTS pg_cron
   - CREATE EXTENSION IF NOT EXISTS pg_net
   - コメントでVaultシークレット設定手順を記載（実際の設定は本番デプロイ時）
   - ジョブ定義SQLをコメント化して記載（本番でuncomment）:
     - monthly-point-grant: '0 0 1 * *'（毎月1日 00:00 UTC）
     - check-reminder-emails: '*/15 * * * *'（15分ごと）
     - check-thank-you-emails: '*/15 * * * *'（15分ごと）

5. **365日自動クリーンアップジョブ**:
   - cleanup-task-execution-logs: '0 2 * * *'
  </action>
  <verify>supabase db reset でマイグレーション適用確認、テーブル存在確認</verify>
  <done>task_execution_logsテーブルが存在し、bookings/meeting_menusに新カラムが追加されている</done>
</task>

<task type="auto">
  <name>Task 2: リマインダーメールテンプレート作成</name>
  <files>src/emails/BookingReminder.tsx</files>
  <action>
既存のBookingConfirmation.tsxをベースに、リマインダー用テンプレートを作成:

1. **Props定義**:
   - userName: string
   - sessionTitle: string
   - startTime: string（ISO8601）
   - zoomJoinUrl: string
   - isAdminCopy?: boolean（管理者宛フラグ）

2. **内容**:
   - 件名: 「明日のセッションのお知らせ」
   - 本文:
     - 挨拶: 「{userName}さん、明日のセッションをお忘れなく!」
     - セッション詳細（メニュー名、日時）
     - Zoomリンク（ボタン）
     - 管理者宛の場合は「予約者: {userName}」を追加

3. **Layoutコンポーネント**を使用（既存パターン踏襲）

4. **スタイル**: BookingConfirmationと同じパターン
  </action>
  <verify>npx email preview でテンプレート確認（またはTS型チェック: npx tsc --noEmit）</verify>
  <done>BookingReminder.tsxが存在し、TypeScriptエラーなく、必要な情報を表示する</done>
</task>

<task type="auto">
  <name>Task 3: サンキューメールテンプレート作成</name>
  <files>src/emails/ThankYouEmail.tsx</files>
  <action>
セッション終了後に送信するサンキューメールテンプレートを作成:

1. **Props定義**:
   - userName: string
   - sessionTitle: string
   - sessionDate: string（ISO8601）

2. **内容**:
   - 件名: 「本日のセッション、ありがとうございました」
   - 本文:
     - 感謝メッセージ: 「{userName}さん、本日はセッションにご参加いただきありがとうございました」
     - セッション情報（メニュー名、日付）
     - シンプルなクロージング（フィードバック依頼等は将来拡張）

3. **Layoutコンポーネント**を使用

4. **スタイル**: 既存テンプレートと一貫性を保つ
  </action>
  <verify>npx tsc --noEmit でTS型チェック</verify>
  <done>ThankYouEmail.tsxが存在し、TypeScriptエラーなく、感謝メッセージを表示する</done>
</task>

<task type="auto">
  <name>Task 4: メニュー管理にsend_thank_you_email設定追加</name>
  <files>src/components/admin/forms/menu-form.tsx, src/lib/actions/admin/menus.ts</files>
  <action>
1. **menu-form.tsx**:
   - フォームスキーマに send_thank_you_email: z.boolean().default(false) を追加
   - Checkbox + Labelコンポーネントで「サンキューメールを送信」フィールド追加
   - 説明文: 「このメニューのセッション終了後にサンキューメールを送信します」

2. **menus.ts**:
   - createMenu, updateMenuのスキーマに send_thank_you_email を追加
   - Supabaseへのinsert/updateに send_thank_you_email を含める

3. **既存のMenu型**:
   - src/app/admin/menus/columns.tsx のMenu型を確認し、必要に応じてsend_thank_you_emailを追加
  </action>
  <verify>npm run build でビルド成功、管理画面でメニュー編集時にチェックボックスが表示される</verify>
  <done>メニュー管理UIでsend_thank_you_emailをON/OFFでき、DBに保存される</done>
</task>

</tasks>

<verification>
- supabase db reset が成功し、新テーブル/カラムが存在
- npm run build が成功
- TypeScriptエラーなし
- /admin/menus でサンキューメール設定が表示される
</verification>

<success_criteria>
1. task_execution_logsテーブルが存在し、タスク実行履歴を記録できる構造
2. bookingsにreminder_sent_at/thank_you_sent_atカラムが存在
3. meeting_menusにsend_thank_you_emailカラムが存在
4. BookingReminder.tsx, ThankYouEmail.tsxが存在しビルド成功
5. メニュー管理UIでサンキューメール設定が可能
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-tasks/06-01-SUMMARY.md`
</output>
