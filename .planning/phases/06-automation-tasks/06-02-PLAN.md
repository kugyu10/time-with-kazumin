---
phase: 06-automation-tasks
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - supabase/functions/monthly-point-grant/index.ts
  - supabase/functions/check-reminder-emails/index.ts
  - supabase/functions/check-thank-you-emails/index.ts
  - src/app/admin/tasks/page.tsx
  - src/app/admin/tasks/tasks-client.tsx
  - src/app/admin/tasks/columns.tsx
  - src/lib/actions/admin/tasks.ts
  - src/components/admin/sidebar.tsx
autonomous: true
requirements:
  - SYS-01
  - SYS-05
  - MEMBER-06

must_haves:
  truths:
    - "毎月1日にポイント付与Edge Functionが実行され、全アクティブ会員にポイントが付与される"
    - "15分ごとにリマインダーチェックが実行され、24時間後のセッション対象者にメールが送信される"
    - "15分ごとにサンキューチェックが実行され、30分前に終了したセッション対象者にメールが送信される"
    - "管理画面でタスク実行履歴を確認できる"
    - "送信済みフラグにより重複送信が防止される"
  artifacts:
    - path: "supabase/functions/monthly-point-grant/index.ts"
      provides: "月次ポイント付与Edge Function"
      contains: "grant_monthly_points"
    - path: "supabase/functions/check-reminder-emails/index.ts"
      provides: "リマインダーメールチェックEdge Function"
      contains: "reminder_sent_at"
    - path: "supabase/functions/check-thank-you-emails/index.ts"
      provides: "サンキューメールチェックEdge Function"
      contains: "thank_you_sent_at"
    - path: "src/app/admin/tasks/page.tsx"
      provides: "タスク実行履歴管理ページ"
      exports: ["default"]
    - path: "src/lib/actions/admin/tasks.ts"
      provides: "タスク履歴取得Server Actions"
      exports: ["getTaskLogs"]
  key_links:
    - from: "supabase/functions/monthly-point-grant/index.ts"
      to: "grant_monthly_points RPC"
      via: "supabase.rpc呼び出し"
      pattern: "supabase\\.rpc.*grant_monthly_points"
    - from: "supabase/functions/check-reminder-emails/index.ts"
      to: "Resend API"
      via: "メール送信"
      pattern: "resend\\.emails\\.send"
    - from: "src/app/admin/tasks/page.tsx"
      to: "task_execution_logs"
      via: "Server Action"
      pattern: "getTaskLogs"
---

<objective>
Edge Functionsによる自動化タスク実装と、タスク実行履歴管理画面を作成する。

Purpose: 月次ポイント付与、リマインダーメール、サンキューメールを自動化し、実行状況を管理画面で確認できるようにする。
Output: Edge Functions 3つ、タスク履歴管理画面
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-tasks/06-CONTEXT.md
@.planning/phases/06-automation-tasks/06-RESEARCH.md
@.planning/phases/06-automation-tasks/06-01-SUMMARY.md

# 既存の管理画面パターン
@src/app/admin/bookings/page.tsx
@src/app/admin/bookings/bookings-client.tsx
@src/app/admin/bookings/columns.tsx
@src/lib/actions/admin/bookings.ts
@src/components/admin/sidebar.tsx

# メールテンプレート（06-01で作成）
@src/emails/BookingReminder.tsx
@src/emails/ThankYouEmail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 月次ポイント付与Edge Function作成</name>
  <files>supabase/functions/monthly-point-grant/index.ts</files>
  <action>
supabase/functions/monthly-point-grant/index.ts を作成:

1. **基本構造**（Deno runtime）:
```typescript
import { createClient } from 'jsr:@supabase/supabase-js@2'

Deno.serve(async (req) => {
  // 認証ヘッダー検証
  const authHeader = req.headers.get('Authorization')
  if (!authHeader) {
    return new Response('Unauthorized', { status: 401 })
  }

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  )
  // ...
})
```

2. **冪等性チェック**:
   - 今月のmonthly_grantレコードがpoint_transactionsに存在するかチェック
   - 存在すれば早期リターン

3. **grant_monthly_points RPC呼び出し**:
   - 既存のStored Procedureを使用
   - supabase.rpc('grant_monthly_points') を呼び出し

4. **リトライロジック**（CONTEXT.md: 即時リトライ3回）:
   - 失敗時に最大3回リトライ
   - 指数バックオフなし（即時）

5. **task_execution_logs記録**:
   - task_name: 'monthly_point_grant'
   - status: 'success' | 'partial_success' | 'failed'
   - total_count, success_count, failed_count

6. **JSONレスポンス**で結果返却

Note: ローカルでは `supabase functions serve` でテスト可能。本番デプロイはユーザー実施。
  </action>
  <verify>supabase functions serve monthly-point-grant でローカル起動可能、curl呼び出しでレスポンス確認</verify>
  <done>月次ポイント付与Edge Functionが存在し、RPCを呼び出してログを記録する</done>
</task>

<task type="auto">
  <name>Task 2: リマインダーメールチェックEdge Function作成</name>
  <files>supabase/functions/check-reminder-emails/index.ts</files>
  <action>
supabase/functions/check-reminder-emails/index.ts を作成:

1. **24時間後の予約を抽出**:
   - 現在時刻 + 24時間 の前後15分間ウィンドウを計算
   - bookingsテーブルから条件にマッチするレコードを取得:
     - status = 'confirmed'
     - start_time が ウィンドウ内
     - reminder_sent_at IS NULL（未送信のみ）

2. **会員/ゲスト両対応**:
   - 会員: member_plans.user:profiles からメールアドレス取得
   - ゲスト: guest_email を使用

3. **メール送信**（Resend）:
   - RESEND_API_KEY環境変数
   - BookingReminderテンプレートを使用（renderでHTML生成）
   - 送信先: ユーザー + 管理者（isAdminCopy: true）

4. **フラグ更新**:
   - 送信成功後、reminder_sent_at = NOW() を更新

5. **task_execution_logs記録**:
   - task_name: 'reminder_email'
   - 個別ログ: reference_type: 'booking', reference_id: booking.id

6. **リトライ**: メール送信失敗時に最大3回リトライ

Note: メール送信は既存のsrc/lib/email.tsパターンがあれば再利用、なければ直接Resend APIを呼び出し。
  </action>
  <verify>supabase functions serve check-reminder-emails でローカル起動可能</verify>
  <done>リマインダーメールチェックEdge Functionが存在し、24時間後の予約にメールを送信する</done>
</task>

<task type="auto">
  <name>Task 3: サンキューメールチェックEdge Function作成</name>
  <files>supabase/functions/check-thank-you-emails/index.ts</files>
  <action>
supabase/functions/check-thank-you-emails/index.ts を作成:

1. **30分前に終了した予約を抽出**:
   - 現在時刻 - 30分 の前後15分間ウィンドウを計算
   - bookingsテーブルから条件にマッチするレコードを取得:
     - status = 'completed'
     - end_time が ウィンドウ内
     - thank_you_sent_at IS NULL（未送信のみ）
     - menu.send_thank_you_email = true（ON設定のメニューのみ）

2. **メール送信**:
   - ThankYouEmailテンプレートを使用
   - 送信先: ユーザーのみ（管理者宛は不要）

3. **フラグ更新**:
   - 送信成功後、thank_you_sent_at = NOW() を更新

4. **task_execution_logs記録**:
   - task_name: 'thank_you_email'
   - 個別ログ: reference_type: 'booking', reference_id: booking.id

5. **send_thank_you_emailチェック**:
   - JOINでmeeting_menus.send_thank_you_emailを確認
   - falseの場合はスキップ
  </action>
  <verify>supabase functions serve check-thank-you-emails でローカル起動可能</verify>
  <done>サンキューメールチェックEdge Functionが存在し、終了30分後の予約にメールを送信する</done>
</task>

<task type="auto">
  <name>Task 4: タスク実行履歴管理画面作成</name>
  <files>
    src/app/admin/tasks/page.tsx,
    src/app/admin/tasks/tasks-client.tsx,
    src/app/admin/tasks/columns.tsx,
    src/lib/actions/admin/tasks.ts,
    src/components/admin/sidebar.tsx
  </files>
  <action>
1. **src/lib/actions/admin/tasks.ts**:
   - getTaskLogs Server Action:
     - task_execution_logsから最新100件を取得
     - フィルタオプション: task_name, status, 日付範囲
   - requireAdmin()で権限チェック（既存パターン踏襲）

2. **src/app/admin/tasks/columns.tsx**:
   - TanStack Table用のカラム定義
   - 表示項目: task_name, status, started_at, total_count, success_count, failed_count, error_details
   - statusはBadgeで色分け表示（success=緑, failed=赤, partial_success=黄）

3. **src/app/admin/tasks/tasks-client.tsx**:
   - DataTableコンポーネントでログ一覧表示
   - task_nameでフィルタ可能（ドロップダウン: 全て/monthly_point_grant/reminder_email/thank_you_email）
   - statusでフィルタ可能

4. **src/app/admin/tasks/page.tsx**:
   - Server Componentでデータ取得
   - TasksClientにデータを渡す
   - ページタイトル: 「タスク実行履歴」

5. **src/components/admin/sidebar.tsx**:
   - navItemsに「タスク履歴」を追加
   - href: '/admin/tasks'
   - icon: Clock（または適切なアイコン）
  </action>
  <verify>npm run build でビルド成功、/admin/tasks にアクセスしてタスク履歴一覧が表示される</verify>
  <done>管理画面でタスク実行履歴を確認でき、フィルタ機能が動作する</done>
</task>

</tasks>

<verification>
- supabase functions serve で3つのEdge Functionが起動可能
- npm run build が成功
- /admin/tasks でタスク履歴一覧が表示される
- サイドバーに「タスク履歴」リンクが表示される
</verification>

<success_criteria>
1. monthly-point-grant Edge Functionが存在し、grant_monthly_points RPCを呼び出す
2. check-reminder-emails Edge Functionが存在し、24時間後の予約にリマインダーを送信する
3. check-thank-you-emails Edge Functionが存在し、30分前終了のセッションにサンキューメールを送信する
4. /admin/tasks でタスク実行履歴を確認できる
5. 重複送信防止フラグ（reminder_sent_at, thank_you_sent_at）が正しく更新される
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-tasks/06-02-SUMMARY.md`
</output>
