---
phase: 09-playwright-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - e2e/global-setup.ts
  - e2e/global-teardown.ts
  - e2e/auth.setup.ts
  - e2e/fixtures.ts
autonomous: false
requirements:
  - E2E-01

must_haves:
  truths:
    - "テストユーザー（会員・管理者）が global-setup で自動作成される"
    - "global-teardown でテストユーザーが削除される"
    - "auth.setup.ts が会員・管理者として /login でログインし storageState を保存する"
    - "npx playwright test --project=setup がグリーンで完了する"
    - "e2e/.auth/member.json と e2e/.auth/admin.json がテスト後に生成される"
  artifacts:
    - path: "e2e/global-setup.ts"
      provides: "Supabase admin APIでテストユーザー作成（会員＋管理者）、profilesテーブルへのrole挿入"
      exports: ["default globalSetup"]
    - path: "e2e/global-teardown.ts"
      provides: "テストユーザー削除（メールアドレスで検索してdelete）"
      exports: ["default globalTeardown"]
    - path: "e2e/auth.setup.ts"
      provides: "ログインUI操作でstorageStateをe2e/.auth/に保存"
    - path: "e2e/fixtures.ts"
      provides: "memberPage / adminPage カスタムフィクスチャ（Phase 10で使用）"
  key_links:
    - from: "e2e/global-setup.ts"
      to: "supabase.auth.admin.createUser()"
      via: "SUPABASE_SERVICE_ROLE_KEY"
      pattern: "auth\\.admin\\.createUser"
    - from: "e2e/global-setup.ts"
      to: "profiles テーブル"
      via: "supabase.from('profiles').upsert()"
      pattern: "from.*profiles.*upsert"
    - from: "e2e/auth.setup.ts"
      to: "e2e/.auth/member.json"
      via: "page.context().storageState()"
      pattern: "storageState.*member\\.json"
---

<objective>
global-setup/teardown（テストユーザーCRUD）と auth.setup.ts（storageState保存）、fixtures.ts を実装する。

Purpose: Phase 10のテストシナリオが認証済み状態で実行できるよう、認証基盤を確立する。
Output: e2e/global-setup.ts, e2e/global-teardown.ts, e2e/auth.setup.ts, e2e/fixtures.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-playwright-foundation/09-RESEARCH.md
@.planning/phases/09-playwright-foundation/09-playwright-foundation-01-SUMMARY.md
</context>

<interfaces>
<!-- LoginFormの実際のセレクタ情報（src/components/auth/LoginForm.tsx より） -->
<!-- メールアドレス入力: Label htmlFor="email" → page.getByLabel('メールアドレス') -->
<!-- パスワード入力: Label htmlFor="password" → page.getByLabel('パスワード') -->
<!-- 送信ボタン: Button type="submit" テキスト "メールアドレスでログイン" → page.getByRole('button', { name: 'メールアドレスでログイン' }) -->
<!-- ログイン成功後のリダイレクト: window.location.href = "/bookings/new" -->

<!-- requireAdmin() の実装（src/lib/actions/admin/schedules.ts より） -->
<!-- profiles テーブルの role カラムが 'admin' かどうかで判定 -->
<!-- app_metadata ではなく profiles テーブルを使用 -->
<!-- → global-setup では createUser 後に profiles テーブルへの upsert が必要 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: global-setup.ts と global-teardown.ts 作成</name>
  <files>e2e/global-setup.ts, e2e/global-teardown.ts</files>
  <action>
**e2e/global-setup.ts を新規作成:**

```typescript
import { createClient } from '@supabase/supabase-js'
import * as fs from 'fs'
import * as path from 'path'

export default async function globalSetup() {
  // storageState ディレクトリを事前作成（Pitfall 3対策）
  const authDir = path.join(__dirname, '.auth')
  fs.mkdirSync(authDir, { recursive: true })

  const supabase = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  )

  // 会員テストユーザー作成
  const { data: memberData } = await supabase.auth.admin.createUser({
    email: process.env.E2E_MEMBER_EMAIL!,
    password: process.env.E2E_MEMBER_PASSWORD!,
    email_confirm: true,
    user_metadata: { name: 'E2E Test Member' },
  })

  // 会員の profiles レコードを作成（RLS対象外のservice_roleで直接INSERT）
  if (memberData?.user) {
    await supabase.from('profiles').upsert({
      id: memberData.user.id,
      email: process.env.E2E_MEMBER_EMAIL!,
      name: 'E2E Test Member',
      role: 'member',
    })
  }

  // 管理者テストユーザー作成
  const { data: adminData } = await supabase.auth.admin.createUser({
    email: process.env.E2E_ADMIN_EMAIL!,
    password: process.env.E2E_ADMIN_PASSWORD!,
    email_confirm: true,
    user_metadata: { name: 'E2E Test Admin' },
  })

  // 管理者の profiles レコードを作成（role: 'admin' を設定）
  if (adminData?.user) {
    await supabase.from('profiles').upsert({
      id: adminData.user.id,
      email: process.env.E2E_ADMIN_EMAIL!,
      name: 'E2E Test Admin',
      role: 'admin',
    })
  }
}
```

**重要:** requireAdmin() は `app_metadata` ではなく `profiles` テーブルの `role` カラムで判定する。`auth.admin.updateUserById()` は不要で、代わりに `profiles` テーブルへの `upsert` が必要。

**e2e/global-teardown.ts を新規作成:**

```typescript
import { createClient } from '@supabase/supabase-js'

export default async function globalTeardown() {
  const supabase = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  )

  const emails = [
    process.env.E2E_MEMBER_EMAIL!,
    process.env.E2E_ADMIN_EMAIL!,
  ]

  const { data } = await supabase.auth.admin.listUsers()
  for (const email of emails) {
    const user = data?.users.find((u) => u.email === email)
    if (user) {
      await supabase.auth.admin.deleteUser(user.id)
    }
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && test -f e2e/global-setup.ts && test -f e2e/global-teardown.ts && grep "profiles" e2e/global-setup.ts</automated>
  </verify>
  <done>e2e/global-setup.ts と e2e/global-teardown.ts が存在し、global-setup.ts に profiles テーブルへの upsert が含まれている</done>
</task>

<task type="auto">
  <name>Task 2: auth.setup.ts と fixtures.ts 作成</name>
  <files>e2e/auth.setup.ts, e2e/fixtures.ts</files>
  <action>
**e2e/auth.setup.ts を新規作成:**

LoginFormの実際のUIに合わせたセレクタを使用する:
- メール入力: `page.getByLabel('メールアドレス')`
- パスワード入力: `page.getByLabel('パスワード')`
- 送信ボタン: `page.getByRole('button', { name: 'メールアドレスでログイン' })`
- ログイン後のURL: `/bookings/new`

```typescript
import { test as setup, expect } from '@playwright/test'
import path from 'path'

const memberAuthFile = path.join(__dirname, '.auth/member.json')
const adminAuthFile = path.join(__dirname, '.auth/admin.json')

setup('authenticate as member', async ({ page }) => {
  await page.goto('/login')
  await page.getByLabel('メールアドレス').fill(process.env.E2E_MEMBER_EMAIL!)
  await page.getByLabel('パスワード').fill(process.env.E2E_MEMBER_PASSWORD!)
  await page.getByRole('button', { name: 'メールアドレスでログイン' }).click()
  await page.waitForURL('/bookings/new')
  await page.context().storageState({ path: memberAuthFile })
})

setup('authenticate as admin', async ({ page }) => {
  await page.goto('/login')
  await page.getByLabel('メールアドレス').fill(process.env.E2E_ADMIN_EMAIL!)
  await page.getByLabel('パスワード').fill(process.env.E2E_ADMIN_PASSWORD!)
  await page.getByRole('button', { name: 'メールアドレスでログイン' }).click()
  // 管理者もログイン後は /bookings/new にリダイレクトされる（LoginForm実装の動作）
  await page.waitForURL('/bookings/new')
  await page.context().storageState({ path: adminAuthFile })
})
```

**e2e/fixtures.ts を新規作成:**

```typescript
import { test as base, type Page } from '@playwright/test'
import path from 'path'

type AuthFixtures = {
  memberPage: Page
  adminPage: Page
}

export const test = base.extend<AuthFixtures>({
  memberPage: async ({ browser }, use) => {
    const ctx = await browser.newContext({
      storageState: path.join(__dirname, '.auth/member.json'),
    })
    const page = await ctx.newPage()
    await use(page)
    await ctx.close()
  },
  adminPage: async ({ browser }, use) => {
    const ctx = await browser.newContext({
      storageState: path.join(__dirname, '.auth/admin.json'),
    })
    const page = await ctx.newPage()
    await use(page)
    await ctx.close()
  },
})

export { expect } from '@playwright/test'
```

実際にauth.setupを実行するためには .env.test が必要。**事前に `.env.test.example` を `.env.test` にコピーして実際の値を設定する必要がある**ことをSUMMARYに記載する。ここでは .env.test の作成はしない（シークレット情報のため）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && test -f e2e/auth.setup.ts && test -f e2e/fixtures.ts && grep "メールアドレスでログイン" e2e/auth.setup.ts && grep "memberPage" e2e/fixtures.ts</automated>
  </verify>
  <done>e2e/auth.setup.ts と e2e/fixtures.ts が存在し、auth.setup.ts に正しいボタンセレクタが含まれ、fixtures.ts に memberPage / adminPage フィクスチャが定義されている</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E基盤の動作確認</name>
  <files></files>
  <action>自動化ステップは完了済み。ユーザーによる動作確認を実施する。</action>
  <what-built>
    Playwright E2E基盤（playwright.config.ts + global-setup/teardown + auth.setup + fixtures）を構築した。
    .env.test.example が用意されているので、実際の環境変数を設定して動作確認が必要。
  </what-built>
  <how-to-verify>
    1. `.env.test.example` を `.env.test` にコピーして実際の Supabase dev 環境の値を設定する
    2. `npm run test:e2e` を実行して、Playwright が起動することを確認する（テスト0件またはsetupのみ実行）
    3. `npm run test:e2e:ui` を実行して Playwright UI モードがブラウザで開くことを確認する
    4. `grep "e2e/.auth" .gitignore` でgitignore設定を確認する

    **Vercel Deployment Protection の確認（STATE.md に記載のブロッカー）:**
    CI環境でテストを実行する前に、Vercel dashboard で develop ブランチの Deployment Protection を確認・無効化すること。
  </how-to-verify>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && grep "e2e/.auth" .gitignore</automated>
  </verify>
  <done>ユーザーが Playwright UI モードの起動を確認し、npm run test:e2e がエラーなく起動する</done>
  <resume-signal>"approved" と入力するか、発生した問題を説明してください</resume-signal>
</task>

</tasks>

<verification>
- `test -f e2e/global-setup.ts` → 存在する
- `test -f e2e/global-teardown.ts` → 存在する
- `test -f e2e/auth.setup.ts` → 存在する
- `test -f e2e/fixtures.ts` → 存在する
- `grep "profiles" e2e/global-setup.ts` → profiles テーブルへの upsert が含まれる
- `grep "メールアドレスでログイン" e2e/auth.setup.ts` → 正しいボタンセレクタが使われている
- `npx playwright test --list` → エラーなく動作する
</verification>

<success_criteria>
- global-setup が Supabase admin API でテストユーザー2名を作成し、profiles テーブルにroleを設定する
- global-teardown がテストユーザーを削除する
- auth.setup.ts が LoginForm の実際のセレクタで /login にログインし storageState を保存する
- fixtures.ts が memberPage / adminPage フィクスチャを提供する
- `npx playwright test --project=setup` が .env.test 設定後にグリーンで完了する
</success_criteria>

<output>
完了後、`.planning/phases/09-playwright-foundation/09-playwright-foundation-02-SUMMARY.md` を作成する。
</output>
