---
phase: 13-zoom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/integrations/zoom.ts
  - src/__tests__/lib/integrations/zoom.test.ts
autonomous: true
requirements:
  - ZOOM-01
  - ZOOM-02
  - ZOOM-03
  - ZOOM-04

must_haves:
  truths:
    - "getZoomScheduledMeetings('A', from, to) がZoom APIからスケジュール済みミーティングをBusyTime[]で返す"
    - "getZoomScheduledMeetings('B', from, to) がエラー3161時に空配列を返しシステムが正常動作する"
    - "getCachedZoomBusyTimes() が15分TTLでキャッシュし、2回目呼び出しでAPIを叩かない"
    - "clearZoomScheduleCache() でキャッシュをクリアできる"
    - "getZoomScheduledMeetings() は毎回APIを呼び出しキャッシュを使用しない（予約確定時のキャッシュバイパス用）"
  artifacts:
    - path: "src/lib/integrations/zoom.ts"
      provides: "getZoomScheduledMeetings, getCachedZoomBusyTimes, clearZoomScheduleCache"
      exports: ["getZoomScheduledMeetings", "getCachedZoomBusyTimes", "clearZoomScheduleCache"]
    - path: "src/__tests__/lib/integrations/zoom.test.ts"
      provides: "getZoomScheduledMeetings + getCachedZoomBusyTimes テスト（キャッシュバイパス振る舞い含む）"
      contains: "describe(\"getZoomScheduledMeetings\""
  key_links:
    - from: "src/lib/integrations/zoom.ts"
      to: "https://api.zoom.us/v2/users/me/meetings"
      via: "fetch with Bearer token"
      pattern: "api\\.zoom\\.us/v2/users/me/meetings"
    - from: "src/lib/integrations/zoom.ts"
      to: "src/lib/integrations/google-calendar.ts"
      via: "BusyTime type import"
      pattern: "import.*BusyTime.*from.*google-calendar"
---

<objective>
Zoomスケジュール取得関数とキャッシュ機能を既存zoom.tsに追加し、ユニットテストで動作を検証する。

Purpose: ZOOM_AおよびZOOM_Bのスケジュール済みミーティングをBusyTime[]として取得できるAPIレイヤーを構築する。これはPlan 02でのslot API統合・saga統合の基盤となる。
Output: zoom.tsに3つの新規エクスポート関数、zoom.test.tsに対応テスト追加（ZOOM-04キャッシュバイパス振る舞いテスト含む）
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/integrations/zoom.ts:
```typescript
type AccountType = "A" | "B"
export function isZoomConfigured(accountType: AccountType = "A"): boolean
export async function getZoomAccessToken(accountType: AccountType): Promise<string>
export function clearTokenCache(): void
// LRUCache already imported: import { LRUCache } from "lru-cache"
```

From src/lib/integrations/google-calendar.ts:
```typescript
export interface BusyTime {
  start: string
  end: string
}
// LRUCache pattern: new LRUCache<string, BusyTime[]>({ max: 100, ttl: 15 * 60 * 1000 })
```

From src/__tests__/lib/integrations/zoom.test.ts:
```typescript
// Test pattern: vi.stubEnv + mockFetch + clearTokenCache in beforeEach
const mockFetch = vi.fn()
global.fetch = mockFetch
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getZoomScheduledMeetings() と getCachedZoomBusyTimes() を zoom.ts に追加</name>
  <files>src/lib/integrations/zoom.ts, src/__tests__/lib/integrations/zoom.test.ts</files>
  <read_first>
    - src/lib/integrations/zoom.ts
    - src/lib/integrations/google-calendar.ts
    - src/__tests__/lib/integrations/zoom.test.ts
  </read_first>
  <behavior>
    - Test 1: getZoomScheduledMeetings("A", from, to) がアカウント未設定時に空配列を返し、fetchを呼ばない
    - Test 2: getZoomScheduledMeetings("A", from, to) がZoom APIレスポンスのmeetings配列をBusyTime[]に変換する（start_time + duration分 → { start, end }）
    - Test 3: getZoomScheduledMeetings("A", from, to) がエラー3161レスポンスで空配列を返す（console.warnログ出力）
    - Test 4: getZoomScheduledMeetings("A", from, to) がfetch例外時に空配列を返す
    - Test 5: getCachedZoomBusyTimes("A", start, end) が2回目呼び出しでキャッシュからBusyTime[]を返す（fetchが1回だけ呼ばれる）
    - Test 6: clearZoomScheduleCache() がキャッシュをクリアし、次回呼び出しでAPIが再呼び出しされる
    - Test 7 (ZOOM-04): getZoomScheduledMeetings() を同じ引数で2回呼び出した場合、毎回APIを叩く（fetchが2回呼ばれる）。これはキャッシュバイパス振る舞いの検証であり、saga.tsがこの関数を使うことで予約確定時のリアルタイム確認が保証される。
  </behavior>
  <action>
    **Step 1: zoom.ts にインターフェースと関数を追加する（per D-01, D-06, D-08）**

    zoom.ts の先頭 import セクションに BusyTime を追加:
    ```typescript
    import { BusyTime } from "./google-calendar"
    ```

    zoom.ts にZoom APIレスポンス型を追加（exportしない内部型）:
    ```typescript
    interface ZoomScheduledMeeting {
      id: number
      uuid: string
      topic: string
      type: number
      start_time: string
      duration: number
      timezone: string
      status: string
    }

    interface ZoomListMeetingsResponse {
      page_count: number
      page_size: number
      total_records: number
      next_page_token: string
      meetings: ZoomScheduledMeeting[]
    }
    ```

    zoom.ts にヘルパー関数を追加:
    ```typescript
    function convertMeetingsToBusyTimes(meetings: ZoomScheduledMeeting[]): BusyTime[] {
      return meetings.map((meeting) => {
        const start = new Date(meeting.start_time).toISOString()
        const endMs = new Date(meeting.start_time).getTime() + meeting.duration * 60 * 1000
        const end = new Date(endMs).toISOString()
        return { start, end }
      })
    }
    ```

    zoom.ts にLRUCacheインスタンスを追加（既存の tokenCache の下に）:
    ```typescript
    const zoomScheduleCache = new LRUCache<string, BusyTime[]>({
      max: 20,
      ttl: 15 * 60 * 1000, // 15分
    })
    ```

    zoom.ts に `getZoomScheduledMeetings` を追加（per D-01, D-08）:
    ```typescript
    export async function getZoomScheduledMeetings(
      accountType: AccountType,
      fromDate: string,
      toDate: string
    ): Promise<BusyTime[]> {
      if (!isZoomConfigured(accountType)) {
        console.warn(`[Zoom] Account ${accountType} not configured, returning empty schedule`)
        return []
      }

      try {
        const accessToken = await getZoomAccessToken(accountType)
        const params = new URLSearchParams({
          type: "scheduled",
          from: fromDate.split("T")[0],
          to: toDate.split("T")[0],
          page_size: "300",
        })

        const response = await fetch(
          `https://api.zoom.us/v2/users/me/meetings?${params.toString()}`,
          {
            headers: {
              Authorization: `Bearer ${accessToken}`,
            },
          }
        )

        if (!response.ok) {
          const errorBody = await response.json().catch(() => null)

          if (errorBody?.code === 3161) {
            console.warn(`[Zoom] Account ${accountType} API scope limitation (3161), returning empty array`)
            return []
          }

          console.warn(`[Zoom] Failed to get meetings for account ${accountType}: ${response.status}`)
          return []
        }

        const data: ZoomListMeetingsResponse = await response.json()

        if (data.next_page_token) {
          console.warn(`[Zoom] Account ${accountType}: more meetings exist, using first page only`)
        }

        return convertMeetingsToBusyTimes(data.meetings || [])
      } catch (error) {
        console.error(`[Zoom] Error getting scheduled meetings for account ${accountType}:`, error)
        return []
      }
    }
    ```

    zoom.ts に `getCachedZoomBusyTimes` を追加（per D-06）:
    ```typescript
    export async function getCachedZoomBusyTimes(
      accountType: AccountType,
      startDate: string,
      endDate: string
    ): Promise<BusyTime[]> {
      const cacheKey = `${accountType}:${startDate}-${endDate}`
      const cached = zoomScheduleCache.get(cacheKey)

      if (cached) {
        console.log(`[Zoom] Using cached schedule for account ${accountType}`)
        return cached
      }

      const busyTimes = await getZoomScheduledMeetings(accountType, startDate, endDate)
      zoomScheduleCache.set(cacheKey, busyTimes)

      return busyTimes
    }
    ```

    zoom.ts に `clearZoomScheduleCache` を追加:
    ```typescript
    export function clearZoomScheduleCache(): void {
      zoomScheduleCache.clear()
      console.log("[Zoom] Schedule cache cleared")
    }
    ```

    **Step 2: zoom.test.ts にテストを追加**

    zoom.test.ts の import に新関数を追加:
    ```typescript
    import {
      isZoomConfigured,
      createZoomMeeting,
      deleteZoomMeeting,
      clearTokenCache,
      getZoomScheduledMeetings,
      getCachedZoomBusyTimes,
      clearZoomScheduleCache,
    } from "@/lib/integrations/zoom"
    ```

    beforeEach に `clearZoomScheduleCache()` を追加（clearTokenCache の次に）。

    テストファイル末尾（既存describeブロック群の後）に `describe("getZoomScheduledMeetings", ...)` と `describe("getCachedZoomBusyTimes", ...)` ブロックを追加。

    テスト実装の具体的なパターン:
    - アカウント未設定テスト: `vi.stubEnv("ZOOM_ACCOUNT_A_ACCOUNT_ID", "")` → `expect(result).toEqual([])` + `expect(mockFetch).not.toHaveBeenCalled()`
    - BusyTime変換テスト: mockFetchでtoken応答 + meetings応答（`start_time: "2026-04-01T01:00:00Z"`, `duration: 60`）を返す → `expect(result).toEqual([{ start: "2026-04-01T01:00:00.000Z", end: "2026-04-01T02:00:00.000Z" }])`
    - エラー3161テスト: mockFetchでtoken応答 + `{ ok: false, status: 400, json: () => Promise.resolve({ code: 3161, message: "..." }) }` を返す → `expect(result).toEqual([])`
    - fetch例外テスト: mockFetchでtoken応答の後に `mockRejectedValueOnce(new Error("network error"))` → `expect(result).toEqual([])`
    - キャッシュテスト: getCachedZoomBusyTimes を2回呼び、mockFetch の meetings API 呼び出しが1回のみであることを確認
    - キャッシュクリアテスト: 1回呼び出し → clearZoomScheduleCache() → 再度呼び出し → mockFetch meetings API が2回呼ばれることを確認
    - **キャッシュバイパステスト（ZOOM-04振る舞い検証）:** getZoomScheduledMeetings を同じ引数で2回連続呼び出し → mockFetch の meetings API 呼び出しが2回であることを確認（`expect(mockFetch).toHaveBeenCalledTimes(4)` — token取得2回 + meetings取得2回）。テスト名: `"should always call API without caching (cache bypass for booking confirmation per D-07)"`。これにより、saga.ts が getZoomScheduledMeetings を使うことでキャッシュバイパスが保証されることを検証する。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx vitest run src/__tests__/lib/integrations/zoom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - zoom.ts に `export async function getZoomScheduledMeetings(` が存在する
    - zoom.ts に `export async function getCachedZoomBusyTimes(` が存在する
    - zoom.ts に `export function clearZoomScheduleCache(` が存在する
    - zoom.ts に `import { BusyTime } from "./google-calendar"` が存在する
    - zoom.ts に `new LRUCache<string, BusyTime[]>` が存在する（スケジュールキャッシュ用）
    - zoom.ts に `errorBody?.code === 3161` のチェックが存在する（per D-08）
    - zoom.ts に `page_size: "300"` が存在する
    - zoom.ts に `fromDate.split("T")[0]` が存在する（YYYY-MM-DD変換）
    - zoom.test.ts に `describe("getZoomScheduledMeetings"` が存在する
    - zoom.test.ts に `describe("getCachedZoomBusyTimes"` が存在する
    - zoom.test.ts に `cache bypass` を含むテストケースが存在する（ZOOM-04）
    - `npx vitest run src/__tests__/lib/integrations/zoom.test.ts` が exit 0 で完了する
  </acceptance_criteria>
  <done>
    getZoomScheduledMeetings がZoom APIからスケジュールを取得しBusyTime[]で返す。エラー3161フォールバックが動作する。getCachedZoomBusyTimes が15分TTLキャッシュ付きで動作する。getZoomScheduledMeetings が毎回APIを呼びキャッシュを使用しないことがテストで検証済み（ZOOM-04キャッシュバイパス保証）。全テストがパスする。
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/lib/integrations/zoom.test.ts` が全テストパス
- zoom.ts に getZoomScheduledMeetings, getCachedZoomBusyTimes, clearZoomScheduleCache がエクスポートされている
- BusyTime 型が google-calendar.ts からインポートされている
- getZoomScheduledMeetings のキャッシュバイパス振る舞いがテストで検証されている（ZOOM-04）
</verification>

<success_criteria>
- Zoom APIスケジュール取得関数が実装され、エラー3161フォールバック付きで空配列を返す
- 15分TTLのLRUCacheが既存パターンと一貫して実装されている
- getZoomScheduledMeetings は毎回APIを呼びキャッシュを使用しない（ZOOM-04のキャッシュバイパス保証）
- 全ユニットテストがパスする
</success_criteria>

<output>
After completion, create `.planning/phases/13-zoom/13-01-SUMMARY.md`
</output>
