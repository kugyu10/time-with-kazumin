---
phase: 13-zoom
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/app/api/public/slots/route.ts
  - src/app/api/public/slots/week/route.ts
  - src/lib/bookings/saga.ts
autonomous: true
requirements:
  - ZOOM-01
  - ZOOM-02
  - ZOOM-03
  - ZOOM-04

must_haves:
  truths:
    - "ZOOM_Aでスケジュール済みの会議がある時間帯は予約スロット一覧に表示されない"
    - "ZOOM_Bでスケジュール済みの会議がある時間帯は予約スロット一覧に表示されない"
    - "スロット一覧表示時はZoomスケジュール取得結果が15分キャッシュ経由で取得される"
    - "予約確定時はキャッシュを無視してZoomスケジュールをリアルタイムで確認し、競合時はエラーを返す"
    - "ZOOM_Bがエラー3161を返した場合でもシステムが正常動作する"
  artifacts:
    - path: "src/app/api/public/slots/route.ts"
      provides: "Zoomビジー時間マージ済みのスロット一覧API"
      contains: "getCachedZoomBusyTimes"
    - path: "src/app/api/public/slots/week/route.ts"
      provides: "Zoomビジー時間マージ済みの週間スロットAPI"
      contains: "getCachedZoomBusyTimes"
    - path: "src/lib/bookings/saga.ts"
      provides: "Zoom競合チェック付き予約Saga"
      contains: "checkZoomConflict"
  key_links:
    - from: "src/app/api/public/slots/route.ts"
      to: "src/lib/integrations/zoom.ts"
      via: "getCachedZoomBusyTimes import"
      pattern: "import.*getCachedZoomBusyTimes.*from.*zoom"
    - from: "src/app/api/public/slots/week/route.ts"
      to: "src/lib/integrations/zoom.ts"
      via: "getCachedZoomBusyTimes import"
      pattern: "import.*getCachedZoomBusyTimes.*from.*zoom"
    - from: "src/lib/bookings/saga.ts"
      to: "src/lib/integrations/zoom.ts"
      via: "getZoomScheduledMeetings import (cache bypass per D-07)"
      pattern: "import.*getZoomScheduledMeetings.*from.*zoom"
---

<objective>
Zoomスケジュールのビジー時間をスロットAPI 2本と予約Sagaに統合し、Zoomと予約システムの矛盾を解消する。

Purpose: Plan 01で構築したZoomスケジュール取得関数を実際の空き枠判定と予約確定フローに組み込み、Phase 13の全要件を達成する。
Output: slots/route.ts, slots/week/route.ts にZoomビジー時間マージ、saga.ts にリアルタイムZoom競合チェック
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-zoom/13-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these exports in zoom.ts -->
From src/lib/integrations/zoom.ts (after Plan 01):
```typescript
// Cache-enabled: 15min TTL LRUCache
export async function getCachedZoomBusyTimes(
  accountType: "A" | "B",
  startDate: string,
  endDate: string
): Promise<BusyTime[]>

// Direct API call (no cache) - for saga real-time check
export async function getZoomScheduledMeetings(
  accountType: "A" | "B",
  fromDate: string,
  toDate: string
): Promise<BusyTime[]>
```

From src/lib/integrations/google-calendar.ts:
```typescript
export interface BusyTime {
  start: string
  end: string
}
```

From src/lib/bookings/types.ts:
```typescript
export const BookingErrorCodes = {
  SLOT_UNAVAILABLE: "SLOT_UNAVAILABLE",
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: slots/route.ts と slots/week/route.ts にZoomビジー時間マージを追加</name>
  <files>src/app/api/public/slots/route.ts, src/app/api/public/slots/week/route.ts</files>
  <read_first>
    - src/app/api/public/slots/route.ts
    - src/app/api/public/slots/week/route.ts
    - src/lib/integrations/zoom.ts
  </read_first>
  <action>
    **slots/route.ts の変更（per D-02, D-03, D-04, D-06）:**

    1. import文に追加（ファイル先頭、既存import群の最後に）:
    ```typescript
    import { getCachedZoomBusyTimes } from "@/lib/integrations/zoom"
    ```

    2. L190（`} catch (error) { ... }` の閉じ括弧の直後、つまり Google Calendar busyTimes 取得ブロックの直後）に以下を追加:
    ```typescript
    // Zoomビジー時間の取得とマージ（Promise.allSettled 並列 per D-02）
    const [zoomAResult, zoomBResult] = await Promise.allSettled([
      getCachedZoomBusyTimes("A", busyTimeStart, busyTimeEnd),
      getCachedZoomBusyTimes("B", busyTimeStart, busyTimeEnd),
    ])

    if (zoomAResult.status === "fulfilled") {
      busyTimes = [...busyTimes, ...zoomAResult.value]
    }
    if (zoomBResult.status === "fulfilled") {
      busyTimes = [...busyTimes, ...zoomBResult.value]
    }

    console.log(`[GET /api/public/slots] Total busy times after Zoom merge: ${busyTimes.length}`)
    ```

    **slots/week/route.ts の変更（同じパターン）:**

    1. import文に追加（ファイル先頭、既存import群の最後に）:
    ```typescript
    import { getCachedZoomBusyTimes } from "@/lib/integrations/zoom"
    ```

    2. L164（Google Calendar busyTimes 取得ブロックの直後、`} catch (error) { ... }` の閉じ括弧の直後）に以下を追加。重要: このコードは日付ループの外（busyTimes変数宣言の直後）に配置する（Pitfall 5対策: 日付ループ内で呼ぶとAPI呼び出しが7倍になる）:
    ```typescript
    // Zoomビジー時間の取得とマージ（Promise.allSettled 並列 per D-02）
    const [zoomAResult, zoomBResult] = await Promise.allSettled([
      getCachedZoomBusyTimes("A", busyTimeStart, busyTimeEnd),
      getCachedZoomBusyTimes("B", busyTimeStart, busyTimeEnd),
    ])

    if (zoomAResult.status === "fulfilled") {
      busyTimes = [...busyTimes, ...zoomAResult.value]
    }
    if (zoomBResult.status === "fulfilled") {
      busyTimes = [...busyTimes, ...zoomBResult.value]
    }

    console.log(`[GET /api/public/slots/week] Total busy times after Zoom merge: ${busyTimes.length}`)
    ```
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx tsc --noEmit src/app/api/public/slots/route.ts src/app/api/public/slots/week/route.ts 2>&1 || echo "Type check done"</automated>
  </verify>
  <acceptance_criteria>
    - slots/route.ts に `import { getCachedZoomBusyTimes } from "@/lib/integrations/zoom"` が存在する
    - slots/route.ts に `Promise.allSettled` が存在する
    - slots/route.ts に `getCachedZoomBusyTimes("A"` と `getCachedZoomBusyTimes("B"` が存在する
    - slots/route.ts に `zoomAResult.status === "fulfilled"` が存在する
    - slots/route.ts に `Total busy times after Zoom merge` のログが存在する
    - slots/week/route.ts に `import { getCachedZoomBusyTimes } from "@/lib/integrations/zoom"` が存在する
    - slots/week/route.ts に `Promise.allSettled` が存在する
    - slots/week/route.ts に `getCachedZoomBusyTimes("A"` と `getCachedZoomBusyTimes("B"` が存在する
    - slots/week/route.ts の Zoom busyTimes マージコードが日付ループ（`for (const date of dates)`）の外に配置されている
  </acceptance_criteria>
  <done>
    両スロットAPIでZoom_AとZoom_BのビジータイムがGoogle CalendarのbusyTimesにマージされ、既存のisSlotBusy()でZoom予定もブロック対象になる。
  </done>
</task>

<task type="auto">
  <name>Task 2: saga.ts に予約確定時のZoomリアルタイム競合チェックを追加</name>
  <files>src/lib/bookings/saga.ts</files>
  <read_first>
    - src/lib/bookings/saga.ts
    - src/lib/integrations/zoom.ts
    - src/lib/integrations/google-calendar.ts
  </read_first>
  <action>
    **saga.ts の変更（per D-05, D-07）:**

    1. import文に追加（既存のzoom importの行を変更）:

    変更前:
    ```typescript
    import { createZoomMeeting, deleteZoomMeeting } from "../integrations/zoom"
    ```
    変更後:
    ```typescript
    import { createZoomMeeting, deleteZoomMeeting, getZoomScheduledMeetings } from "../integrations/zoom"
    ```

    2. google-calendar から BusyTime 型をインポート:
    ```typescript
    import { BusyTime } from "../integrations/google-calendar"
    ```

    3. saga.ts のファイル末尾（`getMenuZoomAccount` 関数の後）に `checkZoomConflict` ヘルパー関数を追加:
    ```typescript
    /**
     * Check Zoom schedule conflicts (real-time, cache bypass per D-07)
     * Returns true if conflict exists
     */
    async function checkZoomConflict(
      startTime: string,
      endTime: string
    ): Promise<boolean> {
      const [zoomAResult, zoomBResult] = await Promise.allSettled([
        getZoomScheduledMeetings("A", startTime, endTime),
        getZoomScheduledMeetings("B", startTime, endTime),
      ])

      const allBusyTimes: BusyTime[] = []
      if (zoomAResult.status === "fulfilled") allBusyTimes.push(...zoomAResult.value)
      if (zoomBResult.status === "fulfilled") allBusyTimes.push(...zoomBResult.value)

      const start = new Date(startTime).getTime()
      const end = new Date(endTime).getTime()

      return allBusyTimes.some((busy) => {
        const busyStart = new Date(busy.start).getTime()
        const busyEnd = new Date(busy.end).getTime()
        return start < busyEnd && end > busyStart
      })
    }
    ```

    4. createBookingSaga 関数内、Step 2（`completedSteps.push("check_slot")`）の直後、Step 3（`// Step 3: Consume points`）の直前に、Step 2.5を挿入:
    ```typescript
    // Step 2.5: Check Zoom schedule conflicts (real-time, cache bypass per D-05, D-07)
    console.log("[Saga] Step 2.5: Checking Zoom schedule conflicts")
    const zoomConflict = await checkZoomConflict(context.startTime, context.endTime)
    if (zoomConflict) {
      return {
        success: false,
        error: "この時間帯はZoomの予定と重複しています",
        errorCode: BookingErrorCodes.SLOT_UNAVAILABLE,
      }
    }
    // Note: No completedSteps.push needed - this step is read-only, no compensation required
    ```

    **重要な注意点（Pitfall 3対策）:** Step 2.5は `completedSteps` に追加しない。読み取り専用ステップであり、compensateAll の switch 文にケースを追加する必要もない。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx tsc --noEmit src/lib/bookings/saga.ts 2>&1 || echo "Type check done"</automated>
  </verify>
  <acceptance_criteria>
    - saga.ts の import に `getZoomScheduledMeetings` が含まれる
    - saga.ts の import に `BusyTime` が含まれる
    - saga.ts に `async function checkZoomConflict(` が存在する
    - saga.ts の checkZoomConflict 内に `getZoomScheduledMeetings("A"` と `getZoomScheduledMeetings("B"` が存在する（キャッシュバイパス = getCachedではなくgetZoomScheduledMeetings直接呼び出し）
    - saga.ts に `Step 2.5: Checking Zoom schedule conflicts` のログが存在する
    - saga.ts に `この時間帯はZoomの予定と重複しています` のエラーメッセージが存在する
    - saga.ts に `BookingErrorCodes.SLOT_UNAVAILABLE` が Step 2.5 で使用されている
    - saga.ts の Step 2.5 に `completedSteps.push` が存在しない（読み取り専用ステップ）
  </acceptance_criteria>
  <done>
    予約確定時にZoom_AとZoom_Bのスケジュールをキャッシュバイパスでリアルタイム確認し、競合がある場合はSLOT_UNAVAILABLEエラーを返す。ポイント消費前に競合チェックするため、補償トランザクションは不要。
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` で型エラーなし
- slots/route.ts と slots/week/route.ts の両方にZoomビジー時間マージコードが存在する
- saga.ts にStep 2.5のZoom競合チェックが存在する
- getCachedZoomBusyTimes（スロットAPI用）と getZoomScheduledMeetings（saga用）の使い分けが正しい
</verification>

<success_criteria>
- スロットAPI 2本でZoomスケジュールがbusyTimesにマージされ、Zoom予定の時間帯がスロット一覧で unavailable になる
- 予約確定時にリアルタイムでZoomスケジュールを確認し、二重予約を防止する
- ZOOM_B（エラー3161）でもシステムが正常動作する（Plan 01のフォールバックが有効）
</success_criteria>

<output>
After completion, create `.planning/phases/13-zoom/13-02-SUMMARY.md`
</output>
