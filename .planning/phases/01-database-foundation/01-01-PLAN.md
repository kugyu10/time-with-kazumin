---
phase: 01-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260222000001_initial_schema.sql
  - supabase/migrations/20260222000001_initial_schema_down.sql
  - supabase/seed.sql
autonomous: true
requirements:
  - SYS-01

must_haves:
  truths:
    - "データベーステーブルが作成され、制約が有効化されている"
    - "二重予約がデータベース制約で自動的に防止される"
    - "時刻データがタイムゾーン情報を保持している"
    - "外部キー参照が高速に動作する"
    - "マイグレーションをロールバックできる"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Supabaseローカル開発環境設定"
      min_lines: 10
    - path: "supabase/migrations/20260222000001_initial_schema.sql"
      provides: "8テーブル + 制約 + インデックス定義"
      min_lines: 200
    - path: "supabase/migrations/20260222000001_initial_schema_down.sql"
      provides: "ロールバック用DDL"
      min_lines: 30
    - path: "supabase/seed.sql"
      provides: "初期マスターデータ（プラン・メニュー）"
      min_lines: 20
  key_links:
    - from: "supabase/migrations/20260222000001_initial_schema.sql"
      to: "bookings table"
      via: "EXCLUDE制約 + btree_gist"
      pattern: "EXCLUDE USING gist.*tstzrange.*WITH &&"
    - from: "bookings table"
      to: "member_plans/menus"
      via: "外部キー + インデックス"
      pattern: "REFERENCES.*ON DELETE"
    - from: "supabase/config.toml"
      to: "Supabase CLI"
      via: "project設定"
      pattern: "project_id"
---

<objective>
トランザクション整合性を担保するPostgreSQLスキーマの確立。二重予約防止、タイムゾーン対応、外部キー最適化を含む8テーブルの定義とマイグレーション基盤の構築。

Purpose: Phase 2以降の予約・ポイント管理機能の基盤となるデータ構造を確立し、データ整合性をデータベース制約レベルで保証する。

Output: Supabaseローカル開発環境、マイグレーションファイル（UP/DOWN）、シードデータ
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabaseローカル開発環境のセットアップ</name>
  <files>
    supabase/config.toml
    .gitignore
  </files>
  <action>
1. Supabase CLIをグローバルインストール（未インストールの場合）: `npm install -g supabase`
2. プロジェクトルートで `supabase init` を実行してローカル開発環境を初期化
3. `supabase/config.toml` が生成されることを確認
4. `.gitignore` に以下を追加（既に存在する場合はスキップ）:
   ```
   # Supabase
   supabase/.branches
   supabase/.temp
   .env.local
   ```
5. ローカルSupabaseを起動して動作確認: `supabase start`
6. 起動成功後、API URL、anon key、service_role keyをメモ（後続フェーズで使用）

**重要:** このタスクは環境セットアップのみ。リモートプロジェクトへのリンクは行わない（Phase 4以降で実施）。
  </action>
  <verify>
- `supabase/config.toml` が存在する
- `supabase status` でローカルDBが起動していることを確認
- `supabase db reset` が正常に完了する（空のDBリセット）
  </verify>
  <done>
Supabaseローカル開発環境が起動し、マイグレーション実行準備が整っている。
  </done>
</task>

<task type="auto">
  <name>Task 2: 初期スキーママイグレーションの作成</name>
  <files>
    supabase/migrations/20260222000001_initial_schema.sql
  </files>
  <action>
`supabase/migrations/20260222000001_initial_schema.sql` を作成し、以下を実装:

**1. 拡張機能の有効化**
```sql
CREATE EXTENSION IF NOT EXISTS btree_gist;
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
```

**2. テーブル定義（8テーブル）**

CONTEXT.mdの命名規則に従い（複数形、snake_case、英語）、以下の順序で定義:

**2-1. profiles テーブル**
- `id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE`
- `email TEXT NOT NULL UNIQUE`
- `full_name TEXT`
- `role TEXT NOT NULL DEFAULT 'member' CHECK (role IN ('guest', 'member', 'admin'))`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`

**2-2. plans テーブル**（サブスクプラン定義）
- `id SERIAL PRIMARY KEY`
- `name TEXT NOT NULL`
- `monthly_points INTEGER NOT NULL`
- `max_points INTEGER` （繰り越し上限、NULLは無制限）
- `price_monthly DECIMAL(10,2)`
- `is_active BOOLEAN DEFAULT true`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`

**2-3. member_plans テーブル**（会員のプラン契約と残高）
- `id SERIAL PRIMARY KEY`
- `user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`
- `plan_id INTEGER NOT NULL REFERENCES plans(id)`
- `current_points INTEGER NOT NULL DEFAULT 0`
- `monthly_points INTEGER NOT NULL`
- `status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'suspended', 'canceled'))`
- `started_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
- `ended_at TIMESTAMPTZ`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- `CONSTRAINT positive_points CHECK (current_points >= 0)`

**2-4. meeting_menus テーブル**（セッション種別）
- `id SERIAL PRIMARY KEY`
- `name TEXT NOT NULL`
- `duration_minutes INTEGER NOT NULL`
- `points_required INTEGER NOT NULL`
- `zoom_account TEXT NOT NULL CHECK (zoom_account IN ('A', 'B'))`
- `description TEXT`
- `is_active BOOLEAN DEFAULT true`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`

**2-5. weekly_schedules テーブル**（営業時間設定）
- `id SERIAL PRIMARY KEY`
- `day_of_week INTEGER NOT NULL CHECK (day_of_week BETWEEN 0 AND 6)` （0=日曜）
- `is_holiday_pattern BOOLEAN NOT NULL DEFAULT false`
- `start_time TIME NOT NULL`
- `end_time TIME NOT NULL`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- `CONSTRAINT valid_time_range CHECK (start_time < end_time)`

**2-6. point_transactions テーブル**（監査証跡）
- `id SERIAL PRIMARY KEY`
- `member_plan_id INTEGER NOT NULL REFERENCES member_plans(id) ON DELETE CASCADE`
- `points INTEGER NOT NULL`
- `transaction_type TEXT NOT NULL CHECK (transaction_type IN ('consume', 'refund', 'monthly_grant', 'manual_adjust'))`
- `reference_id INTEGER` （booking_idなど）
- `notes TEXT`
- `balance_after INTEGER NOT NULL`
- `created_at TIMESTAMPTZ DEFAULT NOW()`

**2-7. bookings テーブル**（予約）
- `id SERIAL PRIMARY KEY`
- `member_plan_id INTEGER REFERENCES member_plans(id) ON DELETE SET NULL`
- `menu_id INTEGER NOT NULL REFERENCES meeting_menus(id)`
- `guest_email TEXT`
- `guest_name TEXT`
- `guest_token TEXT UNIQUE`
- `start_time TIMESTAMPTZ NOT NULL`
- `end_time TIMESTAMPTZ NOT NULL`
- `status TEXT NOT NULL DEFAULT 'confirmed' CHECK (status IN ('confirmed', 'completed', 'canceled'))`
- `zoom_meeting_id TEXT`
- `zoom_join_url TEXT`
- `google_event_id TEXT`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- `CONSTRAINT no_overlapping_bookings EXCLUDE USING gist (tstzrange(start_time, end_time) WITH &&) WHERE (status != 'canceled')`
- `CONSTRAINT valid_booking_time_range CHECK (start_time < end_time)`
- `CONSTRAINT member_or_guest CHECK ((member_plan_id IS NOT NULL AND guest_email IS NULL) OR (member_plan_id IS NULL AND guest_email IS NOT NULL))`

**2-8. app_settings テーブル**（アプリ設定）
- `id SERIAL PRIMARY KEY`
- `key TEXT NOT NULL UNIQUE`
- `value TEXT`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`

**3. インデックス作成**

RESEARCH.mdのピットフォール5に従い、全外部キーにインデックス作成:
```sql
CREATE INDEX idx_member_plans_user_id ON member_plans(user_id);
CREATE INDEX idx_member_plans_plan_id ON member_plans(plan_id);
CREATE INDEX idx_point_transactions_member_plan_id ON point_transactions(member_plan_id);
CREATE INDEX idx_bookings_member_plan_id ON bookings(member_plan_id);
CREATE INDEX idx_bookings_menu_id ON bookings(menu_id);
CREATE INDEX idx_bookings_start_time ON bookings(start_time) WHERE status != 'canceled';
CREATE INDEX idx_bookings_guest_token ON bookings(guest_token) WHERE guest_token IS NOT NULL;
```

**4. updated_at自動更新トリガー**
```sql
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 各テーブルにトリガー設定
CREATE TRIGGER update_profiles_updated_at BEFORE UPDATE ON profiles FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_plans_updated_at BEFORE UPDATE ON plans FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_member_plans_updated_at BEFORE UPDATE ON member_plans FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_meeting_menus_updated_at BEFORE UPDATE ON meeting_menus FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_weekly_schedules_updated_at BEFORE UPDATE ON weekly_schedules FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_bookings_updated_at BEFORE UPDATE ON bookings FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_app_settings_updated_at BEFORE UPDATE ON app_settings FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

**5. ゲストトークン自動生成トリガー**
```sql
CREATE OR REPLACE FUNCTION generate_guest_token()
RETURNS TRIGGER AS $$
BEGIN
    IF NEW.guest_email IS NOT NULL AND NEW.guest_token IS NULL THEN
        NEW.guest_token := uuid_generate_v4()::TEXT;
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER set_guest_token BEFORE INSERT ON bookings FOR EACH ROW EXECUTE FUNCTION generate_guest_token();
```

**重要:** 全ての時刻カラムは `TIMESTAMPTZ` を使用（RESEARCH.mdのピットフォール2に従う）。`timestamp` は使用しない。
  </action>
  <verify>
- `supabase db reset` を実行してマイグレーション適用
- `supabase db diff` で差分がないことを確認（全て適用済み）
- ローカルSupabase Studioで8テーブルが存在することを確認
- `bookings` テーブルに重複する時間範囲のレコード挿入を試み、制約エラーが発生することを確認
  </verify>
  <done>
8テーブルが作成され、EXCLUDE制約で二重予約が防止され、全外部キーにインデックスが設定されている。
  </done>
</task>

<task type="auto">
  <name>Task 3: ロールバック用マイグレーションとシードデータの作成</name>
  <files>
    supabase/migrations/20260222000001_initial_schema_down.sql
    supabase/seed.sql
  </files>
  <action>
**3-1. DOWNマイグレーション作成**

`supabase/migrations/20260222000001_initial_schema_down.sql` を作成:
```sql
-- Triggers
DROP TRIGGER IF EXISTS set_guest_token ON bookings;
DROP TRIGGER IF EXISTS update_app_settings_updated_at ON app_settings;
DROP TRIGGER IF EXISTS update_bookings_updated_at ON bookings;
DROP TRIGGER IF EXISTS update_weekly_schedules_updated_at ON weekly_schedules;
DROP TRIGGER IF EXISTS update_meeting_menus_updated_at ON meeting_menus;
DROP TRIGGER IF EXISTS update_member_plans_updated_at ON member_plans;
DROP TRIGGER IF EXISTS update_plans_updated_at ON plans;
DROP TRIGGER IF EXISTS update_profiles_updated_at ON profiles;

-- Functions
DROP FUNCTION IF EXISTS generate_guest_token();
DROP FUNCTION IF EXISTS update_updated_at_column();

-- Tables (逆順で削除)
DROP TABLE IF EXISTS app_settings CASCADE;
DROP TABLE IF EXISTS bookings CASCADE;
DROP TABLE IF EXISTS point_transactions CASCADE;
DROP TABLE IF EXISTS weekly_schedules CASCADE;
DROP TABLE IF EXISTS meeting_menus CASCADE;
DROP TABLE IF EXISTS member_plans CASCADE;
DROP TABLE IF EXISTS plans CASCADE;
DROP TABLE IF EXISTS profiles CASCADE;

-- Extensions
DROP EXTENSION IF EXISTS "uuid-ossp";
DROP EXTENSION IF EXISTS btree_gist;
```

**3-2. シードデータ作成**

`supabase/seed.sql` を作成し、マスターデータを定義:

```sql
-- Plans（サブスクプラン）
INSERT INTO plans (name, monthly_points, max_points, price_monthly, is_active) VALUES
('無料プラン', 0, 0, 0, true),
('ベーシックプラン', 400, 800, 5000, true),
('スタンダードプラン', 1000, 2000, 10000, true),
('プレミアムプラン', 2000, 4000, 18000, true)
ON CONFLICT DO NOTHING;

-- Meeting Menus（セッション種別）
INSERT INTO meeting_menus (name, duration_minutes, points_required, zoom_account, description, is_active) VALUES
('カジュアル30分', 30, 0, 'B', 'ゲスト向け無料体験セッション（40分制限）', true),
('ショートセッション', 30, 100, 'A', '会員向け30分セッション', true),
('スタンダードセッション', 60, 200, 'A', '会員向け60分セッション', true),
('ロングセッション', 90, 300, 'A', '会員向け90分セッション', true)
ON CONFLICT DO NOTHING;

-- Weekly Schedules（営業時間デフォルト設定: 月〜金 10:00-18:00）
INSERT INTO weekly_schedules (day_of_week, is_holiday_pattern, start_time, end_time) VALUES
(1, false, '10:00', '18:00'), -- Monday
(2, false, '10:00', '18:00'), -- Tuesday
(3, false, '10:00', '18:00'), -- Wednesday
(4, false, '10:00', '18:00'), -- Thursday
(5, false, '10:00', '18:00')  -- Friday
ON CONFLICT DO NOTHING;

-- App Settings（カレンダー同期タイムスタンプ初期値）
INSERT INTO app_settings (key, value) VALUES
('last_calendar_sync', NULL)
ON CONFLICT (key) DO NOTHING;
```

**3-3. ロールバックテスト実施**

1. `supabase db reset` で全マイグレーション適用 + シードデータ投入
2. `psql` でローカルDBに接続し、DOWNマイグレーションを手動実行してロールバック確認
3. 再度 `supabase db reset` でUPマイグレーション適用を確認

**重要:** CONTEXT.mdに従い、全マイグレーションにUP/DOWNを作成する。
  </action>
  <verify>
- `supabase db reset` 実行後、`plans` テーブルに4レコード存在
- `meeting_menus` テーブルに4レコード存在
- `weekly_schedules` テーブルに5レコード（月〜金）存在
- DOWNマイグレーション実行後、全テーブルが削除される
- 再度 `supabase db reset` で復元できる
  </verify>
  <done>
マイグレーションのロールバックが可能で、シードデータが自動投入される。
  </done>
</task>

</tasks>

<verification>
**全体検証:**

1. **二重予約防止のテスト:**
   ```sql
   -- 予約1: 2026-03-01 14:00-15:00
   INSERT INTO bookings (menu_id, guest_email, guest_name, start_time, end_time, status)
   VALUES (1, 'test@example.com', 'Test User', '2026-03-01 14:00:00+09', '2026-03-01 15:00:00+09', 'confirmed');

   -- 予約2: 2026-03-01 14:30-15:30（重複するため失敗すべき）
   INSERT INTO bookings (menu_id, guest_email, guest_name, start_time, end_time, status)
   VALUES (1, 'test2@example.com', 'Test User 2', '2026-03-01 14:30:00+09', '2026-03-01 15:30:00+09', 'confirmed');
   -- Expected: ERROR: conflicting key value violates exclusion constraint "no_overlapping_bookings"
   ```

2. **タイムゾーン確認:**
   ```sql
   SELECT start_time, start_time AT TIME ZONE 'Asia/Tokyo' AS jst_time FROM bookings LIMIT 1;
   -- タイムゾーン変換が正しく動作することを確認
   ```

3. **外部キーインデックス確認:**
   ```sql
   SELECT indexname FROM pg_indexes WHERE tablename = 'bookings';
   -- idx_bookings_member_plan_id, idx_bookings_menu_id が存在することを確認
   ```

4. **制約チェック:**
   - ポイントマイナス残高の挿入が拒否される
   - 会員とゲスト情報の両方NULLの予約が拒否される
   - 開始時刻 >= 終了時刻の予約が拒否される

5. **マイグレーションのCI/CD準備:**
   - `supabase db diff` で未適用のマイグレーションがない
   - `supabase db reset` が正常に完了する
</verification>

<success_criteria>
**Phase 1 Plan 01が成功したと言えるのは:**

- [ ] Supabaseローカル開発環境が起動している
- [ ] 8テーブル（profiles, plans, member_plans, meeting_menus, weekly_schedules, bookings, point_transactions, app_settings）が作成されている
- [ ] EXCLUDE制約により重複時間帯への予約挿入が自動的に拒否される
- [ ] 全ての時刻カラムが `TIMESTAMPTZ` 型で定義されている
- [ ] 全ての外部キーにインデックスが作成されている
- [ ] シードデータ（4プラン、4メニュー、5営業時間）が投入されている
- [ ] マイグレーションファイル（UP/DOWN）が存在し、`supabase db reset` で正常に適用される
- [ ] DOWNマイグレーションでロールバックが可能である
</success_criteria>

<output>
完了後、`.planning/phases/01-database-foundation/01-01-SUMMARY.md` を作成。

**SUMMARY.mdに含めるべき内容:**
- 作成された8テーブルの概要とリレーション図
- EXCLUDE制約の動作確認結果
- シードデータの内容
- 次のプラン（01-02）への引き継ぎ事項（RLSポリシー、Stored Proceduresの前提条件）
</output>
