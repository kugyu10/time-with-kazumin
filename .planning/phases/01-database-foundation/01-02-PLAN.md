---
phase: 01-database-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260222000002_rls_policies.sql
  - supabase/migrations/20260222000002_rls_policies_down.sql
  - supabase/migrations/20260222000003_stored_procedures.sql
  - supabase/migrations/20260222000003_stored_procedures_down.sql
autonomous: true
requirements:
  - SYS-01

must_haves:
  truths:
    - "会員は自分のデータのみアクセスできる"
    - "管理者は全データにアクセスできる"
    - "ゲストはメニューと自分の予約のみ参照できる"
    - "ポイント消費・返還がアトミックに実行される"
    - "ポイント残高がマイナスにならない"
    - "全ポイント操作が履歴に記録される"
  artifacts:
    - path: "supabase/migrations/20260222000002_rls_policies.sql"
      provides: "8テーブルのRLSポリシー定義"
      min_lines: 150
    - path: "supabase/migrations/20260222000003_stored_procedures.sql"
      provides: "ポイント操作用Stored Procedures（3関数）"
      min_lines: 150
    - path: "supabase/migrations/20260222000002_rls_policies_down.sql"
      provides: "RLSポリシー削除DDL"
      min_lines: 30
    - path: "supabase/migrations/20260222000003_stored_procedures_down.sql"
      provides: "Stored Procedures削除DDL"
      min_lines: 10
  key_links:
    - from: "RLS policies"
      to: "auth.uid()"
      via: "JWT claim参照"
      pattern: "auth\\.uid\\(\\)"
    - from: "consume_points() procedure"
      to: "member_plans table"
      via: "SELECT FOR UPDATE NOWAIT"
      pattern: "FOR UPDATE NOWAIT"
    - from: "point_transactions table"
      to: "consume_points/refund_points"
      via: "監査証跡記録"
      pattern: "INSERT INTO point_transactions"
---

<objective>
JWT ClaimベースのRow Level Security（RLS）とトランザクション整合性を担保したStored Proceduresの実装。会員・ゲスト・管理者の権限分離とポイント二重消費防止を実現する。

Purpose: データベースレベルでのアクセス制御とポイント操作の原子性を確立し、アプリケーション層での実装漏れリスクを排除する。

Output: RLSポリシー（全テーブル）、Stored Procedures（consume_points, refund_points, grant_monthly_points）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Row Level Security (RLS) ポリシーの実装</name>
  <files>
    supabase/migrations/20260222000002_rls_policies.sql
  </files>
  <action>
`supabase/migrations/20260222000002_rls_policies.sql` を作成し、CONTEXT.mdの決定事項に従ってRLSポリシーを実装:

**1. RLSを全テーブルで有効化**
```sql
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE plans ENABLE ROW LEVEL SECURITY;
ALTER TABLE member_plans ENABLE ROW LEVEL SECURITY;
ALTER TABLE meeting_menus ENABLE ROW LEVEL SECURITY;
ALTER TABLE weekly_schedules ENABLE ROW LEVEL SECURITY;
ALTER TABLE bookings ENABLE ROW LEVEL SECURITY;
ALTER TABLE point_transactions ENABLE ROW LEVEL SECURITY;
ALTER TABLE app_settings ENABLE ROW LEVEL SECURITY;
```

**2. profiles テーブルのポリシー**
```sql
-- 会員: 自分のプロフィールのみ参照・更新可能
CREATE POLICY "Users can view their own profile"
ON profiles FOR SELECT
TO authenticated
USING (id = auth.uid());

CREATE POLICY "Users can update their own profile"
ON profiles FOR UPDATE
TO authenticated
USING (id = auth.uid())
WITH CHECK (id = auth.uid());

-- 管理者: 全プロフィール参照・更新可能
CREATE POLICY "Admins can view all profiles"
ON profiles FOR SELECT
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');

CREATE POLICY "Admins can update all profiles"
ON profiles FOR UPDATE
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');

-- 管理者: プロフィール作成可能（会員招待時）
CREATE POLICY "Admins can insert profiles"
ON profiles FOR INSERT
TO authenticated
WITH CHECK ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**3. plans テーブルのポリシー**
```sql
-- 全員: プラン一覧参照可能（公開情報）
CREATE POLICY "Anyone can view active plans"
ON plans FOR SELECT
TO anon, authenticated
USING (is_active = true);

-- 管理者: プラン管理（作成・更新）
CREATE POLICY "Admins can manage plans"
ON plans FOR ALL
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**4. member_plans テーブルのポリシー**
```sql
-- 会員: 自分のプラン契約のみ参照可能
CREATE POLICY "Members can view their own plan"
ON member_plans FOR SELECT
TO authenticated
USING (user_id = auth.uid());

-- 管理者: 全会員プラン参照・管理可能
CREATE POLICY "Admins can manage all member plans"
ON member_plans FOR ALL
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');

-- 管理者: 会員プラン作成（会員登録時）
CREATE POLICY "Admins can insert member plans"
ON member_plans FOR INSERT
TO authenticated
WITH CHECK ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**5. meeting_menus テーブルのポリシー**
```sql
-- 全員: メニュー一覧参照可能（公開情報）
CREATE POLICY "Anyone can view active menus"
ON meeting_menus FOR SELECT
TO anon, authenticated
USING (is_active = true);

-- 管理者: メニュー管理
CREATE POLICY "Admins can manage menus"
ON meeting_menus FOR ALL
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**6. weekly_schedules テーブルのポリシー**
```sql
-- 全員: 営業時間参照可能（空きスロット計算で使用）
CREATE POLICY "Anyone can view schedules"
ON weekly_schedules FOR SELECT
TO anon, authenticated
USING (true);

-- 管理者: 営業時間管理
CREATE POLICY "Admins can manage schedules"
ON weekly_schedules FOR ALL
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**7. bookings テーブルのポリシー**
```sql
-- 会員: 自分の予約のみ参照可能
CREATE POLICY "Members can view their own bookings"
ON bookings FOR SELECT
TO authenticated
USING (
    member_plan_id IN (
        SELECT id FROM member_plans WHERE user_id = auth.uid()
    )
);

-- 会員: 自分の予約を作成可能
CREATE POLICY "Members can create their own bookings"
ON bookings FOR INSERT
TO authenticated
WITH CHECK (
    member_plan_id IN (
        SELECT id FROM member_plans WHERE user_id = auth.uid()
    )
);

-- 会員: 自分の予約をキャンセル可能（ステータス更新）
CREATE POLICY "Members can cancel their own bookings"
ON bookings FOR UPDATE
TO authenticated
USING (
    member_plan_id IN (
        SELECT id FROM member_plans WHERE user_id = auth.uid()
    )
)
WITH CHECK (status IN ('confirmed', 'canceled'));

-- ゲスト: 自分のトークンで予約参照可能
-- Note: ゲストはトークンをクエリパラメータで渡し、アプリケーション層でフィルタリング
-- RLSではanonキーの全アクセスを拒否（セキュリティ強化）
CREATE POLICY "Guests cannot directly access bookings"
ON bookings FOR SELECT
TO anon
USING (false);

-- 管理者: 全予約参照・管理可能
CREATE POLICY "Admins can manage all bookings"
ON bookings FOR ALL
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**8. point_transactions テーブルのポリシー**
```sql
-- 会員: 自分のポイント履歴のみ参照可能
CREATE POLICY "Members can view their own transactions"
ON point_transactions FOR SELECT
TO authenticated
USING (
    member_plan_id IN (
        SELECT id FROM member_plans WHERE user_id = auth.uid()
    )
);

-- 管理者: 全履歴参照可能（監査目的）
CREATE POLICY "Admins can view all transactions"
ON point_transactions FOR SELECT
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');

-- Note: INSERTはStored Procedureのみ許可（直接挿入は禁止）
-- アプリケーション層からの直接挿入を防ぐため、INSERTポリシーは設定しない
```

**9. app_settings テーブルのポリシー**
```sql
-- 管理者のみアクセス可能（システム設定）
CREATE POLICY "Admins can manage app settings"
ON app_settings FOR ALL
TO authenticated
USING ((SELECT auth.jwt() -> 'app_metadata' ->> 'role')::TEXT = 'admin');
```

**重要:** RESEARCH.mdのPattern 3に従い、JWT claimは `SELECT` でラップしてキャッシュ化する（パフォーマンス最適化）。
  </action>
  <verify>
- `supabase db reset` でマイグレーション適用
- `supabase db diff` で差分なし
- ローカルSupabase Studioで各テーブルのRLSが有効化されていることを確認
- 以下のSQLでポリシー一覧を確認:
  ```sql
  SELECT schemaname, tablename, policyname FROM pg_policies WHERE schemaname = 'public';
  ```
- ポリシー数が20個以上存在することを確認
  </verify>
  <done>
全テーブルでRLSが有効化され、会員・ゲスト・管理者の権限分離が実装されている。
  </done>
</task>

<task type="auto">
  <name>Task 2: Stored Procedures（ポイント操作）の実装</name>
  <files>
    supabase/migrations/20260222000003_stored_procedures.sql
  </files>
  <action>
`supabase/migrations/20260222000003_stored_procedures.sql` を作成し、RESEARCH.mdのPattern 1に従ってポイント操作用Stored Proceduresを実装:

**1. consume_points() - ポイント消費**

CONTEXT.mdの決定事項に従い、`SELECT FOR UPDATE NOWAIT` パターンを採用:

```sql
CREATE OR REPLACE FUNCTION consume_points(
    p_member_plan_id INTEGER,
    p_points INTEGER,
    p_transaction_type TEXT DEFAULT 'consume',
    p_reference_id INTEGER DEFAULT NULL,
    p_notes TEXT DEFAULT NULL
)
RETURNS INTEGER
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
    v_current_balance INTEGER;
    v_new_balance INTEGER;
BEGIN
    -- ロック取得（NOWAIT: 競合時は即座にエラー）
    SELECT current_points INTO v_current_balance
    FROM member_plans
    WHERE id = p_member_plan_id
    FOR UPDATE NOWAIT;

    -- プラン存在チェック
    IF v_current_balance IS NULL THEN
        RAISE EXCEPTION 'Member plan not found: %', p_member_plan_id;
    END IF;

    -- 残高チェック（CONTEXT.mdに従い、マイナス残高は拒否）
    IF v_current_balance < p_points THEN
        RAISE EXCEPTION 'Insufficient points: available=%, required=%', v_current_balance, p_points;
    END IF;

    -- ポイント消費
    v_new_balance := v_current_balance - p_points;
    UPDATE member_plans
    SET current_points = v_new_balance,
        updated_at = NOW()
    WHERE id = p_member_plan_id;

    -- 履歴記録
    INSERT INTO point_transactions (
        member_plan_id,
        points,
        transaction_type,
        reference_id,
        notes,
        balance_after
    ) VALUES (
        p_member_plan_id,
        -p_points,
        p_transaction_type,
        p_reference_id,
        p_notes,
        v_new_balance
    );

    RETURN v_new_balance;
END;
$$;

-- セキュリティ: この関数はauthenticatedロールのみ実行可能
REVOKE EXECUTE ON FUNCTION consume_points FROM PUBLIC;
GRANT EXECUTE ON FUNCTION consume_points TO authenticated;
```

**2. refund_points() - ポイント返還**

```sql
CREATE OR REPLACE FUNCTION refund_points(
    p_member_plan_id INTEGER,
    p_points INTEGER,
    p_transaction_type TEXT DEFAULT 'refund',
    p_reference_id INTEGER DEFAULT NULL,
    p_notes TEXT DEFAULT NULL
)
RETURNS INTEGER
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
    v_current_balance INTEGER;
    v_new_balance INTEGER;
BEGIN
    -- ロック取得（返還は競合少ないが、整合性のためロック）
    SELECT current_points INTO v_current_balance
    FROM member_plans
    WHERE id = p_member_plan_id
    FOR UPDATE;

    IF v_current_balance IS NULL THEN
        RAISE EXCEPTION 'Member plan not found: %', p_member_plan_id;
    END IF;

    -- ポイント返還
    v_new_balance := v_current_balance + p_points;
    UPDATE member_plans
    SET current_points = v_new_balance,
        updated_at = NOW()
    WHERE id = p_member_plan_id;

    -- 履歴記録
    INSERT INTO point_transactions (
        member_plan_id,
        points,
        transaction_type,
        reference_id,
        notes,
        balance_after
    ) VALUES (
        p_member_plan_id,
        p_points,
        p_transaction_type,
        p_reference_id,
        p_notes,
        v_new_balance
    );

    RETURN v_new_balance;
END;
$$;

REVOKE EXECUTE ON FUNCTION refund_points FROM PUBLIC;
GRANT EXECUTE ON FUNCTION refund_points TO authenticated;
```

**3. grant_monthly_points() - 月次ポイント付与**

RESEARCH.mdのOpen Question 1に従い、`max_points` 上限付き繰り越しを実装:

```sql
CREATE OR REPLACE FUNCTION grant_monthly_points()
RETURNS TABLE(member_plan_id INTEGER, granted_points INTEGER, new_balance INTEGER)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
    v_member_plan RECORD;
    v_plan_max_points INTEGER;
    v_new_balance INTEGER;
    v_granted INTEGER;
BEGIN
    -- アクティブな全会員プランを処理
    FOR v_member_plan IN
        SELECT mp.id, mp.current_points, mp.monthly_points, mp.plan_id
        FROM member_plans mp
        WHERE mp.status = 'active'
        FOR UPDATE
    LOOP
        -- プランの上限取得（NULLは無制限）
        SELECT p.max_points INTO v_plan_max_points
        FROM plans p
        WHERE p.id = v_member_plan.plan_id;

        -- 繰り越し上限計算
        IF v_plan_max_points IS NULL THEN
            -- 無制限の場合、全額付与
            v_new_balance := v_member_plan.current_points + v_member_plan.monthly_points;
            v_granted := v_member_plan.monthly_points;
        ELSE
            -- 上限ありの場合、max_pointsを超えない範囲で付与
            v_new_balance := LEAST(
                v_member_plan.current_points + v_member_plan.monthly_points,
                v_plan_max_points
            );
            v_granted := v_new_balance - v_member_plan.current_points;
        END IF;

        -- ポイント更新
        UPDATE member_plans
        SET current_points = v_new_balance,
            updated_at = NOW()
        WHERE id = v_member_plan.id;

        -- 履歴記録
        INSERT INTO point_transactions (
            member_plan_id,
            points,
            transaction_type,
            reference_id,
            notes,
            balance_after
        ) VALUES (
            v_member_plan.id,
            v_granted,
            'monthly_grant',
            NULL,
            'Monthly point grant',
            v_new_balance
        );

        -- 結果返却
        member_plan_id := v_member_plan.id;
        granted_points := v_granted;
        new_balance := v_new_balance;
        RETURN NEXT;
    END LOOP;
END;
$$;

-- セキュリティ: Edge Functionsのみ実行可能（service_role）
REVOKE EXECUTE ON FUNCTION grant_monthly_points FROM PUBLIC;
GRANT EXECUTE ON FUNCTION grant_monthly_points TO service_role;
```

**重要:**
- 全関数に `SECURITY DEFINER` を設定してRLSをバイパス（RESEARCH.mdのPattern 4）
- `SET search_path = public` でスキーマ混入攻撃を防止
- `REVOKE/GRANT` で適切なロールのみ実行可能にする
  </action>
  <verify>
- `supabase db reset` でマイグレーション適用
- 以下のSQLで関数が作成されていることを確認:
  ```sql
  SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('consume_points', 'refund_points', 'grant_monthly_points');
  ```
- `prosecdef` が `t` (SECURITY DEFINER) であることを確認
- ポイント消費のテスト:
  ```sql
  -- テスト用会員プラン作成（手動挿入）
  INSERT INTO member_plans (user_id, plan_id, current_points, monthly_points, status)
  VALUES ('00000000-0000-0000-0000-000000000000', 1, 500, 100, 'active');

  -- ポイント消費テスト
  SELECT consume_points(1, 100, 'consume', NULL, 'Test consumption');
  -- Expected: 400 (新しい残高)

  -- ポイント不足テスト
  SELECT consume_points(1, 500, 'consume', NULL, 'Should fail');
  -- Expected: ERROR: Insufficient points: available=400, required=500

  -- ポイント返還テスト
  SELECT refund_points(1, 50, 'refund', NULL, 'Test refund');
  -- Expected: 450 (新しい残高)
  ```
  </verify>
  <done>
ポイント操作用Stored Procedures（consume_points, refund_points, grant_monthly_points）が実装され、トランザクション整合性と監査証跡記録が担保されている。
  </done>
</task>

<task type="auto">
  <name>Task 3: ロールバック用マイグレーションの作成</name>
  <files>
    supabase/migrations/20260222000002_rls_policies_down.sql
    supabase/migrations/20260222000003_stored_procedures_down.sql
  </files>
  <action>
**3-1. RLSポリシーDOWNマイグレーション**

`supabase/migrations/20260222000002_rls_policies_down.sql` を作成:

```sql
-- Drop all policies
DROP POLICY IF EXISTS "Admins can manage app settings" ON app_settings;
DROP POLICY IF EXISTS "Admins can view all transactions" ON point_transactions;
DROP POLICY IF EXISTS "Members can view their own transactions" ON point_transactions;
DROP POLICY IF EXISTS "Admins can manage all bookings" ON bookings;
DROP POLICY IF EXISTS "Guests cannot directly access bookings" ON bookings;
DROP POLICY IF EXISTS "Members can cancel their own bookings" ON bookings;
DROP POLICY IF EXISTS "Members can create their own bookings" ON bookings;
DROP POLICY IF EXISTS "Members can view their own bookings" ON bookings;
DROP POLICY IF EXISTS "Admins can manage schedules" ON weekly_schedules;
DROP POLICY IF EXISTS "Anyone can view schedules" ON weekly_schedules;
DROP POLICY IF EXISTS "Admins can manage menus" ON meeting_menus;
DROP POLICY IF EXISTS "Anyone can view active menus" ON meeting_menus;
DROP POLICY IF EXISTS "Admins can insert member plans" ON member_plans;
DROP POLICY IF EXISTS "Admins can manage all member plans" ON member_plans;
DROP POLICY IF EXISTS "Members can view their own plan" ON member_plans;
DROP POLICY IF EXISTS "Admins can manage plans" ON plans;
DROP POLICY IF EXISTS "Anyone can view active plans" ON plans;
DROP POLICY IF EXISTS "Admins can insert profiles" ON profiles;
DROP POLICY IF EXISTS "Admins can update all profiles" ON profiles;
DROP POLICY IF EXISTS "Admins can view all profiles" ON profiles;
DROP POLICY IF EXISTS "Users can update their own profile" ON profiles;
DROP POLICY IF EXISTS "Users can view their own profile" ON profiles;

-- Disable RLS
ALTER TABLE app_settings DISABLE ROW LEVEL SECURITY;
ALTER TABLE point_transactions DISABLE ROW LEVEL SECURITY;
ALTER TABLE bookings DISABLE ROW LEVEL SECURITY;
ALTER TABLE weekly_schedules DISABLE ROW LEVEL SECURITY;
ALTER TABLE meeting_menus DISABLE ROW LEVEL SECURITY;
ALTER TABLE member_plans DISABLE ROW LEVEL SECURITY;
ALTER TABLE plans DISABLE ROW LEVEL SECURITY;
ALTER TABLE profiles DISABLE ROW LEVEL SECURITY;
```

**3-2. Stored ProceduresDOWNマイグレーション**

`supabase/migrations/20260222000003_stored_procedures_down.sql` を作成:

```sql
-- Drop functions
DROP FUNCTION IF EXISTS grant_monthly_points();
DROP FUNCTION IF EXISTS refund_points(INTEGER, INTEGER, TEXT, INTEGER, TEXT);
DROP FUNCTION IF EXISTS consume_points(INTEGER, INTEGER, TEXT, INTEGER, TEXT);
```

**3-3. ロールバックテスト**

1. `supabase db reset` で全マイグレーション適用
2. DOWNマイグレーションを手動実行してロールバック確認:
   ```bash
   psql $(supabase status | grep 'DB URL' | awk '{print $3}') -f supabase/migrations/20260222000003_stored_procedures_down.sql
   psql $(supabase status | grep 'DB URL' | awk '{print $3}') -f supabase/migrations/20260222000002_rls_policies_down.sql
   ```
3. 関数とポリシーが削除されていることを確認
4. 再度 `supabase db reset` で復元確認

**重要:** CONTEXT.mdに従い、全マイグレーションにUP/DOWNを作成する。
  </action>
  <verify>
- DOWNマイグレーション実行後、`pg_policies` にポリシーが存在しない
- DOWNマイグレーション実行後、`pg_proc` に3関数が存在しない
- 再度 `supabase db reset` で全て復元される
  </verify>
  <done>
RLSポリシーとStored Proceduresのロールバックが可能である。
  </done>
</task>

</tasks>

<verification>
**全体検証:**

1. **RLSポリシーの動作確認:**
   ```sql
   -- 会員ユーザーとして接続（JWT simulated）
   SET request.jwt.claims = '{"sub": "user-uuid-123", "app_metadata": {"role": "member"}}';

   -- 自分のmember_planは参照可能
   SELECT * FROM member_plans WHERE user_id = 'user-uuid-123';
   -- Expected: 結果が返る

   -- 他人のmember_planは参照不可
   SELECT * FROM member_plans WHERE user_id = 'other-user-uuid';
   -- Expected: 結果が返らない（RLSで除外）

   -- 管理者として接続
   SET request.jwt.claims = '{"sub": "admin-uuid", "app_metadata": {"role": "admin"}}';

   -- 全member_plan参照可能
   SELECT * FROM member_plans;
   -- Expected: 全レコードが返る
   ```

2. **ポイント消費の並行制御テスト:**
   ```bash
   # 2つの同時トランザクションでポイント消費を試行
   # Transaction 1: consume_points(1, 100)
   # Transaction 2: consume_points(1, 100)（ほぼ同時）
   # Expected: どちらか一方が成功、もう一方は lock_not_available エラー
   ```

3. **ポイント履歴の記録確認:**
   ```sql
   SELECT * FROM point_transactions ORDER BY created_at DESC LIMIT 5;
   -- Expected: consume, refund, monthly_grant などの履歴が記録されている
   ```

4. **月次ポイント付与テスト:**
   ```sql
   -- max_points上限ありプランのテスト
   UPDATE member_plans SET current_points = 700 WHERE id = 1;
   SELECT * FROM grant_monthly_points();
   -- Expected: current_points が 800（max_points）に制限される

   -- 無制限プランのテスト（max_points IS NULL）
   UPDATE plans SET max_points = NULL WHERE id = 1;
   SELECT * FROM grant_monthly_points();
   -- Expected: 全額付与される
   ```

5. **マイグレーション完全性チェック:**
   ```bash
   supabase db reset
   # Expected: 全マイグレーション + シードデータ適用完了

   # DOWNマイグレーション実行
   psql ... -f supabase/migrations/20260222000003_stored_procedures_down.sql
   psql ... -f supabase/migrations/20260222000002_rls_policies_down.sql

   # 再度UPマイグレーション
   supabase db reset
   # Expected: 復元完了
   ```
</verification>

<success_criteria>
**Phase 1 Plan 02が成功したと言えるのは:**

- [ ] 全8テーブルでRLSが有効化されている
- [ ] 会員は自分のデータのみアクセスでき、他会員のデータは見えない
- [ ] 管理者は全データにアクセスできる
- [ ] ゲストはメニューと営業時間のみ参照できる
- [ ] ポイント消費時に `SELECT FOR UPDATE NOWAIT` で排他制御が動作する
- [ ] ポイント残高不足時に例外が発生する
- [ ] 全ポイント操作が `point_transactions` テーブルに記録される
- [ ] 月次ポイント付与が `max_points` 上限を遵守する
- [ ] RLSポリシーとStored ProceduresのDOWNマイグレーションが存在し、ロールバック可能である
</success_criteria>

<output>
完了後、`.planning/phases/01-database-foundation/01-02-SUMMARY.md` を作成。

**SUMMARY.mdに含めるべき内容:**
- RLSポリシーの権限マトリックス（会員・ゲスト・管理者 × テーブル × 操作）
- Stored Procedures の使用方法とエラーハンドリング
- ポイント並行制御のテスト結果
- Phase 2への引き継ぎ事項（API Route HandlersからのStored Procedures呼び出し方法）
</output>
