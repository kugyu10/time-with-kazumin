---
phase: 18-saga
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/bookings/types.ts
  - src/lib/bookings/saga.ts
  - src/lib/bookings/cancel.ts
  - src/lib/bookings/__tests__/compensateAll.test.ts
autonomous: true
requirements:
  - BUGFIX-13

must_haves:
  truths:
    - "compensateAll()がcompletedSteps配列を破壊せずに逆順実行する"
    - "補償関数が失敗した場合、失敗詳細がBookingSagaResultに含まれる"
    - "1つの補償が失敗しても他の補償ステップは全て実行される"
    - "cancel.tsのZoom/Calendar削除失敗時に失敗情報がCancelBookingResultに含まれる"
  artifacts:
    - path: "src/lib/bookings/saga.ts"
      provides: "修正済みcompensateAll()と補償失敗収集ロジック"
      contains: "[...completedSteps].reverse()"
    - path: "src/lib/bookings/types.ts"
      provides: "CompensationFailure型とBookingSagaResult.compensationFailuresフィールド"
      contains: "CompensationFailure"
    - path: "src/lib/bookings/cancel.ts"
      provides: "Zoom/Calendar削除失敗情報をCancelBookingResult経由で返却"
      contains: "cleanup_failures"
    - path: "src/lib/bookings/__tests__/compensateAll.test.ts"
      provides: "compensateAll()のユニットテスト"
      min_lines: 50
  key_links:
    - from: "src/lib/bookings/saga.ts"
      to: "src/lib/bookings/types.ts"
      via: "CompensationFailure型のimport"
      pattern: "CompensationFailure"
    - from: "src/lib/bookings/saga.ts"
      to: "呼び出し元(createBookingSaga)"
      via: "compensateAll()の戻り値でCompensationFailure[]を返す"
      pattern: "compensationFailures"
---

<objective>
saga.tsの補償トランザクションが失敗時にリソースが残留する問題を修正する（GitHub Issue #13）。

Purpose: 予約作成が途中失敗した際、Zoom会議やGoogleカレンダーイベントが確実にクリーンアップされ、万が一クリーンアップに失敗した場合はユーザーと管理者に詳細が伝わるようにする。
Output: 修正済みsaga.ts, types.ts, cancel.ts + compensateAllのユニットテスト
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-saga/18-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/bookings/types.ts:
```typescript
export interface BookingSagaResult {
  success: boolean
  booking?: BookingResponse
  error?: string
  errorCode?: string
}

export interface BookingSagaContext {
  // ... (full context with zoomMeetingId, googleEventId, etc.)
  pointsConsumed: boolean
  bookingId?: number
  zoomMeetingId?: string
  zoomJoinUrl?: string
  zoomStartUrl?: string
  zoomAccountType?: "A" | "B"
  googleEventId?: string
}
```

From src/lib/bookings/cancel.ts:
```typescript
export type CancelBookingResult = {
  success: boolean
  refunded_points?: number
  error?: string
  error_code?: string
}
```

From src/lib/bookings/saga.ts (L578-603, compensateAll - the main fix target):
```typescript
async function compensateAll(
  supabase: SupabaseClient<Database>,
  context: BookingSagaContext,
  completedSteps: string[]
): Promise<void> {
  // BUG: completedSteps.reverse() mutates the original array
  for (const step of completedSteps.reverse()) {
    switch (step) {
      case "add_calendar":
        await compensateCalendarDelete(context)
        break
      case "create_zoom":
        await compensateZoomDelete(context)
        break
      case "create_booking":
        await compensateBookingCancel(supabase, context)
        break
      case "consume_points":
        await compensatePointsRefund(supabase, context)
        break
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saga.ts compensateAll修正 + 型拡張 + ユニットテスト</name>
  <files>src/lib/bookings/types.ts, src/lib/bookings/saga.ts, src/lib/bookings/__tests__/compensateAll.test.ts</files>
  <read_first>src/lib/bookings/saga.ts, src/lib/bookings/types.ts, src/lib/utils/retry.ts</read_first>
  <behavior>
    - Test 1: compensateAll は completedSteps 配列を変更しない（非破壊的逆順）
    - Test 2: 全補償が成功した場合、空の CompensationFailure[] を返す
    - Test 3: Zoom削除が失敗しても Calendar削除・Booking取消・Points返還は実行される
    - Test 4: 複数の補償が失敗した場合、全ての失敗が CompensationFailure[] に収集される
    - Test 5: createBookingSaga が補償失敗時に compensationFailures をresultに含める
  </behavior>
  <action>
    **Step 1: types.ts に CompensationFailure 型を追加（per D-01）**

    ```typescript
    export interface CompensationFailure {
      step: string        // e.g. "create_zoom", "add_calendar"
      error: string       // エラーメッセージ
    }
    ```

    BookingSagaResult に `compensationFailures?: CompensationFailure[]` フィールドを追加。

    **Step 2: saga.ts の compensateAll() を修正**

    1. `completedSteps.reverse()` を `[...completedSteps].reverse()` に修正（per D-03）
    2. 戻り値を `Promise<CompensationFailure[]>` に変更
    3. 各補償関数（compensateZoomDelete, compensateCalendarDelete, compensateBookingCancel, compensatePointsRefund）の catch ブロックで失敗を配列に収集
    4. 補償は全ステップ最後まで実行する（per D-02、現状の方針維持）

    修正後の compensateAll シグネチャ:
    ```typescript
    async function compensateAll(
      supabase: SupabaseClient<Database>,
      context: BookingSagaContext,
      completedSteps: string[]
    ): Promise<CompensationFailure[]>
    ```

    各補償ステップで try-catch し、catch 時に `failures.push({ step, error: String(error) })` して continue。

    **Step 3: createBookingSaga() の compensateAll 呼び出し箇所を修正**

    saga.ts 内で compensateAll を呼ぶ4箇所（L187, L219, L232, L288）全てで:
    - `const failures = await compensateAll(...)` で失敗を受け取る
    - return する BookingSagaResult に `compensationFailures: failures.length > 0 ? failures : undefined` を含める
    - failures がある場合、error メッセージの末尾に「一部のリソースのクリーンアップに失敗しました。管理者にお問い合わせください。」を追記（per D-01）

    **Step 4: テストファイル作成**

    `src/lib/bookings/__tests__/compensateAll.test.ts` を作成。
    saga.ts の compensateAll は private 関数のため、テスト可能にする方法:
    - compensateAll を export する（テスト用）、または
    - createBookingSaga 経由でE2Eライクにテスト

    推奨: compensateAll を `export { compensateAll as _compensateAllForTest }` でテスト用にexport。
    モック対象: supabase client, deleteZoomMeeting, deleteCalendarEvent, retryWithExponentialBackoff。
    vi.mock で外部依存をモック化。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx vitest run src/lib/bookings/__tests__/compensateAll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "\[\.\.\.completedSteps\]\.reverse()" src/lib/bookings/saga.ts で 1 が返る
    - grep -c "completedSteps\.reverse()" src/lib/bookings/saga.ts で 0 が返る（破壊的呼び出しが消えている）
    - grep -c "CompensationFailure" src/lib/bookings/types.ts で 2以上が返る（interface定義 + フィールド使用）
    - grep -c "compensationFailures" src/lib/bookings/saga.ts で 4以上が返る（各呼び出し箇所）
    - テストファイル src/lib/bookings/__tests__/compensateAll.test.ts が存在する
    - npx vitest run src/lib/bookings/__tests__/compensateAll.test.ts が全テスト通過
  </acceptance_criteria>
  <done>
    compensateAll()が非破壊的に逆順実行し、補償失敗をCompensationFailure[]で返却する。createBookingSagaの戻り値に補償失敗詳細が含まれる。ユニットテストが全パス。
  </done>
</task>

<task type="auto">
  <name>Task 2: cancel.ts クリーンアップ失敗情報の戻り値追加</name>
  <files>src/lib/bookings/cancel.ts</files>
  <read_first>src/lib/bookings/cancel.ts, src/lib/bookings/types.ts</read_first>
  <action>
    **per D-04: cancel.ts の非ブロッキング方針を維持しつつ、失敗情報を戻り値に含める**

    **Step 1: CancelBookingResult 型に cleanup_failures フィールドを追加**

    ```typescript
    export type CancelBookingResult = {
      success: boolean
      refunded_points?: number
      error?: string
      error_code?: string
      cleanup_failures?: string[]  // e.g. ["zoom_delete", "calendar_delete"]
    }
    ```

    **Step 2: Zoom削除（L169-179）とCalendar削除（L183-193）の catch ブロックで失敗を収集**

    関数冒頭で `const cleanupFailures: string[] = []` を宣言。

    Zoom削除 catch 内（L177-179付近）:
    ```typescript
    } catch (error) {
      console.warn("[cancelBooking] Zoom deletion failed (non-blocking):", error)
      cleanupFailures.push("zoom_delete")
    }
    ```

    Calendar削除 catch 内（L191-193付近）:
    ```typescript
    } catch (error) {
      console.warn("[cancelBooking] Calendar deletion failed (non-blocking):", error)
      cleanupFailures.push("calendar_delete")
    }
    ```

    **Step 3: 成功時の戻り値に cleanup_failures を含める**

    L252の return 文を修正:
    ```typescript
    return {
      success: true,
      refunded_points: refundedPoints,
      cleanup_failures: cleanupFailures.length > 0 ? cleanupFailures : undefined,
    }
    ```

    注意: `success: true` のままにする。キャンセル自体は成功しているため（per D-04）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx tsc --noEmit src/lib/bookings/cancel.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "cleanup_failures" src/lib/bookings/cancel.ts で 4以上が返る（型定義 + 収集 + 返却）
    - grep -c "cleanupFailures" src/lib/bookings/cancel.ts で 4以上が返る（変数宣言 + push x2 + 返却）
    - TypeScript コンパイルエラーがないこと
  </acceptance_criteria>
  <done>
    cancel.tsのZoom/Calendar削除失敗時に、cleanup_failuresフィールドで失敗したステップ名が返却される。キャンセル自体のsuccess判定は変わらない。
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/bookings/__tests__/compensateAll.test.ts` が全テスト通過
2. `npx tsc --noEmit` でプロジェクト全体の型エラーなし
3. saga.ts 内に `completedSteps.reverse()` の破壊的呼び出しが存在しない
4. BookingSagaResult.compensationFailures が型定義に存在する
5. CancelBookingResult.cleanup_failures が型定義に存在する
</verification>

<success_criteria>
- GitHub Issue #13 の根本原因（completedSteps.reverse() の破壊的変更）が修正されている
- 補償失敗時にCompensationFailure[]が収集され、BookingSagaResultに含まれる
- cancel.tsのクリーンアップ失敗がCancelBookingResultに含まれる
- 全ての変更がTypeScriptコンパイルを通過する
- compensateAllのユニットテストが存在し、全パスする
</success_criteria>

<output>
After completion, create `.planning/phases/18-saga/18-01-SUMMARY.md`
</output>
