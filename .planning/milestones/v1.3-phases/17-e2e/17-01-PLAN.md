---
phase: 17-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "vitest run がe2eディレクトリのPlaywrightテストファイルを実行しない"
    - "既存の129ユニットテストが全てパスする"
---

# Plan 17-01: vitest設定でe2eディレクトリを除外

<objective>
vitest.config.ts の exclude 配列に "e2e" を追加し、Playwright E2Eテストファイルがvitestで誤実行されるのを防ぐ。
</objective>

<key_links>
- vitest.config.ts → e2e/specs/*.spec.ts (exclude pattern prevents loading)
</key_links>

## Tasks

<task id="1" type="auto">
<name>vitest.config.ts の exclude に e2e を追加</name>
<files>vitest.config.ts</files>
<read_first>
- vitest.config.ts (現在の exclude 設定を確認)
- e2e/ ディレクトリ構造を確認
</read_first>
<action>
`vitest.config.ts` の `test.exclude` 配列に `"e2e"` を追加する。

現在:
```typescript
exclude: ["node_modules", ".claude", "supabase", ".next"],
```

変更後:
```typescript
exclude: ["node_modules", ".claude", "supabase", ".next", "e2e"],
```
</action>
<acceptance_criteria>
- vitest.config.ts の test.exclude 配列に "e2e" が含まれる
- `npx vitest run --no-coverage` で e2e/specs/*.spec.ts のエラーが出ない
- 既存ユニットテスト（129件）が全てパスする
- `npx tsc --noEmit` がエラーなし
</acceptance_criteria>
<verify>
<automated>npx vitest run --no-coverage 2>&1 | tail -5</automated>
</verify>
<done>vitest.config.ts に e2e 除外を追加、全ユニットテスト引き続きパス</done>
</task>

## Verification

After execution:
- `npx vitest run` shows 0 failed test files (E2E files no longer loaded)
- All 129+ unit tests continue to pass
