---
phase: 14-plan-type-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/admin/menus.ts
  - src/app/admin/menus/columns.tsx
  - src/app/admin/menus/menus-client.tsx
  - src/app/admin/menus/page.tsx
  - src/components/admin/forms/menu-form.tsx
autonomous: true
requirements: [MENU-02, MENU-04]

must_haves:
  truths:
    - "管理画面でメニュー編集時にプランタイプのチェックボックスが表示される"
    - "チェックボックスで選択したプランタイプがDBに保存される"
    - "全チェックを外すとallowed_plan_types=NULLとして保存される（全プラン表示）"
    - "メニュー一覧テーブルにallowed_plan_typesカラムの型が含まれる"
  artifacts:
    - path: "src/lib/actions/admin/menus.ts"
      provides: "MenuSchema with allowed_plan_types + CRUD support"
      contains: "allowed_plan_types"
    - path: "src/components/admin/forms/menu-form.tsx"
      provides: "Plan type checkbox UI"
      contains: "allowed_plan_types"
    - path: "src/app/admin/menus/columns.tsx"
      provides: "Menu type with allowed_plan_types"
      contains: "allowed_plan_types"
  key_links:
    - from: "src/app/admin/menus/page.tsx"
      to: "plans table"
      via: "Supabase query for plan list"
      pattern: "from.*plans.*select"
    - from: "src/components/admin/forms/menu-form.tsx"
      to: "src/lib/actions/admin/menus.ts"
      via: "createMenu/updateMenu with allowed_plan_types"
      pattern: "allowed_plan_types"
---

<objective>
管理画面のメニューCRUDに `allowed_plan_types` フィールドを追加し、メニューごとに対象プランタイプをチェックボックスで設定可能にする。

Purpose: MENU-02/MENU-04の前提条件として、管理者がメニューのプランタイプ制限を設定できるようにする
Output: allowed_plan_types対応済みの管理画面メニューCRUD
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-plan-type-menu/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/actions/admin/menus.ts (current):
```typescript
const MenuSchema = z.object({
  name: z.string().min(1).max(100),
  duration_minutes: z.number().min(15).max(480),
  points_required: z.number().min(0),
  zoom_account: z.enum(["A", "B"]),
  description: z.string().optional().nullable(),
  is_active: z.boolean(),
  send_thank_you_email: z.boolean(),
})
// getMenus() returns Array<{id, name, duration_minutes, points_required, zoom_account, description, is_active, send_thank_you_email, created_at, updated_at}>
// NOTE: allowed_plan_types is NOT in the type cast yet (L149-163)
```

From src/app/admin/menus/columns.tsx (current):
```typescript
export type Menu = {
  id: number; name: string; duration_minutes: number; points_required: number;
  zoom_account: "A" | "B"; description: string | null; is_active: boolean;
  send_thank_you_email: boolean; created_at: string; updated_at: string;
}
```

From src/components/admin/forms/menu-form.tsx (current):
```typescript
type MenuFormProps = {
  menu?: { id: number; name: string; duration_minutes: number; points_required: number;
    zoom_account: "A" | "B"; description: string | null; is_active: boolean; send_thank_you_email: boolean; }
  onSuccess?: () => void; onCancel?: () => void;
}
// formSchema does NOT include allowed_plan_types yet
// Checkbox from shadcn/ui is already imported
```

From src/app/admin/menus/menus-client.tsx (current):
```typescript
type MenusClientProps = { initialMenus: Menu[] }
// Does NOT accept plans prop yet
```

From src/app/admin/menus/page.tsx (current):
```typescript
// Server Component. Calls getMenus() and passes to MenusClient.
// Does NOT fetch plans yet.
```

DB schema (from Phase 12):
```sql
-- meeting_menus.allowed_plan_types INTEGER[] DEFAULT NULL
-- plans table: id SERIAL, name TEXT, is_active BOOLEAN
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MenuSchema + CRUD + 型定義にallowed_plan_typesを追加</name>
  <files>src/lib/actions/admin/menus.ts, src/app/admin/menus/columns.tsx</files>
  <read_first>src/lib/actions/admin/menus.ts, src/app/admin/menus/columns.tsx, src/types/database.ts</read_first>
  <action>
1. **menus.ts** の `MenuSchema` に `allowed_plan_types` を追加する（per D-07）:
   ```typescript
   allowed_plan_types: z.array(z.number()).nullable().optional(),
   ```

2. **menus.ts** の `createMenu` 関数の `.insert({...})` に追加:
   ```typescript
   allowed_plan_types: validated.allowed_plan_types ?? null,
   ```

3. **menus.ts** の `updateMenu` 関数の `.update({...})` に追加:
   ```typescript
   allowed_plan_types: validated.allowed_plan_types ?? null,
   ```

4. **menus.ts** の `getMenus` 関数の戻り値型キャスト（L149-163付近）に追加（per D-08）:
   ```typescript
   allowed_plan_types: number[] | null
   ```
   を `data: Array<{...}>` の型定義内に追加する。

5. **columns.tsx** の `Menu` 型に追加:
   ```typescript
   allowed_plan_types: number[] | null
   ```
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allowed_plan_types" src/lib/actions/admin/menus.ts` で MenuSchema, createMenu insert, updateMenu update, getMenus 戻り値型の4箇所にヒットする
    - `grep -n "allowed_plan_types" src/app/admin/menus/columns.tsx` で Menu 型定義にヒットする
    - `npx tsc --noEmit` がエラーなしで完了する
  </acceptance_criteria>
  <done>MenuSchema, createMenu, updateMenu, getMenus, Menu型の全てにallowed_plan_typesが追加され、TypeScriptコンパイルが通る</done>
</task>

<task type="auto">
  <name>Task 2: メニュー編集フォームにプランタイプチェックボックスUIを追加</name>
  <files>src/components/admin/forms/menu-form.tsx, src/app/admin/menus/menus-client.tsx, src/app/admin/menus/page.tsx</files>
  <read_first>src/components/admin/forms/menu-form.tsx, src/app/admin/menus/menus-client.tsx, src/app/admin/menus/page.tsx, src/lib/supabase/service-role.ts</read_first>
  <action>
1. **menu-form.tsx** の `formSchema` に追加:
   ```typescript
   allowed_plan_types: z.array(z.number()).optional(),
   ```

2. **menu-form.tsx** の `MenuFormProps` の `menu?` 型に追加:
   ```typescript
   allowed_plan_types: number[] | null
   ```
   また、新しいpropを追加:
   ```typescript
   plans?: Array<{ id: number; name: string }>
   ```

3. **menu-form.tsx** の `defaultValues` に追加:
   ```typescript
   allowed_plan_types: menu?.allowed_plan_types ?? [],
   ```

4. **menu-form.tsx** の `onSubmit` 関数内で、値を変換してからServer Actionを呼ぶ（per D-06: 空配列=NULL=全プラン表示）:
   ```typescript
   const allowedPlanTypes = values.allowed_plan_types?.length
     ? values.allowed_plan_types
     : null
   const submitValues = { ...values, allowed_plan_types: allowedPlanTypes }
   ```
   `createMenu(submitValues)` / `updateMenu(menu.id, submitValues)` に変更。

5. **menu-form.tsx** の `send_thank_you_email` チェックボックスの後に、プランタイプ選択UIを追加（per D-05）。既存の `is_active` チェックボックスと同じレイアウトパターンを使用:
   ```tsx
   {plans && plans.length > 0 && (
     <div className="space-y-2">
       <FormLabel>対象プランタイプ</FormLabel>
       <FormDescription>
         未選択の場合、全プランの会員に表示されます
       </FormDescription>
       {plans.map((plan) => (
         <FormField
           key={plan.id}
           control={form.control}
           name="allowed_plan_types"
           render={({ field }) => (
             <FormItem className="flex flex-row items-start space-x-3 space-y-0">
               <FormControl>
                 <Checkbox
                   checked={field.value?.includes(plan.id)}
                   onCheckedChange={(checked) => {
                     const current = field.value ?? []
                     field.onChange(
                       checked
                         ? [...current, plan.id]
                         : current.filter((id: number) => id !== plan.id)
                     )
                   }}
                 />
               </FormControl>
               <FormLabel className="font-normal">{plan.name}</FormLabel>
             </FormItem>
           )}
         />
       ))}
     </div>
   )}
   ```

6. **menus-client.tsx** の `MenusClientProps` に `plans` を追加:
   ```typescript
   type MenusClientProps = {
     initialMenus: Menu[]
     plans: Array<{ id: number; name: string }>
   }
   ```
   `MenusClient` の引数を `{ initialMenus, plans }` に変更。
   `MenuForm` に `plans={plans}` を渡す（作成ダイアログ・編集ダイアログの両方）。

7. **page.tsx** (Server Component) で `plans` テーブルからアクティブなプラン一覧を取得し、`MenusClient` に渡す:
   ```typescript
   import { getSupabaseServiceRole } from "@/lib/supabase/service-role"

   export default async function MenusPage() {
     const menus = await getMenus()
     const supabase = getSupabaseServiceRole()
     const { data: plans } = await (supabase as any)
       .from("plans")
       .select("id, name")
       .eq("is_active", true)
       .order("id", { ascending: true }) as { data: Array<{ id: number; name: string }> | null; error: unknown }

     return (
       // ... 既存レイアウト ...
       <MenusClient initialMenus={menus} plans={plans ?? []} />
     )
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allowed_plan_types" src/components/admin/forms/menu-form.tsx` でformSchema, defaultValues, onSubmit変換, チェックボックスUIの4箇所にヒットする
    - `grep -n "plans" src/app/admin/menus/menus-client.tsx` で MenusClientProps と MenuForm への plans 受け渡しがある
    - `grep -n "plans" src/app/admin/menus/page.tsx` で plans テーブルクエリと MenusClient への渡しがある
    - `npm run build` がエラーなしで完了する
  </acceptance_criteria>
  <done>管理画面メニューフォームにプランタイプチェックボックスが表示され、選択結果がallowed_plan_typesとしてDB保存される。未選択時はNULL（全プラン表示）</done>
</task>

</tasks>

<verification>
- `npm run build` がエラーなしで完了すること
- `grep -rn "allowed_plan_types" src/lib/actions/admin/menus.ts src/app/admin/menus/ src/components/admin/forms/menu-form.tsx` で全ファイルにヒットすること
- `npx vitest run` で既存テストが全パスすること
</verification>

<success_criteria>
- 管理画面メニューCRUDがallowed_plan_typesフィールドに対応している
- メニュー編集フォームにプランタイプチェックボックスが表示される
- チェック状態がDBに正しく保存される（選択=配列, 未選択=NULL）
- 既存機能が壊れていない（ビルド成功、既存テストパス）
</success_criteria>

<output>
After completion, create `.planning/phases/14-plan-type-menu/14-01-SUMMARY.md`
</output>
