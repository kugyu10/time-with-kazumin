---
phase: 14-plan-type-menu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(member)/bookings/new/page.tsx
  - src/lib/utils/menu-filter.ts
  - src/lib/utils/menu-filter.test.ts
autonomous: true
requirements: [MENU-02, MENU-04]

must_haves:
  truths:
    - "通常プラン会員は通常プラン対象メニューのみ予約画面に表示される"
    - "お金のブロック解消プラン会員はそのプラン専用メニューが表示される"
    - "allowed_plan_typesがNULLのメニューは全プランの会員に表示される（後方互換）"
    - "zoom_account='B'ハードコードフィルタが撤去されている"
  artifacts:
    - path: "src/app/(member)/bookings/new/page.tsx"
      provides: "Plan-type filtered menu display for members"
      contains: "filterMenusByPlanType"
    - path: "src/lib/utils/menu-filter.ts"
      provides: "Pure filter function for plan-type based menu filtering"
      exports: ["filterMenusByPlanType"]
    - path: "src/lib/utils/menu-filter.test.ts"
      provides: "Unit tests for filter logic"
      contains: "filterMenusByPlanType"
  key_links:
    - from: "src/app/(member)/bookings/new/page.tsx"
      to: "member_plans table"
      via: "Supabase query for user plan_id"
      pattern: "from.*member_plans.*select.*plan_id"
    - from: "src/app/(member)/bookings/new/page.tsx"
      to: "src/lib/utils/menu-filter.ts"
      via: "import filterMenusByPlanType"
      pattern: "filterMenusByPlanType"
---

<objective>
会員予約画面（bookings/new）でメニュー一覧をプランタイプでフィルタリングし、会員が自分のプランに対応するメニューのみ表示されるようにする。

Purpose: MENU-02（プランタイプ対応メニューのみ表示）とMENU-04（お金のブロック解消メニューの限定表示）を実現する
Output: プランタイプフィルタ済みの予約画面 + フィルタロジックのユニットテスト
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-plan-type-menu/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/(member)/bookings/new/page.tsx (current, L49-57):
```typescript
// REPLACE THIS: zoom_account="B" hardcoded filter
const { data: menusData, error: menusError } = await supabase
  .from("meeting_menus")
  .select("id, name, description, duration_minutes, points_required")
  .eq("is_active", true)
  .eq("zoom_account", "B")  // <-- DELETE THIS LINE (per D-03)
  .order("points_required", { ascending: true })
```

From src/components/bookings/MenuSelect (Menu type used by page):
```typescript
export type Menu = {
  id: number
  name: string
  description: string | null
  duration_minutes: number
  points_required: number
}
// NOTE: This type does NOT include allowed_plan_types.
// Filter BEFORE passing to MenuSelect (per Pitfall 5 in RESEARCH.md)
```

DB schema (Phase 12):
```sql
-- member_plans: user_id UUID, plan_id INT (FK to plans.id), status TEXT
-- meeting_menus: allowed_plan_types INTEGER[] DEFAULT NULL
-- plans: id SERIAL, name TEXT
```

Key relationships:
- member_plans.plan_id -> plans.id (plan_id IS the plan type ID)
- meeting_menus.allowed_plan_types[] contains plans.id values
- Do NOT look for a "plan_type_id" column - it does not exist (Pitfall 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filterMenusByPlanType純粋関数の作成とユニットテスト</name>
  <files>src/lib/utils/menu-filter.ts, src/lib/utils/menu-filter.test.ts</files>
  <read_first>src/lib/utils/menu-filter.ts, src/lib/utils/menu-filter.test.ts, vitest.config.ts</read_first>
  <behavior>
    - allowed_plan_types=null のメニューは全プランの会員に表示される（後方互換）
    - allowed_plan_types=[1] のメニューは plan_id=1 の会員にのみ表示される
    - allowed_plan_types=[2] のメニューは plan_id=1 の会員には表示されない
    - allowed_plan_types=[1,2] のメニューは plan_id=1 でも plan_id=2 でも表示される
    - userPlanId=null（プランなし会員）の場合、allowed_plan_types=null のメニューのみ表示される
    - 空配列のメニューリストに対しては空配列が返る
  </behavior>
  <action>
1. **src/lib/utils/menu-filter.ts** を新規作成。純粋関数として抽出する（per D-04, RESEARCH.md推奨）:
   ```typescript
   /**
    * メニューをユーザーのプランタイプでフィルタする。
    * - allowed_plan_types === null -> 全プラン表示（後方互換）
    * - userPlanId が allowed_plan_types に含まれる -> 表示
    * - それ以外 -> 非表示
    */
   export function filterMenusByPlanType<
     T extends { allowed_plan_types: number[] | null }
   >(menus: T[], userPlanId: number | null): T[] {
     return menus.filter((menu) => {
       if (menu.allowed_plan_types === null) return true
       if (userPlanId === null) return false
       return menu.allowed_plan_types.includes(userPlanId)
     })
   }
   ```

2. **src/lib/utils/menu-filter.test.ts** を新規作成。上記のbehavior全パターンをテスト:
   - `describe("filterMenusByPlanType")` で全ケースをカバー
   - テストデータ: `{ id: 1, name: "A", allowed_plan_types: null }`, `{ id: 2, name: "B", allowed_plan_types: [1] }`, `{ id: 3, name: "C", allowed_plan_types: [2] }`, `{ id: 4, name: "D", allowed_plan_types: [1, 2] }`
   - テストケース: userPlanId=1, userPlanId=2, userPlanId=null, 空配列

3. RED: テストを先に書き、`npx vitest run src/lib/utils/menu-filter.test.ts` で失敗確認
4. GREEN: 実装を書き、テストが全パス確認
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npx vitest run src/lib/utils/menu-filter.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cat src/lib/utils/menu-filter.ts` で `export function filterMenusByPlanType` が存在する
    - `npx vitest run src/lib/utils/menu-filter.test.ts` で全テストがパスする
    - `grep -c "test\|it(" src/lib/utils/menu-filter.test.ts` で4件以上のテストケースがある
  </acceptance_criteria>
  <done>filterMenusByPlanType関数が全パターンのユニットテストに合格する</done>
</task>

<task type="auto">
  <name>Task 2: bookings/new/page.tsxのメニューフィルタをallowed_plan_typesベースに置き換え</name>
  <files>src/app/(member)/bookings/new/page.tsx</files>
  <read_first>src/app/(member)/bookings/new/page.tsx, src/lib/utils/menu-filter.ts, src/components/bookings/MenuSelect.tsx</read_first>
  <action>
1. `filterMenusByPlanType` をインポートする:
   ```typescript
   import { filterMenusByPlanType } from "@/lib/utils/menu-filter"
   ```

2. `fetchData` 関数内の `const supabase = createClient()` の直後（L49付近）で、ユーザーのplan_idを取得する（per D-02）:
   ```typescript
   // ユーザーのplan_idを取得
   const { data: { user } } = await supabase.auth.getUser()
   let userPlanId: number | null = null
   if (user) {
     const { data: memberPlan } = await supabase
       .from("member_plans")
       .select("plan_id")
       .eq("user_id", user.id)
       .eq("status", "active")
       .single()
     userPlanId = memberPlan?.plan_id ?? null
   }
   ```

3. メニュー取得クエリを変更（per D-03: `.eq("zoom_account", "B")` を撤去）:
   - `.eq("zoom_account", "B")` の行を削除する
   - `.select(...)` に `allowed_plan_types` を追加する:
   ```typescript
   const { data: menusData, error: menusError } = await supabase
     .from("meeting_menus")
     .select("id, name, description, duration_minutes, points_required, allowed_plan_types")
     .eq("is_active", true)
     .order("points_required", { ascending: true })
   ```

4. フィルタ適用（per D-04）。`setMenus(menusData || [])` の行を以下に変更:
   ```typescript
   const filteredMenus = filterMenusByPlanType(menusData ?? [], userPlanId)
   // MenuSelect の Menu 型に合わせて allowed_plan_types を除外して渡す
   setMenus(filteredMenus.map(({ allowed_plan_types, ...rest }) => rest))
   ```

5. **注意事項:**
   - `user` が null の場合でもクラッシュしない（userPlanId = null のまま filterMenusByPlanType に渡す。NULL のメニューのみ表示される）
   - MenuSelect コンポーネントの Menu 型には `allowed_plan_types` を追加しない（per Pitfall 5）。フィルタ後に除外する
   - ゲスト予約画面（/guest/booking）は変更しない（per D-09）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "zoom_account" src/app/\(member\)/bookings/new/page.tsx` でヒットしない（撤去済み）
    - `grep -n "filterMenusByPlanType" src/app/\(member\)/bookings/new/page.tsx` でインポートと使用箇所がヒットする
    - `grep -n "member_plans" src/app/\(member\)/bookings/new/page.tsx` でplan_id取得クエリがヒットする
    - `grep -n "allowed_plan_types" src/app/\(member\)/bookings/new/page.tsx` でselectとフィルタ除外がヒットする
    - `npm run build` がエラーなしで完了する
  </acceptance_criteria>
  <done>会員予約画面がzoom_accountハードコードではなくallowed_plan_typesベースでメニューをフィルタしている。ビルド成功。</done>
</task>

</tasks>

<verification>
- `npm run build` がエラーなしで完了すること
- `npx vitest run src/lib/utils/menu-filter.test.ts` で全テストパス
- `npx vitest run` で既存テスト含めて全パス
- `grep -rn "zoom_account.*B" src/app/\(member\)/bookings/new/page.tsx` でヒットしない（ハードコード撤去確認）
</verification>

<success_criteria>
- 会員予約画面でプランタイプに応じたメニューのみ表示される
- allowed_plan_types=NULLのメニューは全会員に表示される（後方互換）
- zoom_account="B"ハードコードが撤去されている
- フィルタロジックのユニットテストが全パスする
- 既存テスト・ビルドが壊れていない
</success_criteria>

<output>
After completion, create `.planning/phases/14-plan-type-menu/14-02-SUMMARY.md`
</output>
