---
phase: 16-member-activity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/admin/members.ts
  - src/__tests__/lib/actions/activity-status.test.ts
autonomous: true
requirements: [ACT-01, ACT-02]

must_haves:
  truths:
    - "getMembers() が返す各Memberに last_session_at, has_future_booking, activity_status フィールドが含まれる"
    - "30日以上未訪問かつ将来予約なしの会員は activity_status = 'yellow'"
    - "60日以上未訪問かつ将来予約なしの会員は activity_status = 'red'"
    - "セッション実績なしかつ将来予約なしの会員は activity_status = 'red'"
    - "将来のconfirmed予約がある会員は activity_status = 'normal'"
    - "getFollowUpMembers() がyellow/redの会員のみ返す"
  artifacts:
    - path: "src/lib/actions/admin/members.ts"
      provides: "ActivityStatus型, calcActivityStatus関数, 拡張Member型, getFollowUpMembers関数"
      exports: ["ActivityStatus", "calcActivityStatus", "getFollowUpMembers", "Member"]
    - path: "src/__tests__/lib/actions/activity-status.test.ts"
      provides: "calcActivityStatusのユニットテスト"
  key_links:
    - from: "src/lib/actions/admin/members.ts"
      to: "bookings table"
      via: "supabase .from('bookings') queries in getMembers()"
      pattern: "from\\(\"bookings\"\\)"
---

<objective>
Member型にアクティビティデータ（last_session_at, has_future_booking, activity_status）を追加し、getMembers()でbookingsテーブルから集計して返すようにする。calcActivityStatusの純粋関数をエクスポートし、ユニットテストを作成する。

Purpose: ACT-01/ACT-02のデータ基盤。UIレイヤー（Plan 02）が依存する型とデータを提供する。
Output: 拡張されたMember型、activity計算ロジック、getFollowUpMembers関数、ユニットテスト
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/lib/actions/admin/members.ts:
```typescript
export type Member = {
  id: string
  email: string
  full_name: string | null
  role: "guest" | "member" | "admin"
  created_at: string
  member_plan?: {
    id: number
    plan_id: number
    current_points: number
    status: "active" | "suspended" | "canceled"
    plan: { name: string }
  } | null
}

export async function getMembers(): Promise<Member[]>
```

From supabase schema (bookings table):
- `member_plan_id` INTEGER references member_plans(id)
- `status` TEXT ('confirmed', 'completed', 'canceled', etc.)
- `start_time` TIMESTAMPTZ
- `end_time` TIMESTAMPTZ
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Member型拡張 + getMembers()にbookings集計追加 + getFollowUpMembers()</name>
  <files>src/lib/actions/admin/members.ts</files>
  <read_first>
    - src/lib/actions/admin/members.ts
    - supabase/migrations/20260222000001_initial_schema.sql
    - .planning/phases/16-member-activity/16-RESEARCH.md
  </read_first>
  <behavior>
    - calcActivityStatus(null, false) === 'red' (セッション実績なし、将来予約なし)
    - calcActivityStatus(null, true) === 'normal' (セッション実績なし、将来予約あり)
    - calcActivityStatus('2026-02-01T00:00:00Z', false) at 2026-03-29 === 'yellow' (56日前、30-60日)
    - calcActivityStatus('2026-01-01T00:00:00Z', false) at 2026-03-29 === 'red' (87日前、60日超)
    - calcActivityStatus('2026-03-20T00:00:00Z', false) at 2026-03-29 === 'normal' (9日前、30日未満)
    - calcActivityStatus('2026-03-20T00:00:00Z', true) === 'normal' (将来予約あり)
  </behavior>
  <action>
    src/lib/actions/admin/members.tsを以下のように拡張する:

    1. ActivityStatus型を追加しエクスポートする (per D-01):
       ```typescript
       export type ActivityStatus = 'normal' | 'yellow' | 'red'
       ```

    2. calcActivityStatus純粋関数を追加しエクスポートする (per D-05, D-06, D-07):
       ```typescript
       export function calcActivityStatus(
         lastSessionAt: string | null,
         hasFutureBooking: boolean
       ): ActivityStatus {
         if (hasFutureBooking) return 'normal'
         if (lastSessionAt === null) return 'red'
         const daysAgo = Math.floor(
           (Date.now() - new Date(lastSessionAt).getTime()) / (1000 * 60 * 60 * 24)
         )
         if (daysAgo >= 60) return 'red'
         if (daysAgo >= 30) return 'yellow'
         return 'normal'
       }
       ```

    3. Member型に3フィールド追加:
       ```typescript
       last_session_at: string | null
       has_future_booking: boolean
       activity_status: ActivityStatus
       ```

    4. getMembers()内で既存のprofilesクエリの後に、bookings集計を追加する:
       - member_plan_idsを抽出
       - bookingsテーブルから status='completed' の最新end_timeを取得 (per D-05)
       - bookingsテーブルから status='confirmed' AND start_time > now() を取得 (per D-06)
       - JS側でMapとSetに集計し、各memberにactivity_statusを計算して付与
       - member_planがない or member_plan.status !== 'active' の場合は activity_status='normal' (per D-07: active会員のみ対象)

    5. getFollowUpMembers()を追加しエクスポートする:
       ```typescript
       export async function getFollowUpMembers(): Promise<Member[]> {
         await requireAdmin()
         const all = await getMembers()
         return all.filter(m =>
           m.member_plan?.status === 'active' &&
           (m.activity_status === 'yellow' || m.activity_status === 'red')
         )
       }
       ```

    RESEARCH.mdのPattern 2（2段階クエリ + JS集計）に従う。Supabase JSの3段ネストJOINは避ける（Anti-Pattern）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export type ActivityStatus" src/lib/actions/admin/members.ts
    - grep -q "export function calcActivityStatus" src/lib/actions/admin/members.ts
    - grep -q "last_session_at" src/lib/actions/admin/members.ts
    - grep -q "has_future_booking" src/lib/actions/admin/members.ts
    - grep -q "activity_status" src/lib/actions/admin/members.ts
    - grep -q "export async function getFollowUpMembers" src/lib/actions/admin/members.ts
    - grep -q "from(\"bookings\")" src/lib/actions/admin/members.ts
  </acceptance_criteria>
  <done>
    Member型にlast_session_at, has_future_booking, activity_statusが追加されている。getMembers()がbookingsテーブルから集計してactivity_statusを計算している。calcActivityStatusが純粋関数としてエクスポートされている。getFollowUpMembers()がyellow/redの会員のみ返す。TypeScriptコンパイルが通る。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calcActivityStatus ユニットテスト</name>
  <files>src/__tests__/lib/actions/activity-status.test.ts</files>
  <read_first>
    - src/lib/actions/admin/members.ts
    - vitest.config.ts
  </read_first>
  <behavior>
    - セッション実績なし + 将来予約なし -> 'red'
    - セッション実績なし + 将来予約あり -> 'normal'
    - 60日以上前 + 将来予約なし -> 'red'
    - 30日以上60日未満 + 将来予約なし -> 'yellow'
    - 30日未満 + 将来予約なし -> 'normal'
    - 60日以上前 + 将来予約あり -> 'normal'
    - ちょうど30日前 + 将来予約なし -> 'yellow'
    - ちょうど60日前 + 将来予約なし -> 'red'
  </behavior>
  <action>
    src/__tests__/lib/actions/activity-status.test.ts を新規作成する。

    calcActivityStatusをimportしてテストする:
    ```typescript
    import { describe, it, expect, vi, afterEach } from 'vitest'
    import { calcActivityStatus } from '@/lib/actions/admin/members'
    ```

    Date.now()をvitest.spyOn()でモックし、基準日を固定する（例: 2026-03-29T00:00:00Z = 1774396800000）。
    各テストケースで適切なlastSessionAtのISO文字列を渡す。

    テストケース:
    1. null + false -> 'red'
    2. null + true -> 'normal'
    3. 87日前 (2026-01-01) + false -> 'red'
    4. 56日前 (2026-02-01) + false -> 'yellow'
    5. 9日前 (2026-03-20) + false -> 'normal'
    6. 87日前 + true -> 'normal'
    7. ちょうど30日前 + false -> 'yellow' (境界値)
    8. ちょうど60日前 + false -> 'red' (境界値)
    9. 29日前 + false -> 'normal' (境界値)
    10. 59日前 + false -> 'yellow' (境界値)
  </action>
  <verify>
    <automated>npm test -- src/__tests__/lib/actions/activity-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/__tests__/lib/actions/activity-status.test.ts
    - grep -q "calcActivityStatus" src/__tests__/lib/actions/activity-status.test.ts
    - grep -q "describe" src/__tests__/lib/actions/activity-status.test.ts
    - npm test -- src/__tests__/lib/actions/activity-status.test.ts が全テストパスする
  </acceptance_criteria>
  <done>
    calcActivityStatusの全境界値テスト（10ケース）がパスしている。
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` が成功する
- `npm test -- src/__tests__/lib/actions/activity-status.test.ts` が全テストパスする
- Member型にlast_session_at, has_future_booking, activity_statusが含まれている
</verification>

<success_criteria>
- calcActivityStatusが正しくactivity_statusを計算する（30日=yellow, 60日=red, 将来予約あり=normal, null=red）
- getMembers()がbookingsテーブルから集計してactivity fieldsを返す
- getFollowUpMembers()がyellow/red会員のみ返す
- 全ユニットテストがパスする
</success_criteria>

<output>
After completion, create `.planning/phases/16-member-activity/16-01-SUMMARY.md`
</output>
