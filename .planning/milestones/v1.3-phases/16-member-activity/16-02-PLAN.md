---
phase: 16-member-activity
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/components/ui/data-table.tsx
  - src/app/admin/members/columns.tsx
  - src/app/admin/members/members-client.tsx
  - src/app/admin/dashboard/page.tsx
  - src/app/admin/dashboard/follow-up-list.tsx
autonomous: false
requirements: [ACT-01, ACT-02, ACT-03]

must_haves:
  truths:
    - "会員一覧で30日以上未訪問の会員行が黄色背景で表示される"
    - "会員一覧で60日以上未訪問の会員行が赤色背景で表示される"
    - "会員一覧に「前回セッション」カラムが表示され、XX日前の形式で表示される"
    - "セッション実績なしの会員は「未訪問」と表示される"
    - "ダッシュボード下部に60日以上未訪問セクション（赤）と30-60日未訪問セクション（黄）が表示される"
  artifacts:
    - path: "src/components/ui/data-table.tsx"
      provides: "getRowClassName propsによる行背景色制御"
      contains: "getRowClassName"
    - path: "src/app/admin/members/columns.tsx"
      provides: "前回セッションカラム"
      contains: "last_session_at"
    - path: "src/app/admin/dashboard/follow-up-list.tsx"
      provides: "フォロー必要会員リストServer Component"
  key_links:
    - from: "src/app/admin/members/members-client.tsx"
      to: "src/components/ui/data-table.tsx"
      via: "getRowClassName prop"
      pattern: "getRowClassName"
    - from: "src/app/admin/dashboard/follow-up-list.tsx"
      to: "src/lib/actions/admin/members.ts"
      via: "getFollowUpMembers() import"
      pattern: "getFollowUpMembers"
---

<objective>
会員一覧テーブルにactivity_statusに応じた行背景色と「前回セッション」カラムを追加する。管理ダッシュボードにフォロー必要会員リストを表示する。

Purpose: ACT-01/ACT-02（会員一覧の色分け）とACT-03（ダッシュボードのフォローリスト）をUIとして実装する。
Output: 色分けされた会員一覧 + ダッシュボードのフォローリストコンポーネント
</objective>

<execution_context>
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/かずみん/Time-with-Kazumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-member-activity/16-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan depends on -->

From src/lib/actions/admin/members.ts (after Plan 01):
```typescript
export type ActivityStatus = 'normal' | 'yellow' | 'red'

export type Member = {
  id: string
  email: string
  full_name: string | null
  role: "guest" | "member" | "admin"
  created_at: string
  member_plan?: {
    id: number
    plan_id: number
    current_points: number
    status: "active" | "suspended" | "canceled"
    plan: { name: string }
  } | null
  last_session_at: string | null
  has_future_booking: boolean
  activity_status: ActivityStatus
}

export async function getMembers(): Promise<Member[]>
export async function getFollowUpMembers(): Promise<Member[]>
export function calcActivityStatus(lastSessionAt: string | null, hasFutureBooking: boolean): ActivityStatus
```

From src/components/ui/data-table.tsx (current):
```typescript
interface DataTableProps<TData, TValue> {
  columns: ColumnDef<TData, TValue>[]
  data: TData[]
  pageSize?: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataTable getRowClassName対応 + columns.tsx前回セッションカラム + members-client行スタイル</name>
  <files>
    src/components/ui/data-table.tsx
    src/app/admin/members/columns.tsx
    src/app/admin/members/members-client.tsx
  </files>
  <read_first>
    - src/components/ui/data-table.tsx
    - src/app/admin/members/columns.tsx
    - src/app/admin/members/members-client.tsx
    - src/lib/actions/admin/members.ts
    - .planning/phases/16-member-activity/16-RESEARCH.md
  </read_first>
  <action>
    3つのファイルを変更する:

    **1. src/components/ui/data-table.tsx** (per D-01)

    DataTablePropsに `getRowClassName` propsを追加する:
    ```typescript
    import { Row } from "@tanstack/react-table"  // Row型をimportに追加

    interface DataTableProps<TData, TValue> {
      columns: ColumnDef<TData, TValue>[]
      data: TData[]
      pageSize?: number
      getRowClassName?: (row: Row<TData>) => string  // 追加
    }
    ```

    関数の引数に `getRowClassName` を追加:
    ```typescript
    export function DataTable<TData, TValue>({
      columns,
      data,
      pageSize = 10,
      getRowClassName,
    }: DataTableProps<TData, TValue>) {
    ```

    TableRowのclassNameに適用:
    ```tsx
    <TableRow
      key={row.id}
      data-state={row.getIsSelected() && "selected"}
      className={getRowClassName?.(row)}
    >
    ```

    **2. src/app/admin/members/columns.tsx** (per D-02)

    「前回セッション」カラムを、ステータスカラムの前（actionsカラムの前）に追加する:
    ```typescript
    {
      accessorKey: "last_session_at",
      header: ({ column }) => (
        <Button
          variant="ghost"
          onClick={() => column.toggleSorting(column.getIsSorted() === "asc")}
        >
          前回セッション
          <ArrowUpDown className="ml-2 h-4 w-4" />
        </Button>
      ),
      cell: ({ row }) => {
        const lastSessionAt = row.original.last_session_at
        if (!lastSessionAt) return <span className="text-muted-foreground text-sm">未訪問</span>
        const daysAgo = Math.floor(
          (Date.now() - new Date(lastSessionAt).getTime()) / (1000 * 60 * 60 * 24)
        )
        if (daysAgo === 0) return "今日"
        return `${daysAgo}日前`
      },
      sortingFn: (rowA, rowB) => {
        const a = rowA.original.last_session_at ?? ""
        const b = rowB.original.last_session_at ?? ""
        return a < b ? -1 : a > b ? 1 : 0
      },
    },
    ```

    **3. src/app/admin/members/members-client.tsx** (per D-01)

    DataTableにgetRowClassNameを渡す:
    ```typescript
    import type { Row } from "@tanstack/react-table"
    import type { Member } from "@/lib/actions/admin/members"

    function getMemberRowClassName(row: Row<Member>): string {
      const status = row.original.activity_status
      if (status === 'red') return 'bg-red-50 hover:bg-red-100'
      if (status === 'yellow') return 'bg-yellow-50 hover:bg-yellow-100'
      return ''
    }
    ```

    DataTable呼び出しを更新:
    ```tsx
    <DataTable
      columns={columns}
      data={members}
      getRowClassName={getMemberRowClassName}
    />
    ```

    色コード: bg-yellow-50/hover:bg-yellow-100 (30日), bg-red-50/hover:bg-red-100 (60日) -- shadcn/uiのneutralテーマと調和する淡い色。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getRowClassName" src/components/ui/data-table.tsx
    - grep -q "getRowClassName" src/app/admin/members/members-client.tsx
    - grep -q "last_session_at" src/app/admin/members/columns.tsx
    - grep -q "bg-red-50" src/app/admin/members/members-client.tsx
    - grep -q "bg-yellow-50" src/app/admin/members/members-client.tsx
    - grep -q "未訪問" src/app/admin/members/columns.tsx
  </acceptance_criteria>
  <done>
    DataTableがgetRowClassNameを受け取り行背景色を制御できる。会員一覧に「前回セッション」カラムが表示される。30日以上未訪問行が黄色、60日以上が赤の背景色になる。TypeScriptコンパイルが通る。
  </done>
</task>

<task type="auto">
  <name>Task 2: ダッシュボード FollowUpList Server Component</name>
  <files>
    src/app/admin/dashboard/follow-up-list.tsx
    src/app/admin/dashboard/page.tsx
  </files>
  <read_first>
    - src/app/admin/dashboard/page.tsx
    - src/lib/actions/admin/members.ts
    - .planning/phases/16-member-activity/16-RESEARCH.md
  </read_first>
  <action>
    2つのファイルを変更/作成する:

    **1. src/app/admin/dashboard/follow-up-list.tsx** (新規作成, per D-03, D-04)

    async Server Componentとして作成する。getFollowUpMembers()を呼び出し、赤セクション（60日超）と黄セクション（30-60日）に分けて表示する。

    ```typescript
    import { getFollowUpMembers } from "@/lib/actions/admin/members"
    import type { Member } from "@/lib/actions/admin/members"
    import {
      Table,
      TableBody,
      TableCell,
      TableHead,
      TableHeader,
      TableRow,
    } from "@/components/ui/table"

    function formatLastSession(lastSessionAt: string | null): string {
      if (!lastSessionAt) return "未訪問"
      const daysAgo = Math.floor(
        (Date.now() - new Date(lastSessionAt).getTime()) / (1000 * 60 * 60 * 24)
      )
      if (daysAgo === 0) return "今日"
      return `${daysAgo}日前`
    }

    function FollowUpSection({
      title,
      members,
      bgClass,
      titleClass,
    }: {
      title: string
      members: Member[]
      bgClass: string
      titleClass: string
    }) {
      if (members.length === 0) return null
      return (
        <div className="space-y-2">
          <h3 className={`font-semibold ${titleClass}`}>{title} ({members.length}名)</h3>
          <div className={`rounded-md border ${bgClass}`}>
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>名前</TableHead>
                  <TableHead>プラン</TableHead>
                  <TableHead>前回セッション</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {members.map((m) => (
                  <TableRow key={m.id}>
                    <TableCell>{m.full_name ?? m.email}</TableCell>
                    <TableCell>{m.member_plan?.plan.name ?? "-"}</TableCell>
                    <TableCell>{formatLastSession(m.last_session_at)}</TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          </div>
        </div>
      )
    }

    export async function FollowUpList() {
      const members = await getFollowUpMembers()

      const redMembers = members.filter(m => m.activity_status === 'red')
      const yellowMembers = members.filter(m => m.activity_status === 'yellow')

      if (redMembers.length === 0 && yellowMembers.length === 0) {
        return (
          <div className="text-muted-foreground text-sm">
            フォローが必要な会員はいません。
          </div>
        )
      }

      return (
        <div className="space-y-4">
          <FollowUpSection
            title="60日以上未訪問"
            members={redMembers}
            bgClass="bg-red-50"
            titleClass="text-red-700"
          />
          <FollowUpSection
            title="30〜60日未訪問"
            members={yellowMembers}
            bgClass="bg-yellow-50"
            titleClass="text-yellow-700"
          />
        </div>
      )
    }
    ```

    全件表示とする（会員数が少ないサロン運営のため、ページングは不要 -- YAGNI）。

    **2. src/app/admin/dashboard/page.tsx** (per D-03)

    FollowUpListをインポートしてダッシュボード下部に配置する。dashboard/page.tsx自体はasyncにせず、FollowUpListがasync Server Componentとして独立する（RESEARCH.md Pitfall 5の対策）。

    Suspenseで囲んでローディング状態を表示する:
    ```typescript
    import { Suspense } from "react"
    import { FollowUpList } from "./follow-up-list"
    ```

    既存のgridの後に追加:
    ```tsx
    {/* Follow-up members section */}
    <div className="space-y-2">
      <h2 className="text-xl font-semibold">フォローが必要な会員</h2>
      <Suspense fallback={<div className="text-muted-foreground text-sm">読み込み中...</div>}>
        <FollowUpList />
      </Suspense>
    </div>
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/admin/dashboard/follow-up-list.tsx
    - grep -q "getFollowUpMembers" src/app/admin/dashboard/follow-up-list.tsx
    - grep -q "60日以上未訪問" src/app/admin/dashboard/follow-up-list.tsx
    - grep -q "30〜60日未訪問" src/app/admin/dashboard/follow-up-list.tsx
    - grep -q "FollowUpList" src/app/admin/dashboard/page.tsx
    - grep -q "Suspense" src/app/admin/dashboard/page.tsx
    - grep -q "bg-red-50" src/app/admin/dashboard/follow-up-list.tsx
    - grep -q "bg-yellow-50" src/app/admin/dashboard/follow-up-list.tsx
  </acceptance_criteria>
  <done>
    ダッシュボード下部にフォロー必要会員リストが赤セクション（60日超）を上、黄セクション（30-60日）を下に表示される。0件の場合は「フォローが必要な会員はいません」メッセージが表示される。TypeScriptコンパイルが通る。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UI目視確認 - 会員一覧色分け + ダッシュボードフォローリスト</name>
  <action>
    自動実装済みのUIを目視確認する。npm run devでローカルサーバーを起動し、管理者としてログイン後、/admin/membersと/admin/dashboardを確認する。
  </action>
  <what-built>
    会員一覧の行背景色（黄色/赤）と「前回セッション」カラム、ダッシュボードのフォロー必要会員リスト
  </what-built>
  <how-to-verify>
    1. `npm run dev` でローカルサーバーを起動
    2. 管理者としてログイン
    3. /admin/members を開き、以下を確認:
       - 「前回セッション」カラムが表示されている
       - 30日以上未訪問の会員行が淡い黄色背景で表示されている（該当者がいる場合）
       - 60日以上未訪問の会員行が淡い赤色背景で表示されている（該当者がいる場合）
       - 「前回セッション」カラムのヘッダーをクリックしてソートが動作する
    4. /admin/dashboard を開き、以下を確認:
       - ページ下部に「フォローが必要な会員」セクションが表示されている
       - 赤セクション（60日以上）が上、黄セクション（30-60日）が下に表示されている
       - 各会員の名前・プラン・前回セッション日時が表示されている
  </how-to-verify>
  <verify>目視確認</verify>
  <done>管理者が会員一覧とダッシュボードでフォロー対象会員を色分けで確認できる</done>
  <resume-signal>"approved" と入力するか、問題点を記述してください</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` が成功する
- `npm test` が全テストパスする
- /admin/members で行背景色と前回セッションカラムが表示される
- /admin/dashboard でフォローリストが表示される
</verification>

<success_criteria>
- 30日以上未訪問会員が黄色行で表示される (ACT-01)
- 60日以上未訪問会員が赤色行で表示される (ACT-02)
- ダッシュボード下部にフォローリストが赤/黄セクション別で表示される (ACT-03)
- 「前回セッション」カラムにXX日前形式で表示される
- TypeScriptコンパイルが通る
</success_criteria>

<output>
After completion, create `.planning/phases/16-member-activity/16-02-SUMMARY.md`
</output>
