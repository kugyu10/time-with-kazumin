---
phase: 12-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260327000001_add_allowed_plan_types.sql
  - supabase/migrations/20260327000002_seed_money_block_plan.sql
  - src/types/database.ts
autonomous: true
requirements: [MENU-01, MENU-03, MENU-05]
must_haves:
  truths:
    - "meeting_menus テーブルに allowed_plan_types INTEGER[] DEFAULT NULL カラムが存在する"
    - "allowed_plan_types カラムに GIN パーシャルインデックスが適用されている"
    - "plans テーブルに「お金のブロック解消プラン」が登録されている（monthly_points=120, max_points=240, price_monthly=50000）"
    - "meeting_menus テーブルに「60分お金のブロック解消セッション」が登録されている（duration_minutes=60, points_required=60, allowed_plan_types に新プランIDが設定）"
    - "既存メニューの allowed_plan_types は全て NULL（後方互換: 全プラン表示）"
    - "TypeScript 型定義に allowed_plan_types フィールドが反映されている"
  artifacts:
    - path: "supabase/migrations/20260327000001_add_allowed_plan_types.sql"
      provides: "allowed_plan_types カラム追加 + GINインデックス"
      contains: "ALTER TABLE meeting_menus"
    - path: "supabase/migrations/20260327000002_seed_money_block_plan.sql"
      provides: "新プラン + 新メニューの seed データ"
      contains: "INSERT INTO plans"
    - path: "src/types/database.ts"
      provides: "Supabase 自動生成型定義（allowed_plan_types 含む）"
      contains: "allowed_plan_types"
  key_links:
    - from: "supabase/migrations/20260327000002_seed_money_block_plan.sql"
      to: "plans テーブル → meeting_menus テーブル"
      via: "CTE (WITH new_plan AS INSERT ... RETURNING id)"
      pattern: "WITH new_plan AS"
---

<objective>
Phase 14（プランタイプ別メニュー表示）の基盤となるDBマイグレーションを適用する。

Purpose: meeting_menus テーブルにプランタイプ制御用カラムを追加し、新プラン・新メニューのseedデータを投入することで、Phase 14でのアプリ層実装を可能にする。
Output: 2つのマイグレーションファイル + 更新された TypeScript 型定義
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-db/12-CONTEXT.md
@.planning/phases/12-db/12-RESEARCH.md

<interfaces>
<!-- 既存テーブル定義（plans, meeting_menus）- executor はこれを理解した上でマイグレーションを書く -->

From supabase/migrations/20260222000001_initial_schema.sql:
```sql
CREATE TABLE plans (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    monthly_points INTEGER NOT NULL,
    max_points INTEGER,
    price_monthly DECIMAL(10,2),
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE meeting_menus (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    duration_minutes INTEGER NOT NULL,
    points_required INTEGER NOT NULL,
    zoom_account TEXT NOT NULL CHECK (zoom_account IN ('A', 'B')),
    description TEXT,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

From supabase/migrations/20260222200002_meeting_menus_zoom_account.sql (カラム追加パターン参考):
```sql
ALTER TABLE meeting_menus
ADD COLUMN IF NOT EXISTS zoom_account CHAR(1) DEFAULT 'A' CHECK (zoom_account IN ('A', 'B'));
COMMENT ON COLUMN meeting_menus.zoom_account IS 'Zoom account to use for meetings created from this menu (A or B)';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: allowed_plan_types カラム追加マイグレーションとseedデータ投入</name>
  <files>
    supabase/migrations/20260327000001_add_allowed_plan_types.sql
    supabase/migrations/20260327000002_seed_money_block_plan.sql
  </files>
  <read_first>
    supabase/migrations/20260222000001_initial_schema.sql
    supabase/migrations/20260222200002_meeting_menus_zoom_account.sql
    .planning/phases/12-db/12-RESEARCH.md
  </read_first>
  <action>
2つのマイグレーションファイルを作成する。

**ファイル1: `supabase/migrations/20260327000001_add_allowed_plan_types.sql`**

```sql
-- Migration: Add allowed_plan_types column to meeting_menus
-- Created: 2026-03-27
-- Purpose: プランタイプ別メニュー表示（Phase 14）の基盤カラム

ALTER TABLE meeting_menus
ADD COLUMN IF NOT EXISTS allowed_plan_types INTEGER[] DEFAULT NULL;

COMMENT ON COLUMN meeting_menus.allowed_plan_types IS 'NULL = 全プランに表示（後方互換）。ARRAY[plan_id, ...] で対象プランを限定する。';

CREATE INDEX IF NOT EXISTS idx_meeting_menus_allowed_plan_types
ON meeting_menus USING gin(allowed_plan_types)
WHERE allowed_plan_types IS NOT NULL;
```

**ファイル2: `supabase/migrations/20260327000002_seed_money_block_plan.sql`**

CTE を使って plans INSERT の id を取得し、meeting_menus の allowed_plan_types に設定する。

```sql
-- Migration: Seed money block resolution plan and menu
-- Created: 2026-03-27
-- Purpose: お金のブロック解消プランと専用メニューの初期データ投入

WITH new_plan AS (
  INSERT INTO plans (name, monthly_points, max_points, price_monthly, is_active)
  VALUES ('お金のブロック解消プラン', 120, 240, 50000, true)
  RETURNING id
)
INSERT INTO meeting_menus (name, duration_minutes, points_required, zoom_account, description, is_active, allowed_plan_types)
SELECT
  '60分お金のブロック解消セッション',
  60,
  60,
  'A',
  'お金のブロック解消プラン会員向け専用60分セッション',
  true,
  ARRAY[new_plan.id]
FROM new_plan;
```

注意: zoom_account は暫定 'A' を使用（CONTEXT.md D-04 で要確認と記載あり。後で変更可能）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && cat supabase/migrations/20260327000001_add_allowed_plan_types.sql && echo "---" && cat supabase/migrations/20260327000002_seed_money_block_plan.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260327000001_add_allowed_plan_types.sql が存在する
    - 20260327000001 に `ALTER TABLE meeting_menus` と `ADD COLUMN IF NOT EXISTS allowed_plan_types INTEGER[] DEFAULT NULL` が含まれる
    - 20260327000001 に `CREATE INDEX IF NOT EXISTS idx_meeting_menus_allowed_plan_types` と `USING gin(allowed_plan_types)` と `WHERE allowed_plan_types IS NOT NULL` が含まれる
    - 20260327000001 に `COMMENT ON COLUMN meeting_menus.allowed_plan_types` が含まれる
    - supabase/migrations/20260327000002_seed_money_block_plan.sql が存在する
    - 20260327000002 に `WITH new_plan AS` が含まれる
    - 20260327000002 に `INSERT INTO plans (name, monthly_points, max_points, price_monthly, is_active)` と `'お金のブロック解消プラン', 120, 240, 50000, true` が含まれる
    - 20260327000002 に `INSERT INTO meeting_menus` と `'60分お金のブロック解消セッション'` と `60, 60, 'A'` と `ARRAY[new_plan.id]` が含まれる
  </acceptance_criteria>
  <done>2つのマイグレーションファイルが存在し、allowed_plan_types カラム追加 + GINインデックス + seedデータ（新プラン・新メニュー）が正しいSQL構文で記述されている</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript 型定義の再生成と既存テスト通過確認</name>
  <files>
    src/types/database.ts
  </files>
  <read_first>
    src/types/database.ts
  </read_first>
  <action>
Supabase ローカル環境にマイグレーションを適用し、TypeScript型定義を再生成する。

**手順:**

1. `supabase db reset` でローカルDBをリセットし、全マイグレーション（新規2ファイル含む）を適用する
2. `supabase gen types typescript --local > src/types/database.ts` で型定義を再生成する
3. `npm run build` でTypeScriptコンパイルが通ることを確認する
4. `npm run test` で既存テストが壊れていないことを確認する

**期待する型定義の変化:**
- `meeting_menus` の `Row` 型に `allowed_plan_types: number[] | null` が追加される
- `meeting_menus` の `Insert` 型に `allowed_plan_types?: number[] | null` が追加される
- `meeting_menus` の `Update` 型に `allowed_plan_types?: number[] | null` が追加される

**注意:** `supabase gen types` は自動生成なので手動編集は行わない。生成結果が期待と異なる場合はマイグレーションSQLを見直す。

**supabase db reset が失敗する場合:** `supabase start` が未実行の可能性がある。`supabase start` を先に実行する。それでも失敗する場合は `supabase stop && supabase start` でリセットしてから再試行する。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/かずみん/Time-with-Kazumin && grep -c "allowed_plan_types" src/types/database.ts && npm run build 2>&1 | tail -5 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/types/database.ts に `allowed_plan_types` が3箇所以上含まれる（Row, Insert, Update の各型）
    - `npm run build` がエラーなく完了する（exit code 0）
    - `npm run test` が既存テスト全てパスする（exit code 0）
  </acceptance_criteria>
  <done>TypeScript 型定義が allowed_plan_types を含んで再生成され、ビルドと既存テストが通る状態</done>
</task>

</tasks>

<verification>
1. マイグレーションファイルが2つ存在すること
2. `allowed_plan_types INTEGER[] DEFAULT NULL` カラムが定義されていること
3. GINパーシャルインデックスが定義されていること
4. 新プラン（お金のブロック解消プラン）のINSERT文が正しい値で記述されていること
5. 新メニュー（60分お金のブロック解消セッション）のINSERT文がCTE経由で allowed_plan_types を設定していること
6. `src/types/database.ts` に `allowed_plan_types` が反映されていること
7. `npm run build` と `npm run test` がパスすること
</verification>

<success_criteria>
- 2つのマイグレーションファイルが `supabase/migrations/` に存在する
- ローカル Supabase DB にマイグレーションが適用されている
- `src/types/database.ts` が再生成され allowed_plan_types を含む
- TypeScript ビルドがエラーなく完了する
- 既存テストが全てパスする
</success_criteria>

<output>
After completion, create `.planning/phases/12-db/12-01-SUMMARY.md`
</output>
