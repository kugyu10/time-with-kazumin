---
phase: 08-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/lib/integrations/email.test.ts
  - src/emails/WelcomeEmail.tsx
  - src/lib/integrations/email.ts
  - src/lib/actions/admin/members.ts
  - src/app/(member)/bookings/[id]/page.tsx
  - src/components/bookings/BookingCard.tsx
  - src/app/admin/bookings/columns.tsx
  - src/app/(public)/guest/booking/success/page.tsx
  - src/app/(public)/guest/cancel/[token]/page.tsx
  - docs/rules.md
autonomous: false
requirements:
  - BUG-02
  - BUG-03
  - BUG-04

must_haves:
  truths:
    - "/booking/[id] の予約詳細画面で時刻がJST（例: 14:00）で表示される"
    - "全画面（ゲスト/会員/管理者）の時刻表示がJSTで統一されており、UTC表示が一切ない"
    - "docs/rules.md にUTC/JST変換のコーディング規約が記載されている"
    - "会員を招待して承認すると、その会員にウェルカムメールが届く"
  artifacts:
    - path: "src/emails/WelcomeEmail.tsx"
      provides: "ウェルカムメールのReact Emailテンプレート"
      contains: "WelcomeEmail"
    - path: "src/lib/integrations/email.ts"
      provides: "sendWelcomeEmail関数"
      contains: "sendWelcomeEmail"
    - path: "src/lib/actions/admin/members.ts"
      provides: "createMember()でウェルカムメール送信"
      contains: "sendWelcomeEmail"
    - path: "docs/rules.md"
      provides: "UTC/JST変換コーディング規約"
      contains: "Asia/Tokyo"
  key_links:
    - from: "src/lib/actions/admin/members.ts"
      to: "src/lib/integrations/email.ts"
      via: "sendWelcomeEmail()"
      pattern: "sendWelcomeEmail"
    - from: "src/app/(member)/bookings/[id]/page.tsx"
      to: "JST表示"
      via: "toLocaleDateString with timeZone: Asia/Tokyo"
      pattern: "timeZone.*Asia/Tokyo"
---

<objective>
BUG-02（予約詳細画面のUTC表示）、BUG-03（全画面JST統一）、BUG-04（ウェルカムメール未送信）を修正する。

Purpose: Vercel（UTC環境）でも全画面で正しくJST時刻を表示し、新規会員へのウェルカムメールを実装する。
Output: JST修正（5ファイル）、docs/rules.md新規作成、WelcomeEmailテンプレート、sendWelcomeEmail関数、createMember修正
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes/08-RESEARCH.md
@.planning/phases/08-bug-fixes/08-VALIDATION.md

<interfaces>
<!-- 既存コードから抽出した重要インターフェース -->

From src/emails/BookingConfirmation.tsx（JSTの正しい参照パターン）:
```typescript
// 正しいJST変換パターン（このファイルをテンプレートとして使う）
function formatDateTime(isoString: string): string {
  const date = new Date(isoString)
  return date.toLocaleDateString("ja-JP", {
    year: "numeric",
    month: "long",
    day: "numeric",
    weekday: "short",
    hour: "2-digit",
    minute: "2-digit",
    timeZone: "Asia/Tokyo",  // これが必須
  })
}
```

From src/lib/integrations/email.ts（既存のメール送信パターン）:
```typescript
export interface SendEmailResult {
  success: boolean
  error?: string
}
export async function sendBookingConfirmationEmail(params: SendBookingConfirmationParams): Promise<SendEmailResult>
export async function sendBookingCancellationEmail(params: SendBookingCancellationParams): Promise<SendEmailResult>
// sendWelcomeEmail を同じパターンで追加する
```

From src/lib/actions/admin/members.ts（BUG-04の修正対象）:
```typescript
// L239付近 - generateLinkの呼び出し
const { data: linkData } = await supabase.auth.admin.generateLink({
  type: "recovery",
  email: validated.email,
})
// linkData?.properties?.action_link がリセットURL
// この直後にsendWelcomeEmail()を追加する（非ブロッキング）
```

JST修正対象ファイル（5箇所）:
- src/app/(member)/bookings/[id]/page.tsx — formatDateTimeFull()
- src/components/bookings/BookingCard.tsx — formatDateTime()
- src/app/admin/bookings/columns.tsx — date-fnsのformat()をtoLocaleStringに変更
- src/app/(public)/guest/booking/success/page.tsx — toLocaleDateString/toLocaleTimeString
- src/app/(public)/guest/cancel/[token]/page.tsx — toLocaleDateString/toLocaleTimeString
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sendWelcomeEmailテストケース追加（Wave 0 - テスト先行）</name>
  <files>src/__tests__/lib/integrations/email.test.ts</files>
  <behavior>
    - Test 1: sendWelcomeEmail({ userEmail, userName, passwordResetUrl }) がResend APIを呼び出す
    - Test 2: Resend API成功時に { success: true } を返す
    - Test 3: RESEND_API_KEY未設定時に { success: false } を返す（isEmailConfigured()チェック）
    - Test 4: passwordResetUrl が null でもメール送信が成功する（フォールバック対応）
  </behavior>
  <action>
    既存の `src/__tests__/lib/integrations/email.test.ts` に `sendWelcomeEmail` の describe ブロックを追加する。

    既存テスト（sendBookingConfirmationEmail）と同じパターン（vi.resetModules、mockFetch）を使うこと。

    テストが RED になることを確認してから Task 2 の実装に進む。
  </action>
  <verify>
    <automated>cd "/Users/kugyu10/work/かずみん/Time-with-Kazumin" && npx vitest run src/__tests__/lib/integrations/email.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>新規テストケース4件追加。既存テストは全パス。新規テストはredも可（実装前）。</done>
</task>

<task type="auto">
  <name>Task 2: BUG-04 WelcomeEmailテンプレートとsendWelcomeEmail実装</name>
  <files>src/emails/WelcomeEmail.tsx, src/lib/integrations/email.ts, src/lib/actions/admin/members.ts</files>
  <action>
    **手順1: src/emails/WelcomeEmail.tsx を新規作成**

    `BookingConfirmation.tsx` の構造を参考に以下を実装する：
    ```typescript
    export interface WelcomeEmailProps {
      userName: string
      passwordResetUrl: string | null
    }

    export function WelcomeEmail({ userName, passwordResetUrl }: WelcomeEmailProps) {
      // React Emailコンポーネントを使用
      // パスワードリセットURLがある場合はボタンを表示
      // URLがない場合は「管理者にお問い合わせください」のフォールバックテキストを表示
      // メール本文に「パスワード設定リンクの有効期限: 1時間」を明記する
    }
    ```
    Layout コンポーネントは `./components/Layout` を使用（既存パターン）。

    **手順2: src/lib/integrations/email.ts に sendWelcomeEmail を追加**

    既存の `sendBookingConfirmationEmail` パターンと同じ構造で追加：
    ```typescript
    export interface SendWelcomeEmailParams {
      userEmail: string
      userName: string
      passwordResetUrl: string | null
    }

    export async function sendWelcomeEmail(
      params: SendWelcomeEmailParams
    ): Promise<SendEmailResult>
    ```
    - `isEmailConfigured()` チェックを最初に行う
    - Resend APIを使って WelcomeEmail テンプレートをレンダリングして送信
    - 件名: `「かずみん時間」へようこそ！`

    **手順3: src/lib/actions/admin/members.ts の createMember() を修正**

    `generateLink` の直後に sendWelcomeEmail を非ブロッキングで呼び出す：
    ```typescript
    const resetUrl = linkData?.properties?.action_link ?? null

    // 非ブロッキング: 失敗しても会員作成は成功扱い
    try {
      await sendWelcomeEmail({
        userEmail: validated.email,
        userName: validated.full_name,
        passwordResetUrl: resetUrl,
      })
    } catch (error) {
      console.warn("[createMember] Welcome email failed (non-blocking):", error)
    }
    ```
    `sendWelcomeEmail` を email.ts からインポートすること。
  </action>
  <verify>
    <automated>cd "/Users/kugyu10/work/かずみん/Time-with-Kazumin" && npx vitest run src/__tests__/lib/integrations/email.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    email.test.ts の全テスト（既存 + 新規4件）がパスすること。
    WelcomeEmail.tsx が存在すること。
    email.ts に sendWelcomeEmail が export されていること。
    members.ts に sendWelcomeEmail の呼び出しが追加されていること。
  </done>
</task>

<task type="auto">
  <name>Task 3: BUG-02/BUG-03 全画面JST時刻表示統一 + docs/rules.md作成</name>
  <files>
    src/app/(member)/bookings/[id]/page.tsx,
    src/components/bookings/BookingCard.tsx,
    src/app/admin/bookings/columns.tsx,
    src/app/(public)/guest/booking/success/page.tsx,
    src/app/(public)/guest/cancel/[token]/page.tsx,
    docs/rules.md
  </files>
  <action>
    **JST修正パターン（全ファイル共通）:**

    `timeZone: "Asia/Tokyo"` を欠いている全ての日時フォーマット関数に追加する。参照パターンは `src/emails/BookingConfirmation.tsx` の `formatDateTime()` を使うこと（既に正しく実装済み）。

    **各ファイルの修正:**

    1. `src/app/(member)/bookings/[id]/page.tsx`
       - `formatDateTimeFull()` に `timeZone: "Asia/Tokyo"` を追加する

    2. `src/components/bookings/BookingCard.tsx`
       - `formatDateTime()` に `timeZone: "Asia/Tokyo"` を追加する

    3. `src/app/admin/bookings/columns.tsx`
       - `date-fns` の `format()` は timeZone が効かないため、`toLocaleString("ja-JP", { timeZone: "Asia/Tokyo", ... })` ベースに書き換える
       - date-fns-tz の追加インストールは不要（YAGNI原則）

    4. `src/app/(public)/guest/booking/success/page.tsx`
       - `toLocaleDateString()` / `toLocaleTimeString()` に `timeZone: "Asia/Tokyo"` オプションを追加する

    5. `src/app/(public)/guest/cancel/[token]/page.tsx`
       - `toLocaleDateString()` / `toLocaleTimeString()` に `timeZone: "Asia/Tokyo"` オプションを追加する

    **docs/rules.md 新規作成:**

    プロジェクトルートの `docs/` ディレクトリに `rules.md` を新規作成する。以下の内容を含めること：

    - UTC/JST変換規約のセクション
    - NG パターン（`format(date, "HH:mm")` や timeZone未指定の toLocaleString）
    - OK パターン（`toLocaleString("ja-JP", { timeZone: "Asia/Tokyo", ... })`）
    - Vercel環境がUTCで動作する理由の説明
    - date-fns は timeZone 非対応のため使用禁止（日時フォーマット用途限定）
  </action>
  <verify>
    <automated>cd "/Users/kugyu10/work/かずみん/Time-with-Kazumin" && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npx vitest run` が全件パスすること。
    5ファイル全てに `timeZone: "Asia/Tokyo"` が含まれていること（grep確認）。
    docs/rules.md が存在し「Asia/Tokyo」の記述があること。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: BUG-02/BUG-03/BUG-04 Vercel preview目視確認</name>
  <action>Vercel preview URLで各修正点を目視確認する（自動化不可：UIレンダリングとメール受信確認が必要）</action>
  <what-built>
    BUG-02/BUG-03/BUG-04 の修正が完了した。
    - 全画面（ゲスト/会員/管理者）の時刻表示を JST に統一
    - 管理者が会員を招待するとウェルカムメールが送信される
    - docs/rules.md にコーディング規約を明文化
  </what-built>
  <how-to-verify>
    1. Vercel preview URL（develop ブランチ）にアクセスして以下を確認:
       a. 予約詳細画面（/booking/[id]）で時刻が JST（例: 14:00）で表示されること
       b. ゲスト予約完了画面（/guest/booking/success）で時刻が JST で表示されること
       c. 管理者予約一覧画面（/admin/bookings）で時刻が JST で表示されること
    2. 管理画面から会員を招待し、招待した会員のメールボックスにウェルカムメールが届くことを確認
    3. ウェルカムメールにパスワード設定リンク（1時間有効）が含まれていることを確認
  </how-to-verify>
  <verify>
    <automated>MISSING — 目視確認のみ必要（UIレンダリングとメール受信確認はVercel preview + 実メール送信が必要）</automated>
  </verify>
  <done>JST表示が全画面で確認済み、ウェルカムメールが受信確認済み</done>
  <resume-signal>目視確認が完了したら「approved」と入力、または問題があれば内容を記述してください</resume-signal>
</task>

</tasks>

<verification>
```bash
cd "/Users/kugyu10/work/かずみん/Time-with-Kazumin" && npx vitest run 2>&1 | tail -20
```
全テストがパスすること。

JST修正の確認:
```bash
grep -l "timeZone.*Asia/Tokyo" \
  "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/app/(member)/bookings/[id]/page.tsx" \
  "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/components/bookings/BookingCard.tsx" \
  "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/app/admin/bookings/columns.tsx" \
  "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/app/(public)/guest/booking/success/page.tsx" \
  "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/app/(public)/guest/cancel/[token]/page.tsx"
```
5ファイル全てが一致すること。

ウェルカムメール実装確認:
```bash
ls "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/emails/WelcomeEmail.tsx"
grep "sendWelcomeEmail" "/Users/kugyu10/work/かずみん/Time-with-Kazumin/src/lib/actions/admin/members.ts"
```

docs/rules.md確認:
```bash
grep "Asia/Tokyo" "/Users/kugyu10/work/かずみん/Time-with-Kazumin/docs/rules.md"
```
</verification>

<success_criteria>
- email.test.ts の sendWelcomeEmail テスト4件を含む全テストがパス
- 5つのUIファイル全てに `timeZone: "Asia/Tokyo"` が追加されている
- WelcomeEmail.tsx が作成され、sendWelcomeEmail が email.ts に実装されている
- createMember() が非ブロッキングでウェルカムメールを送信する
- docs/rules.md にUTC/JST変換規約が記載されている
- 人間によるVercel preview目視確認が完了している
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes/08-02-SUMMARY.md`
</output>
